# Undone issues

| | |
|---|---|
| **Session** | `0103d436-541e-4695-adbd-8b5fdc5ae0ef` |
| **Started** | 2026-08-29T13:31:39.481Z |
| **Ended** | 2026-08-29T13:38:54.227Z |
| **Working dir** | `~/Downloads/projects/shopify/.claude/worktrees/repo-ui-issues-status-afad25` |
| **Branch** | `ws-e/discount-keeps-shipping` |
| **Model** | `claude-opus-5` |
| **Claude Code** | 2.1.247 |
| **Archived** | no |

---

### 👤 User

what issues are undone

### 🤖 Claude

I'll check the issue backlog and the agent log to see what's claimed and what's still open.

🔧 **Bash**
```
ls /Users/pauljurgens/Downloads/projects/shopify/.claude/worktrees/repo-ui-issues-status-afad25/docs/issues/
```

🔧 **Bash**
```
cat /Users/pauljurgens/Downloads/projects/shopify/.claude/worktrees/repo-ui-issues-status-afad25/docs/AGENT-LOG.md
```

> **↳ result**
> ```
> A1-auth-sessions-tenancy.md
> A2-tenancy-suite.md
> A3-admin-shell.md
> A4-settings-hub.md
> A5-production-stack.md
> B1-products-api.md
> B2-files-media.md
> B3-collections-api.md
> B4-inventory-locations-api.md
> B5-products-admin-ui.md
> B6-collections-inventory-admin-ui.md
> B7-variant-images.md
> C1-discounts-engine.md
> C2-orders-api.md
> C3-fulfillment-refund-api.md
> C4-customers-api.md
> C5-orders-admin-ui.md
> C6-customers-discounts-admin-ui.md
> C7-orders-index-filters.md
> D1-vault.md
> D2-processor-adapters.md
> D3-payment-router.md
> D4-payments-settings-ui.md
> E1-storefront-api-cart.md
> E2-storefront-pages.md
> E3-checkout-api.md
> E4-checkout-ui.md
> E5-customer-accounts.md
> E6-checkout-save-card.md
> E7-theme-publish-revalidatio
> … [truncated, 443 more chars]
> ```

> **↳ result**
> ```
> <persisted-output>
> Output too large (105.9KB). Full output saved to: /Users/pauljurgens/.claude/projects/-Users-pauljurgens-Downloads-projects-shopify--claude-worktrees-repo-ui-issues-status-afad25/0103d436-541e-4695-adbd-8b5fdc5ae0ef/tool-results/b0zj2tj57.txt
> 
> Preview (first 2KB):
> # Agent log
> 
> Append-only scratch log for cross-agent visibility. `merge=union`: append at the
> bottom, never edit existing lines.
> 
> Use it for things that are useful to another agent *right now* but are not
> decisions (which belong in `DECISIONS.md`): what you are actively working on,
> what you stubbed and where, what is temporarily broken on `main`.
> 
> Format: `YYYY-MM-DD HH:MM | WS-X | message`
> 
> ---
> 2026-08-28 11:35
> … [truncated, 1,434 more chars]
> ```

🔧 **Bash**
```
cat /Users/pauljurgens/Downloads/projects/shopify/.claude/worktrees/repo-ui-issues-status-afad25/docs/issues/INDEX.md
```

> **↳ result**
> ```
> # Issue index
> 
> One line per issue. **Do not edit this file to track status** — status lives in
> `docs/AGENT-LOG.md` (append-only, conflict-free); this file only changes when an
> issue is added or re-scoped. Sizes: S ≈ ≤2h, M ≈ half-day, L ≈ full-day.
> 
> Grab order: anything whose deps are all landed. At the start, that is
> **A1, C1, D1, D2, F1, G1** (+ A5, which is dep-free but scheduled for
> Day 2) — six agents can start immediately, and A1 unblocks the rest of the
> swarm, so it goes first to the strongest agent.
> 
> **UI issues**: [PARITY.md](PARITY.md) is the shared, binding description of
> what Shopify actually looks like — the defense against eight agents shipping
> eight interpretations. Read your
> … [truncated, 4,833 more chars]
> ```

🔧 **Bash**
```
grep -oE '(CLAIM|DONE|UNCLAIM|ABANDON)[A-Za-z]* [A-Z][0-9]+' /Users/pauljurgens/.claude/projects/-Users-pauljurgens-Downloads-projects-shopify--claude-worktrees-repo-ui-issues-status-afad25/0103d436-541e-4695-adbd-8b5fdc5ae0ef/tool-results/b0zj2tj57.txt | sort | uniq -c | sort -k3
```

> **↳ result**
> ```
> 1 CLAIM A1
>    1 DONE A1
>    1 CLAIM A2
>    1 DONE A2
>    1 CLAIM A3
>    1 DONE A3
>    1 CLAIM A4
>    1 DONE A4
>    1 CLAIM A5
>    1 DONE A5
>    1 CLAIM B1
>    1 DONE B1
>    1 CLAIM B2
>    1 DONE B2
>    1 CLAIM B3
>    1 DONE B3
>    1 CLAIM B4
>    1 DONE B4
>    1 CLAIM B5
>    1 DONE B5
>    1 CLAIM B6
>    1 DONE B6
>    1 CLAIM C1
>    1 DONE C1
>    1 CLAIM C2
>    1 DONE C2
>    1 CLAIM C3
>    1 DONE C3
>    1 CLAIM C4
>    1 DONE C4
>    1 CLAIM C5
>    1 DONE C5
>    1 CLAIM C6
>    1 DONE C6
>    1 CLAIM C7
>    1 DONE C7
>    1 CLAIM D1
>    1 DONE D1
>    1 CLAIM D2
>    1 DONE D2
>    1 CLAIM D3
>    1 DONE D3
>    1 CLAIM D4
>    1 DONE D4
>    1 CLAIM E1
>    1 DONE E1
>    1 CLAIM E2
>    1 DONE E2
>    1 CLAIM E3
>    1 DONE E3
>    1 CLAIM E4
>    1 DONE E4
> … [truncated, 443 more chars]
> ```

🔧 **Bash**
```
grep -nE 'E8|E9|B7|H6' /Users/pauljurgens/.claude/projects/-Users-pauljurgens-Downloads-projects-shopify--claude-worktrees-repo-ui-issues-status-afad25/0103d436-541e-4695-adbd-8b5fdc5ae0ef/tool-results/b0zj2tj57.txt | tail -20
```

> **↳ result**
> ```
> 246:2026-08-29T13:35Z | WS-QA | FILED H6 (docs/issues/H6-next-start-standalone.md, in INDEX): both Next apps set `output: 'standalone'` for the Dockerfiles, and both `package.json` `start` scripts — which `e2e/playwright.config.ts` boots the admin and storefront with — run `next start`, which Next prints "does not work with output: standalone" on every run. Not cosmetic: on that stack the storefront's Add-to-cart Server Action lands server-side but its `revalidatePath` never reaches the client (POST 200 then `net::ERR_ABORTED`), so the button sticks on "Adding…" forever and the header cart badge stays stale — the exact thing `cartRequest`'s revalidate comment claims to prevent. Identical ste
> … [truncated, 2,576 more chars]
> ```

### 🤖 Claude

Three issues remain undone — everything else in the backlog (A1–H6, 46 of 49) is claimed and logged DONE in `docs/AGENT-LOG.md`:

| Issue | Title | Size | Status |
|---|---|---|---|
| [E8](docs/issues/E8-storefront-action-hang-in-production.md) | Storefront Server Actions never settle in a production build | M | Filed, unclaimed |
| [E9](docs/issues/E9-checkout-discount-keeps-shipping.md) | Applying a discount must not drop the chosen shipping method | S | Filed, unclaimed |
| [B7](docs/issues/B7-variant-images.md) | Variant images: populate `variantIds`, wire the PDP gallery swap | M | Filed, unclaimed |

All three were filed late yesterday during the QA/repo-review passes, and none has a CLAIM line in the agent log. Context from the log:

- **E8** — under a production build (`next start` or the standalone `server.js`), add-to-cart's Server Action POST returns 200 but the browser aborts the response stream, so the button hangs on "Adding…". `next dev` is unaffected, so the demo path is fine. WS-H already ruled out several suspects (revalidatePath, middleware header rewrite, cookies, duplicate React) and left a test harness plus next steps in the issue.
- **E9** — applying a discount at checkout clears a still-qualifying selected shipping rate; the sidebar reverts to "Enter shipping address" on the page a shopper pays on. Small fix, high demo visibility.
- **B7** — `productImageSchema.variantIds` has three API readers but zero writers, and the product form erases the field on save, so the PDP gallery swap can never trigger. This also absorbs H4's `variant.imageUrl` handoff.

If you want one picked up, E9 is the smallest and the most demo-visible; say the word and I'll run resolve-issue on it.

### 👤 User

<local-command-caveat>Caveat: The messages below were generated by the user while running local commands. DO NOT respond to these messages or otherwise consider them in your response unless the user explicitly asks you to.</local-command-caveat>

### 👤 User

<command-name>/model</command-name>
            
            <command-args>claude-opus-5</command-args>

### 👤 User

do e9 and use the resolve issue skill

### 🤖 Claude

I'll pick up E9 using the resolve-issue skill.

🔧 **Skill**
```
{"skill": "resolve-issue", "args": "E9"}
```

> **↳ result**
> ```
> Launching skill: resolve-issue
> ```

### 👤 User

Base directory for this skill: /Users/pauljurgens/Downloads/projects/shopify/.claude/worktrees/repo-ui-issues-status-afad25/.claude/skills/resolve-issue

# Resolving an issue

Read [CLAUDE.md](../../../CLAUDE.md), [SPEC.md](../../../SPEC.md) and
[docs/issues/README.md](../../../docs/issues/README.md) first. This is the
operating experience on top of them: the things that are not policy, and that
cost real time when ignored.

**The bar:** a merged PR whose behaviour you have *seen work*, not one that
compiles and has green tests.

**This skill vs the others:** `resolve-issue` *builds*.
[`critical-review`](../critical-review/SKILL.md) audits one named scope in depth
and only reports. [`repo-review`](../repo-review/SKILL.md) surveys the whole
build, drives the app in a browser, and lands the small fixes itself.

**The tiebreaker, for every judgement call below:** the KPI — a Shopify user
opens our admin and cannot tell it isn't Shopify. Resolve trade-offs in
CLAUDE.md §0's order — appearance parity → functionality → performance →
everything else. When two defensible options exist, take the one that serves
that, log a line in `DECISIONS.md`, and keep moving.

### Delegating — leverage, not ceremony

Delegating is available and sometimes clearly better. It is never required, and
forcing it costs more than it saves.

**Worth delegating:**

- A question that spans many files where you only want the conclusion — "which
  workstreams call this helper", "where is tenant scoping applied inconsistently".
  A read-only search agent returns the answer instead of filling your context
  with file dumps.
- Genuinely independent work you would otherwise do serially — several unrelated
  audits, or reading three subsystems at once before a design decision.
- An adversarial second pass on a finished diff — that is what
  [`critical-review`](../critical-review/SKILL.md) is for. Running it over your
  own work before pushing is cheap and catches what you have gone blind to.

**Not worth it:**

- A focused change in files you already have open. The handoff costs more than
  the work.
- Anything needing your live state — a running dev server, a logged-in browser
  session, uncommitted edits. A subagent does not share them, and will report
  confidently about a stack it cannot see.
- A single-fact lookup you could answer with one `grep`.

**The rule that does not bend:** a subagent's report is not verification. It can
tell you where to look and what it believes; it cannot discharge "I have seen
this work". Treat its findings as leads to confirm, not conclusions to relay —
they are sometimes confidently wrong, and the bar at the top of this file is
still yours to meet.

---

## 1. Pick and claim

```bash
git fetch origin
git show origin/main:docs/AGENT-LOG.md | grep -E "CLAIM|DONE"   # what is taken
git ls-remote --heads origin                                     # what is in flight
gh pr list --state open
```

Available means **every dependency is `DONE` in the log** and no branch or open
PR exists for it. An unfinished claim under ~3h old belongs to someone else.
Then, in one commit:

```bash
git checkout -B ws-{x}/{slug} origin/main
# append to docs/AGENT-LOG.md:  <ISO time> | WS-X | CLAIM {id} … | branch ws-{x}/{slug}
```

Prefer the issue that **unblocks the most others** — read the INDEX dependency
graph, not the ID order.

**Confirm the choice with whoever asked before you start**, with the state you
found: what is landed, what is in flight, and why this one. Picking is the one
decision worth a round trip — it commits hours and it is the easiest to get
wrong when three agents are moving. Everything after it is yours to decide.

### Decide alone; escalate almost never

You have the repo, the contracts and `DECISIONS.md`. That is enough for
essentially every question the work raises. Ambiguity is resolved by picking the
option that serves the KPI, logging one line in `DECISIONS.md`, and continuing —
never by stopping.

Escalate only when proceeding under *any* assumption would be unsafe or would
waste the work if wrong. "Which of these two layouts?" is not that. "This issue
contradicts a landed decision" might be. When you do ask, do the parts that do
not depend on the answer first, and ask with a recommendation rather than a
menu.

---

## 2. Read in this order — later sources lose

1. **`packages/contracts/**` — the integration contract beats the issue's prose.**
   Where an issue described a request shape one way and the contract another, the
   contract was right both times: it is what the other seven workstreams code
   against, and "fixing" it to match the prose would have been a breaking change
   for nothing.
2. **Registries and shared config** — `navigation/`, `schema.prisma`,
   `launch.json`, `packages/config/constants.ts`. These are pre-built complete
   and often asserted by a test. When an issue's file paths disagree with the
   registry, the registry wins.
3. **`DECISIONS.md`** — never relitigate a logged decision.
4. **`PARITY.md`** — binding for anything with a screen. Read your page's
   section before writing JSX.
5. The issue's own prose — last, and it is a sketch.

When 1–4 contradict the issue, follow them and **log the divergence in
DECISIONS.md in the same PR**.

---

## 3. Test-driven — and only tests that earn their place

`docs/issues/README.md` calls this **test-driven, feedback-based**. The feedback
half is not optional: running the real thing and reading your own diff are
*inside* the loop, not gates at the end of it.

**The loop, in this order, every time:**

1. **Write the test first**, from the issue's own Test plan.
2. **Run it and watch it fail — and check the failure is the RIGHT one.** A
   missing endpoint fails `404 Endpoint not found`; a missing DTO field fails on
   `undefined`; a missing rule fails the assertion, not the setup. A red bar for
   the wrong reason is not a red bar.
3. Implement until green.
4. **Mutation-check** the assertions that carry the weight (below).
5. **Run the real thing and look at it** (§4). What you see here feeds straight
   back to step 1 — every bug found by hand gets a test before it gets a fix.
6. **Review your own diff** (§5), fix what it turns up, and go round again.

Steps 5 and 6 are where most of the real bugs came from. Treat a green suite as
permission to start looking, not as the finish line.

Step 2 is the one that gets skipped, and skipping it is how a test that asserts
nothing ends up in the suite. **If a test passes the first time you run it, stop
and find out why** — either the behaviour already existed (so the test is
documentation, not verification), or the assertion is vacuous, or you are not
sending the input you think you are.

That last case is the one that got me. A suite I wrote test-first passed
entirely on its first run. I noticed, mutation-checked it, satisfied myself it
was not vacuous — and it still shipped three bugs, because an all-green first
run also means nothing has told you which inputs you never sent. Test-first is
necessary and not sufficient; a suite that has never been red is a suite you
have not interrogated.

Writing tests after the fact is a fallback, not the plan. When you do it — a
refactor, a bug found by hand — make the test fail against the *old* behaviour
before you keep it.

### Which tests earn their place

SPEC §14 lists what not to write. This is the other half. Before writing a test,
answer: **what does it fail on, and would that bug otherwise ship silently?** If
there is no answer, do not write it — an invented test is a maintenance cost and
a false signal, not a safety net.

Worth testing wherever they appear:

- **Money.** Integer minor units on the wire, strings in inputs, converted once
  at the boundary. `"1.005"` must become `101`, not `100`. This is not one
  workstream's problem: prices, discount math, order totals, refund caps,
  checkout sums and rule conditions all carry it.
- **Rules with a "do not" attached.** The landmines in CLAUDE.md §9 exist because
  the wrong behaviour is plausible — a decline must *not* cascade to the next
  processor, a quantity must *not* move without an adjustment row, a webhook HMAC
  must be over the *raw* body. Each deserves the test that fails when someone
  does the plausible thing.
- **Concurrency, where it is the point.** Anything that increments, allocates a
  sequence, or caps a total. Replacing an atomic write with read-then-write lost
  4 of 8 simultaneous decrements — assert the sum, not one call.
- **Duplicated logic.** Whenever a client previews what the server will do, or a
  seed reproduces what a service does, pin both to the same expectation or they
  drift silently.
- **Anything that writes permanent history** — adjustments, order events,
  payments. An input touched and restored must produce *no* row.
- **The one tenancy hole your own query opens.** General isolation is A2's suite;
  an `OR` clause you added to a list query is yours. A search that reaches a
  neighbouring shop is the unforgivable bug.

### Mutation-check the load-bearing assertions

Break the implementation deliberately and confirm the *right* test fails —
reverse an ordering, swap an atomic write for read-then-write, swap the scoped
client for the unscoped one, neuter a sanitiser. Restore immediately.

**This proves a test is not vacuous. It does not prove coverage.** A suite of
mine survived every mutation and still missed a bug, because I had only ever
sent the full and empty versions of a payload, never a partial one. Ask
separately: *which shapes of input have I never sent?*

### When a test fails, decide which side is wrong

More than once the test was wrong, not the code. Fix the assertion and say so.
Do not bend an implementation to a mistaken expectation.

---

## 4. Verify by running it

Green tests are not verification. Opening one admin page in a browser found two
bugs in ten minutes that fifteen unit tests had missed. Match the method to the
surface:

| Surface | How |
|---|---|
| API (`apps/api`) | `curl` against a live server; assert the SPEC §5 error shape on the failure paths too |
| Admin (Polaris) | browser — see the recipe below |
| Storefront / checkout (Tailwind) | browser at `{slug}.localhost:3002` |
| Worker / jobs | enqueue a real job, read the log and the DB row it wrote |
| Seed / data | `pnpm db:query` — reconcile what you wrote against what it implies |

### Browser recipe

CLAUDE.md §1 has the canonical version. What it does not say loudly enough:

- Start **`dev-localhost`**, not `dev`. The pane only loads `_next/static/*` for
  the origin of a *registered* preview server, and only the localhost variants
  are registered — browse `http://localhost:3000` (admin) and
  `http://demo.localhost:3002` (storefront). On `*.lvh.me` every asset is
  `ERR_BLOCKED_BY_CLIENT` and you get unstyled HTML with no JS, which looks
  exactly like a broken page rather than a blocked one.
- **Check who owns the port before trusting what you see.** Another worktree's
  server will happily serve you *their* code; a stale placeholder page is the
  tell.
  ```bash
  lsof -a -p "$(lsof -nP -iTCP:3000 -sTCP:LISTEN | awk 'NR==2{print $2}')" -d cwd -Fn
  ```
  If it is not yours, do **not** just kill it — that is someone's running work.
  Start your own pair on free ports and add a temporary url-only entry to
  `.claude/launch.json` (url + no command = attach), then revert it before
  committing.
- A `_merchant_session` cookie minted by another worktree's API fails signature
  checks against yours and is indistinguishable from a broken login. Clear it:
  ```js
  fetch('http://localhost:3001/auth/logout', {method:'POST', credentials:'include',
    headers:{'x-requested-with':'merchant-admin'}})
  ```
- Background dev servers get SIGTERM'd by the task runner; `nohup … & disown`
  survives.
- Drive inputs with `form_input` and a `ref` — coordinate typing often fails to
  reach React state. Coordinates are in the *last screenshot's* frame and g
… [truncated, 6,326 more chars]

### 🤖 Claude

I'll start by reading the issue and checking what's in flight.

🔧 **Bash**
```
cat docs/issues/E9-checkout-discount-keeps-shipping.md
```

> **↳ result**
> ```
> # E9 — applying a discount must not drop the chosen shipping method
> 
> | | |
> |---|---|
> | Workstream | E |
> | Size | S |
> | Depends on | E3, E4 (landed) |
> | Unblocks | the demo's optional WELCOME10 beat reading as polished |
> | Branch | `ws-e/discount-keeps-shipping` |
> 
> ## You own
> ```
> apps/api/src/services/checkout/checkout.ts   (the PUT/update path)
> apps/api/src/services/checkout/totals.ts     (rate re-qualification)
> apps/storefront/src/components/checkout/**   (only if the fix is client-side)
> ```
> 
> ## Context (found in repo review, 2026-08-29, driving the live checkout)
> 
> Fill the address, pick **Standard shipping ($8.95)**, then type `WELCOME10`
> and Apply. The discount applies (−$1.80, tax recomp
> … [truncated, 1,822 more chars]
> ```

🔧 **Bash**
```
git fetch origin -q && git ls-remote --heads origin | tail -20 && echo "--- PRs ---" && gh pr list --state open
```

> **↳ result**
> ```
> 6a3a4697df8f1438453ceaf21aadd87d5fc8b883	refs/heads/ws-d/adapters
> 46e598127063f64d28d66fb8a220d3a31329aac5	refs/heads/ws-d/mock-ledger-across-processes
> 7f1b24421eea3b2a2c8ea285aef045a588f05670	refs/heads/ws-d/payment-router
> 1f5f92904442ceb4a4fcbb78a019cf2ab090bd74	refs/heads/ws-d/refund-race-capture-guard
> 67d3c6dac3d9854fc7721a2a08b658c0bc9f5241	refs/heads/ws-e/storefront-pages
> 0cf2d8337a971e1d3984a33cc85b9eb0d171457d	refs/heads/ws-e/storefront-seams
> f13735146357753f9202e147c1cacc2968fbb5d3	refs/heads/ws-f/ai-timeout-ladder
> 94ac398a16cb27480065da69a6ce89bc7f7e914c	refs/heads/ws-f/builder-admin-ui
> 1a1dc0057a53ee84c712c06a7c8899e93f3d2dfd	refs/heads/ws-f/themes-api-ai-job
> 626cb3261412abf53b002
> … [truncated, 958 more chars]
> ```

### 🤖 Claude

No branch or PR for E9 — it's free. Claiming it and reading the checkout code.

🔧 **Bash**
```
git checkout -B ws-e/discount-keeps-shipping origin/main 2>&1 | tail -3 && git log --oneline -1
```

> **↳ result**
> ```
> Switched to a new branch 'ws-e/discount-keeps-shipping'
> branch 'ws-e/discount-keeps-shipping' set up to track 'origin/main' by rebasing.
> fee606d docs(shared): repo-review findings — E9/B7 filed, DEMO timing realigned, third-pass log (#115)
> ```

🔧 **Bash**
```
ls apps/api/src/services/checkout/ && echo "--- storefront checkout ---" && ls apps/storefront/src/components/checkout/
```

> **↳ result**
> ```
> checkout.ts
> complete.ts
> totals.ts
> --- storefront checkout ---
> card-fields.tsx
> checkout-view.tsx
> order-summary.tsx
> ```

🔧 **Read**
```
/Users/pauljurgens/Downloads/projects/shopify/.claude/worktrees/repo-ui-issues-status-afad25/apps/api/src/services/checkout/checkout.ts
```

> **↳ result**
> ```
> 1	/**
> 2	 * Checkout lifecycle (SPEC §10). Owner: WS-E.
> 3	 *
> 4	 * A checkout is a FROZEN copy of the cart. The cart reprices from live variants
> 5	 * on every read (E1); a checkout must not — the shopper is committing to the
> 6	 * number in front of them, and a price edit mid-payment that silently changes
> 7	 * what the card is charged is indefensible.
> 8	 *
> 9	 * The cart deliberately survives creation, so an abandoned checkout leaves the
> 10	 * shopper's cart where they left it.
> 11	 *
> 12	 * Pricing lives entirely in `totals.ts`; nothing here computes money.
> 13	 */
> 14	import { newId, newSecret } from '@merchant/config/ids';
> 15	import type { CartLine } from '@merchant/contracts/cart';
> 16	import typ
> … [truncated, 14,102 more chars]
> ```

### 🤖 Claude

The API update path already guards `undefined` correctly. Let me look at the re-qualification in `totals.ts`.

🔧 **Read**
```
/Users/pauljurgens/Downloads/projects/shopify/.claude/worktrees/repo-ui-issues-status-afad25/apps/api/src/services/checkout/totals.ts
```

> **↳ result**
> ```
> 1	/**
> 2	 * Checkout pricing (SPEC §10). Owner: WS-E.
> 3	 *
> 4	 * **The only place a checkout total is computed.** Every step update, the
> 5	 * shipping-rate list and the final charge all call `computeCheckoutTotals`, so
> 6	 * the sidebar and the card can never disagree — drift between the two is the
> 7	 * worst bug this surface has.
> 8	 *
> 9	 * The order of operations, and why:
> 10	 *
> 11	 *   1. C1's engine prices the lines. Product discounts, then order discounts —
> 12	 *      the engine owns that, we never re-derive it.
> 13	 *   2. Shipping rates are filtered on the DISCOUNTED subtotal. A "free over
> 14	 *      $150" rate must stop applying when a coupon takes the order under $150,
> 15	 *      which i
> … [truncated, 6,755 more chars]
> ```

### 🤖 Claude

API side looks correct — `undefined` is preserved and the rate is re-found by id. Let me check the storefront client, which is where the PUT is actually built.

🔧 **Bash**
```
wc -l apps/storefront/src/components/checkout/checkout-view.tsx apps/storefront/src/components/checkout/order-summary.tsx && echo "--- actions ---" && grep -rln "discountCode" apps/storefront/src --include=*.ts --include=*.tsx
```

> **↳ result**
> ```
> 419 apps/storefront/src/components/checkout/checkout-view.tsx
>      154 apps/storefront/src/components/checkout/order-summary.tsx
>      573 total
> --- actions ---
> (eval):1: no matches found: --include=*.ts
> ```

### 🤖 Claude

Let me answer that while continuing — reading B7 alongside the checkout client.

🔧 **Bash**
```
cat docs/issues/B7-variant-images.md
```

> **↳ result**
> ```
> # B7 — variant images: populate `variantIds`, or the PDP gallery can never swap
> 
> | | |
> |---|---|
> | Workstream | B (form + API), with one read already waiting in E |
> | Size | M |
> | Depends on | B5 (landed) |
> | Unblocks | PDP variant → image swap (PARITY §Motion "instant, client-side") |
> | Branch | `ws-b/variant-images` |
> 
> ## You own
> ```
> apps/admin/src/lib/product-draft.ts
> apps/admin/src/app/store/[slug]/products/_components/**   (media card)
> apps/api/src/services/catalog/products.ts                 (write passthrough already exists)
> apps/storefront/src/components/product-form.tsx           (gallery swap — coordinate with WS-E)
> ```
> 
> ## Context (found in repo review, 2026-08-29, contracts sweep
> … [truncated, 2,182 more chars]
> ```

🔧 **Bash**
```
grep -rn "discountCode" apps/storefront/src | head -30
```

> **↳ result**
> ```
> apps/storefront/src/components/checkout/checkout-view.tsx:343:          onApplyDiscount={(code) => save({ discountCode: code || null })}
> apps/storefront/src/components/checkout/order-summary.tsx:25:  const [code, setCode] = useState(checkout.discountCode ?? '');
> ```

🔧 **Read**
```
/Users/pauljurgens/Downloads/projects/shopify/.claude/worktrees/repo-ui-issues-status-afad25/apps/storefront/src/components/checkout/checkout-view.tsx
```

> **↳ result**
> ```
> 1	'use client';
> 2	
> 3	/**
> 4	 * The checkout screen (SPEC §10, PARITY.md "Checkout (E4)"). Owner: WS-E.
> 5	 *
> 6	 * Shopify's single-page checkout: sections stack in one column, each saving as
> 7	 * the shopper leaves it, with the summary sidebar repricing on every save. The
> 8	 * layout order is PARITY's and is not negotiable — express row, Contact,
> 9	 * Delivery, shipping methods, Payment, Pay now.
> 10	 *
> 11	 * State lives here and totals never do: every number rendered comes from the
> 12	 * `Checkout` E3 last returned, so the sidebar cannot disagree with the charge.
> 13	 *
> 14	 * The card is the one thing this component does not hold — `CardFields` keeps
> 15	 * it and hands back a vault token (SPEC
> … [truncated, 15,638 more chars]
> ```

### 🤖 Claude

The client and service both preserve `undefined`. Checking the contract schema and the server action — a zod `.default()` would turn absent into null.

🔧 **Bash**
```
grep -n "updateCheckoutInput" -A 30 packages/contracts/src/checkout.ts | head -60
```

> **↳ result**
> ```
> 83:export const updateCheckoutInput = z.object({
> 84-  email: z.string().email().optional(),
> 85-  phone: z.string().max(64).nullable().optional(),
> 86-  acceptsMarketing: z.boolean().optional(),
> 87-  shippingAddress: addressSchema.optional(),
> 88-  billingAddress: addressSchema.nullable().optional(),
> 89-  billingSameAsShipping: z.boolean().optional(),
> 90-  selectedShippingRateId: idSchema.nullable().optional(),
> 91-  discountCode: z.string().max(64).nullable().optional(),
> 92-  note: z.string().max(2000).nullable().optional(),
> 93-});
> 94:export type UpdateCheckoutInput = z.infer<typeof updateCheckoutInput>;
> 95-
> 96-/** "Pay now". `cardTokenId` came from the vault; this API never sees a PAN. */
> 97-e
> … [truncated, 1,019 more chars]
> ```

🔧 **Bash**
```
cat apps/storefront/src/lib/checkout-actions.ts
```

> **↳ result**
> ```
> 'use server';
> 
> /**
>  * Checkout mutations as Server Actions (SPEC §10). Owner: WS-E.
>  *
>  * **No card data passes through here, ever.** The browser posts the PAN
>  * straight to `/vault/tokenize` and only the resulting `card_tok_…` reaches
>  * these functions — that separation is the entire point of the vault
>  * (SPEC §11, CLAUDE.md §9). If you find yourself adding a `number` or `cvc`
>  * parameter below, stop.
>  *
>  * Actions rather than browser fetches because `complete` has to forward and
>  * then clear the cart cookie, which is httpOnly and scoped to the storefront
>  * origin — a cross-origin call from the browser would never send it.
>  */
> import { CART_COOKIE } from '@merchant/config/constants';
> … [truncated, 3,874 more chars]
> ```

### 🤖 Claude

Client, action, and contract all preserve `undefined`. Let me check the route handler and the actual seeded shipping rates.

🔧 **Bash**
```
grep -rn "checkouts/:token" -A 20 apps/api/src/routes/storefront/*.ts | grep -n "put\|updateCheckout\|parse" | head -20; echo "=== route files ==="; grep -rln "updateCheckout" apps/api/src/routes/
```

> **↳ result**
> ```
> 2:apps/api/src/routes/storefront/checkouts.ts-50-    const { token } = tokenParam.parse(request.params);
> 3:apps/api/src/routes/storefront/checkouts.ts-51-    return checkoutSchema.parse(await getCheckout(request.db, token));
> 7:apps/api/src/routes/storefront/checkouts.ts:55:  app.put('/checkouts/:token', async (request) => {
> 8:apps/api/src/routes/storefront/checkouts.ts-56-    const { token } = tokenParam.parse(request.params);
> 9:apps/api/src/routes/storefront/checkouts.ts-57-    const input = updateCheckoutInput.parse(request.body);
> 10:apps/api/src/routes/storefront/checkouts.ts-58-    return checkoutSchema.parse(await updateCheckout(request.db, token, input));
> 15:apps/api/src/routes/storefr
> … [truncated, 534 more chars]
> ```

🔧 **Bash**
```
grep -rn "shippingRates" packages/db/prisma/seed*.ts packages/db/src/seed*.ts packages/db/prisma/seed/*.ts 2>/dev/null | head -10
```

> **↳ result**
> ```
> (eval):1: no matches found: packages/db/prisma/seed*.ts
> ```

🔧 **Bash**
```
grep -rn "shippingRates" packages/db/src/ | head -10
```

> **↳ result**
> ```
> (Bash completed with no output)
> ```

🔧 **Bash**
```
grep -rn "WELCOME10" packages/db/src/ | head -10
```

> **↳ result**
> ```
> (Bash completed with no output)
> ```

🔧 **Bash**
```
grep -rn "shippingRates" --include=*.ts packages/ apps/api/src | grep -v node_modules | head -20
```

> **↳ result**
> ```
> (eval):1: no matches found: --include=*.ts
> ```

🔧 **Bash**
```
grep -rn "WELCOME10" --include=*.ts . 2>/dev/null | grep -v node_modules | head -10
```

> **↳ result**
> ```
> (eval):1: no matches found: --include=*.ts
> ```

🔧 **Bash**
```
grep -rn "shippingRates" --include='*.ts' packages apps/api/src | grep -v node_modules | head -20
```

> **↳ result**
> ```
> packages/db/prisma/seed/shop.ts:126:      shippingRates: [
> apps/api/src/services/settings/shipping.ts:7: * Rates live in the `shops.shippingRates` JSON column rather than a table of
> apps/api/src/services/settings/shipping.ts:48:  const shop = await db.shop.findFirst({ select: { shippingRates: true } });
> apps/api/src/services/settings/shipping.ts:49:  return parseRates(shop?.shippingRates);
> apps/api/src/services/settings/shipping.ts:66:    const shop = await tx.shop.findFirst({ select: { shippingRates: true } });
> apps/api/src/services/settings/shipping.ts:67:    const next = mutate(parseRates(shop?.shippingRates));
> apps/api/src/services/settings/shipping.ts:68:    await tx.shop.update({ where
> … [truncated, 483 more chars]
> ```

🔧 **Bash**
```
grep -rn "WELCOME10" --include='*.ts' packages apps | grep -v node_modules | head
```

> **↳ result**
> ```
> packages/db/prisma/seed/seed.test.ts:309:      where: { shopId_code: { shopId, code: 'WELCOME10' } },
> packages/db/prisma/seed/orders.ts:147:  // WELCOME10 is `oncePerCustomer` (discounts.ts). Seeding a second redemption
> packages/db/prisma/seed/discounts.ts:4: * `WELCOME10` is load-bearing: H2's smoke flow (c) applies exactly that code at
> packages/db/prisma/seed/discounts.ts:20:export const WELCOME_CODE = 'WELCOME10';
> apps/api/test/checkout.test.ts:267:        code: 'WELCOME10',
> apps/api/test/checkout.test.ts:366:  it('prices WELCOME10 exactly as the C1 engine does, taxing the discounted base', async () => {
> apps/api/test/checkout.test.ts:368:    const state = await readyToPay(checkout.token,
> … [truncated, 365 more chars]
> ```

🔧 **Bash**
```
sed -n 115,150p packages/db/prisma/seed/shop.ts
```

> **↳ result**
> ```
> customizeStorefront: true,
>         addPaymentProcessor: true,
>         placeTestOrder: true,
>         dismissed: false,
>       },
>       taxSettings: { ratePercentage: TAX_RATE_PERCENTAGE, pricesIncludeTax: false },
>       checkoutSettings: {
>         requireCustomerAccount: false,
>         showTipping: false,
>         orderNotePrompt: 'Add a note for the packing team',
>       },
>       shippingRates: [
>         {
>           id: newId('shippingRate'),
>           name: SHIPPING_STANDARD.name,
>           price: { amount: SHIPPING_STANDARD.price, currencyCode: ctx.currencyCode },
>           minOrderSubtotal: null,
>           maxOrderSubtotal: null,
>         },
>         {
>           id: newId('shippingRate'),
> … [truncated, 506 more chars]
> ```

🔧 **Bash**
```
cat packages/db/prisma/seed/discounts.ts | head -80
```

> **↳ result**
> ```
> /**
>  * The three demo discounts (H1, SPEC §7).
>  *
>  * `WELCOME10` is load-bearing: H2's smoke flow (c) applies exactly that code at
>  * checkout, and C1's engine reads these rows verbatim.
>  */
> import { newId } from '@merchant/config/ids';
> import type { PrismaClient } from '@prisma/client';
> import { daysAgo, type SeedContext } from './context.ts';
> import { FREE_SHIPPING_THRESHOLD } from './shop.ts';
> 
> export interface SeededDiscount {
>   id: string;
>   code: string | null;
>   type: string;
>   valueType: string;
>   value: number;
> }
> 
> export const WELCOME_CODE = 'WELCOME10';
> 
> export async function createDiscounts(
>   db: PrismaClient,
>   ctx: SeedContext,
> ): Promise<{ welcome: SeededDiscount; freeShipping
> … [truncated, 1,646 more chars]
> ```

### 🤖 Claude

Let me look at the existing checkout tests around the shipping/discount interaction — there's already something there.

🔧 **Bash**
```
sed -n 240,300p apps/api/test/checkout.test.ts; echo "=== 355-445 ==="; sed -n 355,445p apps/api/test/checkout.test.ts
```

> **↳ result**
> ```
> const processorId = newId('processor');
>   await dbAdmin.processorConfig.create({
>     data: {
>       id: processorId,
>       shopId: shop.shopId,
>       processor: 'mock',
>       displayName: 'Mock Gateway (test)',
>       enabled: true,
>       testMode: true,
>     },
>   });
>   await dbAdmin.routingRule.create({
>     data: {
>       id: newId('routingRule'),
>       shopId: shop.shopId,
>       processorConfigId: processorId,
>       position: 0,
>       weight: 100,
>     },
>   });
> 
>   await dbAdmin.discount.createMany({
>     data: [
>       {
>         id: newId('discount'),
>         shopId: shop.shopId,
>         title: '10% off your first order',
>         code: 'WELCOME10',
>         type: 'amount_off_order',
>         valueT
> … [truncated, 5,054 more chars]
> ```

🔧 **Bash**
```
grep -n "readyToPay\|STANDARD_RATE\|FREE_RATE_ID\|const req\b\|async function req\|function openCheckout" -A 22 apps/api/test/checkout.test.ts | sed -n '1,90p'
```

> **↳ result**
> ```
> 35:const STANDARD_RATE_ID = newId('event');
> 36:const FREE_RATE_ID = newId('event');
> 37-const FREE_SHIPPING_THRESHOLD = 15000;
> 38-
> 39-const usd = (amount: number) => ({ amount, currencyCode: 'USD' });
> 40-const host = () => `${shop.slug}.lvh.me:3002`;
> 41-
> 42-/**
> 43- * Every `inject` looks like 127.0.0.1, and `complete` is rate-limited to 5/min
> 44- * per IP (SPEC §8). Without a distinct source address per request the suite
> 45- * throttles itself and then reports 429s that have nothing to do with what it
> 46- * was testing — the trap `helpers.ts` documents for login.
> 47- */
> 48-let clientIp = 0;
> 49-const nextIp = () => {
> 50-  clientIp += 1;
> 51-  return `10.${Math.floor(clientIp / 65025)}.${Math.fl
> … [truncated, 2,391 more chars]
> ```

### 🤖 Claude

Found the gap: `readyToPay` always resends `selectedShippingRateId`, so no test ever exercises a PUT carrying *only* a discount code. Let me check the id schema, then write the failing test.

🔧 **Bash**
```
grep -n "idSchema" -A 8 packages/contracts/src/common.ts | head -20; echo "=== prefixes ==="; grep -n "shippingRate\|prefixes\|PREFIX" packages/config/src/ids.ts | head -40
```

> **↳ result**
> ```
> 14:export const idSchema = z.string().regex(/^[a-z_]+_[0-9A-HJKMNP-TV-Z]{26}$/, 'Invalid id');
> 15-
> 16-export const prefixedId = (prefix: string) =>
> 17-  z.string().regex(new RegExp(`^${prefix}_[0-9A-HJKMNP-TV-Z]{26}$`), `Expected a ${prefix}_ id`);
> 18-
> 19-/** URL-safe, lowercase. Unique per shop for products/collections. */
> 20-export const handleSchema = z
> 21-  .string()
> 22-  .min(1)
> --
> 154:export const idParam = z.object({ id: idSchema });
> 155-
> 156-/** Standard 200 for a delete. */
> 157:export const deletedResponse = z.object({ id: idSchema, deleted: z.literal(true) });
> === prefixes ===
> 11:export const ID_PREFIXES = {
> 42:  shippingRate: 'ship',
> 46:export type IdKind = keyof typeof ID_PREFIXE
> … [truncated, 293 more chars]
> ```

### 🤖 Claude

Now the test, written first, before any fix.

🔧 **Read**
```
/Users/pauljurgens/Downloads/projects/shopify/.claude/worktrees/repo-ui-issues-status-afad25/apps/api/test/checkout.test.ts
```

> **↳ result**
> ```
> 418	  });
> 419	
> 420	  it('drops a selected rate that stops being applicable', async () => {
> 421	    // Otherwise the shopper keeps free shipping they no longer qualify for, and
> 422	    // the charge silently disagrees with the sidebar.
> 423	    const { checkout } = await openCheckout([{ variantId: v.socks, quantity: 9 }]);
> 424	    const free = await readyToPay(checkout.token, {}, FREE_RATE_ID);
> 425	    expect(free.selectedShippingRateId).toBe(FREE_RATE_ID);
> 426	    expect(free.totals.shippingTotal).toEqual(usd(0));
> 427	
> 428	    const after = await readyToPay(checkout.token, { discountCode: 'WELCOME10' }, FREE_RATE_ID);
> 429	    expect(after.selectedShippingRateId).toBeNull();
> 430	  });
> 431	});
> … [truncated, 185 more chars]
> ```

🔧 **Edit**
```
/Users/pauljurgens/Downloads/projects/shopify/.claude/worktrees/repo-ui-issues-status-afad25/apps/api/test/checkout.test.ts
```

> **↳ result**
> ```
> The file /Users/pauljurgens/Downloads/projects/shopify/.claude/worktrees/repo-ui-issues-status-afad25/apps/api/test/checkout.test.ts has been updated successfully. (file state is current in your context — no need to Read it back)
> ```

🔧 **Bash**
```
export PATH="/opt/homebrew/bin:$PATH"; docker ps --format '{{.Names}}' 2>&1 | head -5; echo "--- env ---"; ls .env 2>&1; grep -c . .env 2>/dev/null
```

> **↳ result**
> ```
> merchant-minio-1
> merchant-postgres-1
> merchant-mailpit-1
> merchant-redis-1
> iti0302-2023-backend-database-1
> --- env ---
> .env
> 56
> ```

🔧 **Bash**
```
export PATH="/opt/homebrew/bin:$PATH"; pnpm worktree:env --migrate 2>&1 | tail -15
```

> **↳ result**
> ```
> WARN  Unsupported engine: wanted: {"node":">=22.0.0"} (current: {"node":"v20.20.1","pnpm":"9.15.4"})
> 
> > merchant@ worktree:env /Users/pauljurgens/Downloads/projects/shopify/.claude/worktrees/repo-ui-issues-status-afad25
> > bash scripts/worktree-env.sh "--migrate"
> 
> 
> ==> repo-ui-issues-status-afad25
>   ✓ database merchant_repo_ui_issues_status_afad25  ·  redis db 5
>   ! no node_modules — skipping migrate/seed (run `pnpm install && pnpm db:setup` here)
> 
>   Each worktree now has its own database and Redis keyspace.
>   Ports 3000/3001/3002 are still shared on purpose — one dev stack at a time.
> ```

🔧 **Bash**
```
export PATH="/opt/homebrew/bin:$PATH"; ls /opt/homebrew/opt | grep -i "^node" ; echo "---"; which -a node; node -v; ls ~/.nvm/versions/node 2>/dev/null
```

> **↳ result**
> ```
> node@20
> ---
> /opt/homebrew/bin/node
> /Users/pauljurgens/.nvm/versions/node/v22.23.2/bin/node
> /opt/homebrew/bin/node
> /usr/local/bin/node
> v20.20.1
> v22.23.2
> ```

🔧 **Bash**
```
export PATH="/Users/pauljurgens/.nvm/versions/node/v22.23.2/bin:/opt/homebrew/bin:$PATH"; node -v && pnpm -v && pnpm install 2>&1 | tail -6
```

> **↳ result**
> ```
> v22.23.2
> 9.15.4
> devDependencies:
> + @biomejs/biome 2.5.11
> + turbo 2.10.12
> + typescript 5.9.3
> 
> Done in 16s
> ```

🔧 **Bash**
```
export PATH="/Users/pauljurgens/.nvm/versions/node/v22.23.2/bin:/opt/homebrew/bin:$PATH"; pnpm worktree:env --migrate 2>&1 | tail -12
```

> **↳ result**
> ```
> > merchant@ worktree:env /Users/pauljurgens/Downloads/projects/shopify/.claude/worktrees/repo-ui-issues-status-afad25
> > bash scripts/worktree-env.sh "--migrate"
> 
> 
> ==> repo-ui-issues-status-afad25
>   ✓ database merchant_repo_ui_issues_status_afad25  ·  redis db 5
>   ✓ migrated and seeded
> 
>   Each worktree now has its own database and Redis keyspace.
>   Ports 3000/3001/3002 are still shared on purpose — one dev stack at a time.
> ```

### 🤖 Claude

Now running the new test to watch it fail.

🔧 **Bash**
```
export PATH="/Users/pauljurgens/.nvm/versions/node/v22.23.2/bin:/opt/homebrew/bin:$PATH"; pnpm --filter @merchant/api exec vitest run test/checkout.test.ts -t "only a discount code" 2>&1 | tail -40
```

> **↳ result**
> ```
> RUN  v3.2.7 /Users/pauljurgens/Downloads/projects/shopify/.claude/worktrees/repo-ui-issues-status-afad25/apps/api
> 
>  ✓ test/checkout.test.ts (30 tests | 29 skipped) 1341ms
> 
>  Test Files  1 passed (1)
>       Tests  1 passed | 29 skipped (30)
>    Start at  16:35:38
>    Duration  2.69s (transform 407ms, setup 19ms, collect 1.04s, tests 1.34s, environment 0ms, prepare 91ms)
> ```

### 🤖 Claude

The test passes on the first run — so the API's PUT path is already correct, and the bug is elsewhere. Let me read the sidebar, which the issue also implicates.

🔧 **Read**
```
/Users/pauljurgens/Downloads/projects/shopify/.claude/worktrees/repo-ui-issues-status-afad25/apps/storefront/src/components/checkout/order-summary.tsx
```

> **↳ result**
> ```
> 1	/**
> 2	 * Checkout order summary — Shopify's grey right-hand sidebar (PARITY.md).
> 3	 * Owner: WS-E.
> 4	 *
> 5	 * Every number here comes from E3's `totals`, recomputed on each save, so the
> 6	 * sidebar and the amount the card is charged cannot drift apart.
> 7	 */
> 8	'use client';
> 9	
> 10	import { format } from '@merchant/config/money';
> 11	import type { Checkout } from '@merchant/contracts/checkout';
> 12	import { useState } from 'react';
> 13	
> 14	const money = (amount: number, currency: string) => format({ amount, currencyCode: currency });
> 15	
> 16	export function OrderSummary({
> 17	  checkout,
> 18	  onApplyDiscount,
> 19	  busy,
> 20	}: {
> 21	  checkout: Checkout;
> 22	  onApplyDiscount?: (code: string) => voi
> … [truncated, 5,969 more chars]
> ```

### 🤖 Claude

The unit path is clean, so I need the live seed's data (3 rates, `oncePerCustomer` WELCOME10). Let me reproduce the exact sequence against a real server.

🔧 **Bash**
```
export PATH="/opt/homebrew/bin:$PATH"; for p in 3000 3001 3002; do echo "--- $p ---"; lsof -nP -iTCP:$p -sTCP:LISTEN 2>/dev/null | awk 'NR==2{print $1, $2}'; done
```

> **↳ result**
> ```
> --- 3000 ---
> node 56959
> --- 3001 ---
> --- 3002 ---
> ```

🔧 **Bash**
```
export PATH="/Users/pauljurgens/.nvm/versions/node/v22.23.2/bin:/opt/homebrew/bin:$PATH" && nohup pnpm --filter @merchant/api dev > /private/tmp/claude-501/-Users-pauljurgens-Downloads-projects-shopify--claude-worktrees-repo-ui-issues-status-afad25/0103d436-541e-4695-adbd-8b5fdc5ae0ef/scratchpad/api.log 2>&1 & disown; sleep 12; tail -5 /private/tmp/claude-501/-Users-pauljurgens-Downloads-projects-shopify--claude-worktrees-repo-ui-issues-status-afad25/0103d436-541e-4695-adbd-8b5fdc5ae0ef/scratchpad/api.log
```

> **↳ result**
> ```
> > tsx watch --env-file=../../.env src/server.ts
> 
> [16:36:30.229] INFO (74421): Server listening at http://127.0.0.1:3001
> [16:36:30.229] INFO (74421): Server listening at http://192.168.1.202:3001
> ```

🔧 **Bash**
```
export PATH="/opt/homebrew/bin:$PATH"
H='Host: demo.lvh.me:3002'
API=http://127.0.0.1:3001/storefront/api
# find a cheap variant
pnpm db:query "select v.id, v.title, v.price, p.title from \"ProductVariant\" v join \"Product\" p on p.id=v.\"productId\" where v.price <= 2000 limit 3" 2>&1 | tail -6
echo "=== rates ==="
pnpm db:query "select \"shippingRates\" from \"Shop\" where slug='demo'" 2>&1 | tail -5
```

> **↳ result**
> ```
> > bash scripts/db-query.sh "select v.id, v.title, v.price, p.title from \"ProductVariant\" v join \"Product\" p on p.id=v.\"productId\" where v.price <= 2000 limit 3"
> 
> ERROR:  relation "ProductVariant" does not exist
> LINE 1: select v.id, v.title, v.price, p.title from "ProductVariant"...
>                                                     ^
>  ELIFECYCLE  Command failed with exit code 1.
> === rates ===
> 
> ERROR:  relation "Shop" does not exist
> LINE 1: select "shippingRates" from "Shop" where slug='demo'
>                                     ^
>  ELIFECYCLE  Command failed with exit code 1.
> ```

🔧 **Bash**
```
export PATH="/opt/homebrew/bin:$PATH"; pnpm db:query tables 2>&1 | tail -30
```

> **↳ result**
> ```
> product_options         |   34
>  fulfillments            |   32
>  products                |   32
>  customer_addresses      |   25
>  customers               |   25
>  collection_products     |   17
>  discount_redemptions    |    8
>  _prisma_migrations      |    5
>  collections             |    4
>  payment_methods         |    4
>  vault_cards             |    4
>  discounts               |    3
>  webhook_deliveries      |    3
>  checkouts               |    2
>  locations               |    2
>  payment_refunds         |    2
>  refunds                 |    2
>  staff_users             |    2
>  theme_versions          |    2
>  apps                    |    1
>  builder_conversations   |    1
>  order_sequences         |
> … [truncated, 204 more chars]
> ```

🔧 **Bash**
```
export PATH="/opt/homebrew/bin:$PATH"; pnpm db:query "select shipping_rates from shops where slug='demo'" 2>&1 | tail -8; echo "=== welcome ==="; pnpm db:query "select code, type, value, once_per_customer, usage_limit, used_count, status from discounts" 2>&1 | tail -8
```

> **↳ result**
> ```
> > merchant@ db:query /Users/pauljurgens/Downloads/projects/shopify/.claude/worktrees/repo-ui-issues-status-afad25
> > bash scripts/db-query.sh "select shipping_rates from shops where slug='demo'"
> 
> ERROR:  column "shipping_rates" does not exist
> LINE 1: select shipping_rates from shops where slug='demo'
>                ^
> HINT:  Perhaps you meant to reference the column "shops.shippingRates".
>  ELIFECYCLE  Command failed with exit code 1.
> === welcome ===
> 
> > merchant@ db:query /Users/pauljurgens/Downloads/projects/shopify/.claude/worktrees/repo-ui-issues-status-afad25
> > bash scripts/db-query.sh "select code, type, value, once_per_customer, usage_limit, used_count, status from discounts"
> 
> ERROR:  col
> … [truncated, 192 more chars]
> ```

🔧 **Bash**
```
export PATH="/opt/homebrew/bin:$PATH"; pnpm db:query 'select "shippingRates" from shops' 2>&1 | tail -8; echo "=== discounts ==="; pnpm db:query 'select code, type, value, "oncePerCustomer", "usedCount", status from discounts' 2>&1 | tail -8
```

> **↳ result**
> ```
> > merchant@ db:query /Users/pauljurgens/Downloads/projects/shopify/.claude/worktrees/repo-ui-issues-status-afad25
> > bash scripts/db-query.sh "select \"shippingRates\" from shops"
> 
>                                                                                                                                                                                                                                                                                        shippingRates
> … [truncated, 1,621 more chars]
> ```

### 🤖 Claude

Now the real sequence against the seeded data via curl.

🔧 **Bash**
```
export PATH="/opt/homebrew/bin:$PATH"; pnpm db:query 'select v.id, v.title, v.price, p.title, p.handle from product_variants v join products p on p.id=v."productId" where v.price between 1000 and 2000 limit 3' 2>&1 | tail -8
```

> **↳ result**
> ```
> > bash scripts/db-query.sh "select v.id, v.title, v.price, p.title, p.handle from product_variants v join products p on p.id=v.\"productId\" where v.price between 1000 and 2000 limit 3"
> 
>                id               | title | price |      title       |      handle      
> --------------------------------+-------+-------+------------------+------------------
>  var_01M16VQ4B2TN5P4Q91KDTS8H0G | L     |  1800 | Basin Wool Socks | basin-wool-socks
>  var_01M16VQ4B27X4QW8QYF4Z2HAGQ | M     |  1800 | Basin Wool Socks | basin-wool-socks
> (2 rows)
> ```

### 🤖 Claude

$18.00 socks — exactly the repro's −$1.80. Running the sequence.

🔧 **Bash**
```
grep -n "app\.\(get\|post\|put\|delete\)" apps/api/src/routes/storefront/carts.ts apps/api/src/routes/storefront/checkouts.ts 2>/dev/null | head -20
```

> **↳ result**
> ```
> 41:  app.post('/checkouts', async (request, reply) => {
> 49:  app.get('/checkouts/:token', async (request) => {
> 55:  app.put('/checkouts/:token', async (request) => {
> 62:  app.get('/checkouts/:token/shipping-rates', async (request) => {
> 71:  app.post(
> ```

🔧 **Bash**
```
grep -rn "app\.\(get\|post\|put\|delete\)('" apps/api/src/routes/storefront/*.ts | grep -i cart
```

> **↳ result**
> ```
> apps/api/src/routes/storefront/cart.ts:58:  app.get('/cart', async (request, reply) => cartSchema.parse(await currentCart(request, reply)));
> apps/api/src/routes/storefront/cart.ts:60:  app.post('/cart', async (request, reply) => {
> apps/api/src/routes/storefront/cart.ts:66:  app.post('/cart/lines', async (request, reply) => {
> apps/api/src/routes/storefront/cart.ts:74:  app.put('/cart/lines/:lineId', async (request, reply) => {
> apps/api/src/routes/storefront/cart.ts:81:  app.delete('/cart/lines/:lineId', async (request, reply) => {
> ```

🔧 **Bash**
```
cd /private/tmp/claude-501/-Users-pauljurgens-Downloads-projects-shopify--claude-worktrees-repo-ui-issues-status-afad25/0103d436-541e-4695-adbd-8b5fdc5ae0ef/scratchpad
API=http://127.0.0.1:3001/storefront/api
H='Host: demo.lvh.me:3002'
rm -f jar.txt
echo "== add line =="
curl -s -c jar.txt -b jar.txt -H "$H" -H 'content-type: application/json' -X POST "$API/cart/lines" \
  -d '{"variantId":"var_01M16VQ4B2TN5P4Q91KDTS8H0G","quantity":1}' | head -c 300
echo; echo "== create checkout =="
TOK=$(curl -s -c jar.txt -b jar.txt -H "$H" -X POST "$API/checkouts" | python3 -c 'import sys,json;print(json.
… [truncated, 45 more chars]
```

> **↳ result**
> ```
> == add line ==
> {"id":"cart_01M16VTE06RGPR1VNF5T2SKV4J","token":"cart_77ed34385bb634fdb6d2c617d01b9b28a559910dd31d8800","currencyCode":"USD","lines":[{"id":"li_01M16VTE1P5MVAKB7WZ976WFPZ","productId":"prod_01M16VQ4B2JGKFEM140ATX17BV","variantId":"var_01M16VQ4B2TN5P4Q91KDTS8H0G","quantity":1,"title":"Basin Wool Sock
> == create checkout ==
> token=chk_5ced0b69a81a9e2b964e7564954224b81d71065e37690d30
> Shell cwd was reset to /Users/pauljurgens/Downloads/projects/shopify/.claude/worktrees/repo-ui-issues-status-afad25
> ```

🔧 **Bash**
```
API=http://127.0.0.1:3001/storefront/api
H='Host: demo.lvh.me:3002'
TOK=chk_5ced0b69a81a9e2b964e7564954224b81d71065e37690d30
RATE=ship_01M16VQ49F9Y9Y0NVGSZKZQMJJ
echo "== 1. email =="
curl -s -H "$H" -H 'content-type: application/json' -X PUT "$API/checkouts/$TOK" \
 -d '{"email":"shopper@example.com"}' | python3 -c 'import sys,json;d=json.load(sys.stdin);print("selected:",d["selectedShippingRateId"],"opts:",len(d["shippingOptions"]))'
echo "== 2. address =="
curl -s -H "$H" -H 'content-type: application/json' -X PUT "$API/checkouts/$TOK" \
 -d '{"shippingAddress":{"firstName":"Jane","lastName
… [truncated, 1,085 more chars]
```

> **↳ result**
> ```
> Exit code 1
> == 1. email ==
> Traceback (most recent call last):
>   File "<string>", line 1, in <module>
>   File "/Library/Frameworks/Python.framework/Versions/3.7/lib/python3.7/json/__init__.py", line 296, in load
>     parse_constant=parse_constant, object_pairs_hook=object_pairs_hook, **kw)
>   File "/Library/Frameworks/Python.framework/Versions/3.7/lib/python3.7/json/__init__.py", line 348, in loads
>     return _default_decoder.decode(s)
>   File "/Library/Frameworks/Python.framework/Versions/3.7/lib/python3.7/json/decoder.py", line 337, in decode
>     obj, end = self.raw_decode(s, idx=_w(s, 0).end())
>   File "/Library/Frameworks/Python.framework/Versions/3.7/lib/python3.7/json/decoder.py", line 355,
> … [truncated, 2,723 more chars]
> ```

🔧 **Bash**
```
API=http://127.0.0.1:3001/storefront/api
H='Host: demo.lvh.me:3002'
TOK=chk_5ced0b69a81a9e2b964e7564954224b81d71065e37690d30
curl -s -i -H "$H" -H 'content-type: application/json' -X PUT "$API/checkouts/$TOK" -d '{"email":"shopper@example.com"}' | head -20
```

> **↳ result**
> ```
> HTTP/1.1 404 Not Found
> vary: Origin
> content-type: application/json; charset=utf-8
> cache-control: no-store
> content-length: 64
> Date: Sat, 29 Aug 2026 13:37:42 GMT
> Connection: keep-alive
> Keep-Alive: timeout=72
> 
> {"errors":[{"code":"not_found","message":"Checkout not found"}]}
> ```

🔧 **Bash**
```
export PATH="/opt/homebrew/bin:$PATH"; pnpm db:query 'select token, "shopId", status, "shippingRateId" from checkouts order by "createdAt" desc limit 3' 2>&1 | tail -8; echo "=== shops ==="; pnpm db:query 'select id, slug from shops' 2>&1 | tail -5
```

> **↳ result**
> ```
> token                         |             shopId              | status | shippingRateId 
> ------------------------------------------------------+---------------------------------+--------+----------------
>  chk_5ced0b69a81a9e2b964e7564954224b81d71065e37690d30 | shop_01M16VQ3T6X0PX9A04YJS1TDV4 | open   | 
>  chk_c01fd10ea9da4ba828e899a13db7ecb34e43371a35810371 | shop_01M16VQ3T6X0PX9A04YJS1TDV4 | open   | 
>  chk_18b8beada091e85b59b066b899104dfc21e0c8ed835d8508 | shop_01M16VQ3T6X0PX9A04YJS1TDV4 | open   | 
> (3 rows)
> 
> === shops ===
>                id                | slug 
> ---------------------------------+------
>  shop_01M16VQ3T6X0PX9A04YJS1TDV4 | demo
> (1 row)
> ```

🔧 **Bash**
```
API=http://127.0.0.1:3001/storefront/api
H='Host: demo.lvh.me:3002'
TOK=chk_5ced0b69a81a9e2b964e7564954224b81d71065e37690d30
echo "== GET =="; curl -s -o /dev/null -w '%{http_code}\n' -H "$H" "$API/checkouts/$TOK"
echo "== GET no host =="; curl -s -o /dev/null -w '%{http_code}\n' "$API/checkouts/$TOK"
echo "== tail log =="; tail -12 /private/tmp/claude-501/-Users-pauljurgens-Downloads-projects-shopify--claude-worktrees-repo-ui-issues-status-afad25/0103d436-541e-4695-adbd-8b5fdc5ae0ef/scratchpad/api.log
```

> **↳ result**
> ```
> == GET ==
> 404
> == GET no host ==
> 404
> == tail log ==
>       "url": "/storefront/api/checkouts",
>       "host": "demo.lvh.me:3002",
>       "remoteAddress": "127.0.0.1",
>       "remotePort": 64818
>     }
> [16:37:21.552] INFO (74421): request completed
>     reqId: "9416748b-cc99-4b84-86a0-d5f68c7ee269"
>     res: {
>       "statusCode": 201
>     }
>     responseTime: 59.18587500000285
> [16:37:27.511] INFO (74421): SIGTERM received, closing
> ```

### 🤖 Claude

The server was SIGTERM'd and something else took the port. Let me check ownership and restart properly.

🔧 **Bash**
```
export PATH="/opt/homebrew/bin:$PATH"; PID=$(lsof -nP -iTCP:3001 -sTCP:LISTEN 2>/dev/null | awk 'NR==2{print $2}'); echo "pid=$PID"; [ -n "$PID" ] && lsof -a -p "$PID" -d cwd -Fn 2>/dev/null
```

> **↳ result**
> ```
> pid=77052
> p77052
> fcwd
> n/Users/pauljurgens/Downloads/projects/shopify/apps/api
> ```

### 🤖 Claude

Main stack owns :3001 — I'll leave it alone and run mine on a free port.

🔧 **Bash**
```
grep -n "PORT" packages/config/src/env.ts | head -10; echo "=== server.ts ==="; grep -n "listen\|port" apps/api/src/server.ts | head -10
```

> **↳ result**
> ```
> 34:  API_PORT: z.coerce.number().int().positive().default(3001),
> 35:  ADMIN_PORT: z.coerce.number().int().positive().default(3000),
> 36:  STOREFRONT_PORT: z.coerce.number().int().positive().default(3002),
> 65:  SMTP_PORT: z.coerce.number().int().positive().default(1025),
> 72:  // Absent is a SUPPORTED state: the builder falls back to canned presets so the
> === server.ts ===
> 2:import { env } from '@merchant/config/env';
> 3:import { buildApp } from './app.ts';
> 4:import { closeRedis } from './lib/redis.ts';
> 21:  await app.listen({ port: config.API_PORT, host: '0.0.0.0' });
> ```

🔧 **Bash**
```
export PATH="/Users/pauljurgens/.nvm/versions/node/v22.23.2/bin:/opt/homebrew/bin:$PATH"; SD=/private/tmp/claude-501/-Users-pauljurgens-Downloads-projects-shopify--claude-worktrees-repo-ui-issues-status-afad25/0103d436-541e-4695-adbd-8b5fdc5ae0ef/scratchpad; API_PORT=3011 nohup pnpm --filter @merchant/api dev > $SD/api.log 2>&1 & disown; sleep 14; grep -c "listening" $SD/api.log; tail -3 $SD/api.log
```

> **↳ result**
> ```
> 2
> 
> [16:38:25.270] INFO (78307): Server listening at http://127.0.0.1:3011
> [16:38:25.270] INFO (78307): Server listening at http://192.168.1.202:3011
> ```

### 🤖 Claude

Server up on :3011. Let me run the repro, and check the B7 data while it runs.

🔧 **Bash**
```
API=http://127.0.0.1:3011/storefront/api
H='Host: demo.lvh.me:3002'
J=$(mktemp)
RATE=ship_01M16VQ49F9Y9Y0NVGSZKZQMJJ
curl -s -c $J -b $J -H "$H" -H 'content-type: application/json' -X POST "$API/cart/lines" -d '{"variantId":"var_01M16VQ4B2TN5P4Q91KDTS8H0G","quantity":1}' > /dev/null
TOK=$(curl -s -c $J -b $J -H "$H" -X POST "$API/checkouts" | python3 -c 'import sys,json;print(json.load(sys.stdin)["token"])')
echo "token=$TOK"
show() { python3 -c 'import sys,json;d=json.load(sys.stdin);print("  selected:",d["selectedShippingRateId"],"| ship:",d["totals"]["shippingTotal"]["amount"],"| disc:",d["
… [truncated, 928 more chars]
```

> **↳ result**
> ```
> token=chk_7363183e189bedc3f14750eb9114f83bca831f0f9a831386
> 1. email:
>   selected: None | ship: 0 | disc: 0 | applied: [] | rejected: None
> 2. address:
>   selected: None | ship: 0 | disc: 0 | applied: [] | rejected: None
> 3. pick Standard:
>   selected: ship_01M16VQ49F9Y9Y0NVGSZKZQMJJ | ship: 895 | disc: 0 | applied: [] | rejected: None
> 4. apply WELCOME10 (discountCode only):
>   selected: ship_01M16VQ49F9Y9Y0NVGSZKZQMJJ | ship: 895 | disc: 180 | applied: ['WELCOME10'] | rejected: None
> ```
