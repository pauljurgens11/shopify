# Agent log

Append-only scratch log for cross-agent visibility. `merge=union`: append at the
bottom, never edit existing lines.

Use it for things that are useful to another agent *right now* but are not
decisions (which belong in `DECISIONS.md`): what you are actively working on,
what you stubbed and where, what is temporarily broken on `main`.

Format: `YYYY-MM-DD HH:MM | WS-X | message`

---
2026-08-28 11:35 | WS-A | Branch ruleset 21717783 applied to main: PR required, linear history, `pr-checks` must be green. Direct pushes to main are now rejected server-side, not just by the local pre-push hook. Repo is squash-merge only with auto-merge and branch auto-delete on.
2026-08-28 12:05 | WS-C | CLAIM C1 discounts engine | branch ws-c/discounts-engine
2026-08-28 12:35 | WS-C | DONE C1 discounts engine — applyDiscounts() in apps/api/src/services/discounts/engine.ts is pure and importable now (E3 checkout totals, C6 preview). Contracts additions: DiscountEngineResult, DiscountedLine, DiscountRejectionReason, DiscountAppliesTo, DiscountableLine types. apps/api no longer runs with --passWithNoTests.
2026-08-28 12:05 | WS-D | CLAIM D1 vault (crypto, Luhn, tokenize endpoint) | branch ws-d/vault
2026-08-28 12:50 | WS-D | Fixed a platform bug found by D1's rate-limited route: app.ts errorResponseBuilder returned a plain object, but @fastify/rate-limit THROWS that value — with no statusCode it fell through to 500. It now returns `new ApiError('rate_limited', …)`, so any route with `config.rateLimit` gets a real SPEC-shaped 429. WS-A: one-line change in your file, flagged rather than silently kept.
2026-08-28 12:50 | WS-D | Tenant writes must pass `shopId` in `data` for Prisma's types even under `dbForShop` (the extension overrides it). First one to hit this in B/C/E: that is expected, not a scoping bug.
2026-08-28 13:05 | WS-D | pr-checks.yml had VAULT_MASTER_KEY unquoted — YAML read 64 zeros as the number 0, so CI ran with VAULT_MASTER_KEY="0" and any test that touches env() died. Quoted it. Nothing had read the var in CI before D1, so this was invisible until now.
2026-08-28 13:20 | WS-D | DONE D1 vault | PR #5 — packages/pay/{crypto,vault}.ts + POST /vault/tokenize, 60 unit tests (§14.2). D3/E4 unblocked: tokenizeCard(db, shopId, card) and getCard(db, cardTokenId) (pay-internal, decrypts) in @merchant/pay/vault.
2026-08-28T12:20Z | WS-F | CLAIM F1 theme-engine core | branch ws-f/theme-engine-core
2026-08-28T13:05Z | WS-F | F1 landed. Public API: `@merchant/theme-engine/render` (`renderPage`, `renderFooter`, `themeCssVariables`, `googleFontsHref`), `@merchant/theme-engine/presets` (`presetThemeDoc('aurora'|'monochrome'|'bloom')`, `DEFAULT_PRESET`), `@merchant/theme-engine/shared` (ThemeButton, ProductCard, SectionShell, RichHtml, Price, productGridClass, route helpers). Data contract + client-island slots: `src/context.ts`.
2026-08-28T13:05Z | WS-F | WS-E (E2): Tailwind 4 does not scan `node_modules`, so `apps/storefront/src/app/globals.css` needs `@source "../../../../packages/theme-engine/src";` — without it none of the section classes are emitted and every page renders unstyled. Left for you since globals.css is yours.
2026-08-28T13:05Z | WS-F | WS-H (H1): presets reference exactly one collection handle, `featured` — enforced by `presets.test.ts`. Aurora is the preset to seed as the published theme (`DEFAULT_PRESET`).
2026-08-28T13:05Z | WS-F | WS-F (F2): the 13 marketing sections are still placeholders. Build on `src/shared/` and take `SectionProps<'hero'>` for props; `render.test.tsx` already fails the build on a hardcoded colour in `sections/` or `shared/`.
2026-08-28T13:20Z | WS-F | DONE F1 | PR #7
2026-08-28T09:35Z | WS-D | CLAIM D2 (processor adapters: mock, stripe, maverick) | branch ws-d/adapters
2026-08-28T09:48Z | WS-D | DONE D2 (processor adapters) | PR #9 — mock/stripe/maverick implemented, adapters no longer throw. ProcessorAdapter.authorize now takes CardMaterial (PAN) as its 2nd arg: D3 must detokenize via vault.getCard and pass it through. `…0119` hard-fails on mock and APPROVES on maverick — that pair is the failover demo. TEST_CARDS + maverickMode exported from @merchant/pay/index for D4/H1/H2.
2026-08-28 12:05 | WS-A | CLAIM A1 (auth, sessions, tenant resolution) | branch ws-a/auth-sessions-tenancy. Lands `lib/{redis,sessions,permissions}.ts`, `plugins/{auth,csrf}.ts`, tenancy onRequest resolution, `routes/auth/**`, and the `apps/api` vitest harness that A2 builds on. Everything downstream of A1 stays blocked until this merges.
2026-08-28 12:55 | WS-A | DONE A1 — auth, sessions, tenant resolution. What you need from it: `request.db` now resolves on `/admin/api/*` (session), `/storefront/api/*` (Host `{slug}.lvh.me`), `/api/*` (Bearer). Gate admin routes with `requirePermission('<area>')` from `apps/api/src/lib/permissions.ts` — do not roll your own role check. Cookie-authenticated MUTATIONS require a non-empty `x-requested-with` header (A3: send `CSRF_HEADER_VALUE` from every admin API client write). Call the API as `api.lvh.me:3001` from admin and `{slug}.lvh.me:3001` from storefront — `localhost:3001` silently drops the session cookie. `apps/api` now has a vitest harness (`test/helpers.ts`: buildTestApp, createTestShop, sessionCookie, deleteTestShops) that A2 should build the tenancy suite on; `--passWithNoTests` removed. NOTE for A4: sessions snapshot role+permissions at login, so destroy a user's sessions when you edit them.
2026-08-28 13:00 | WS-A | A1 merged as PR #8 (ba3abfb). A2, A3, B1–B4, C2, C4, E1, G2, G4 are now unblocked.
2026-08-28T09:55Z | WS-D | CLAIM D3 (payment router, Payment records, refunds) | branch ws-d/payment-router
2026-08-28T10:10Z | WS-D | LANDMINE for every workstream adding API routes: @fastify/autoload treats a directory index file as the WHOLE directory and silently skips its siblings. `routes/admin/payments/index.ts` made processors.ts and routing-rules.ts vanish with no error — they 404 at runtime. Name route files after what they serve (payments.ts, processors.ts) and keep route directories index-free, like routes/vault/. Verify with `app.printRoutes()`, not by assuming.
2026-08-28T10:20Z | WS-D | DONE D3 (payment router) | PR #12 — @merchant/pay/router exports charge / capturePayment(db, paymentId, amount?) / voidPayment(db, paymentId) / refundPayment(db, shopId, paymentId, {amount, reason, idempotencyKey}) / savePaymentMethod / chargeSavedCard, all taking dbForShop(shopId). WS-C (C3): call refundPayment directly — I deliberately did NOT add a payments-level refund route so the cap lives in one place; it throws PaymentError{code} which maps 1:1 onto the SPEC §5 error codes. WS-D (D4): GET/POST/PATCH/DELETE /admin/api/payments/processors, GET/PUT /admin/api/payments/routing-rules, POST /admin/api/payments/charge-saved-card. Contracts additions: updateProcessorInput, capturePaymentInput. New ID_PREFIXES: routingRule=rule_, paymentMethod=pm_.
2026-08-28T13:30Z | WS-F | CLAIM F2 marketing sections | branch ws-f/marketing-sections
2026-08-28T14:10Z | WS-F | F2 landed: all 13 marketing sections implemented. New shared primitives any future section should use — `ThemeImage` (image + layout-stable `data-placeholder="image"` fallback), `ProductSkeletonGrid`/`CardSkeletonGrid` (unresolved-handle empty states), `SNAP_TRACK` (scroll-snap carousel track), and `ThemeButton variant="on-image"` for buttons over an image overlay.
2026-08-28T14:10Z | WS-F | WS-E (E2): sections consume `data.collectionsByHandle`, `data.productsByHandle` and `data.newestProducts` — resolve the handles named in each section's settings and pass them in; anything unresolved degrades to a skeleton rather than crashing, so a stale AI handle is safe. `product-grid` with an empty `productHandles` expects `newestProducts`.
2026-08-28T14:10Z | WS-F | Reminder for whoever lands E2: `apps/storefront/src/app/globals.css` still needs `@source "../../../../packages/theme-engine/src";` — verified locally that without it Tailwind emits none of the section classes, and with it the storefront renders correctly.
2026-08-28T14:15Z | WS-F | DONE F2 | PR #15
2026-08-28T14:00Z | WS-B | CLAIM B1 products & variants API | branch ws-b/products-api
2026-08-28T14:40Z | WS-B | DONE B1 products & variants API. `/admin/api/products` (list/create/get/update/delete) + `/:id/variants/:variantId` (get/update), all `requirePermission('products')`. Service is importable without HTTP: `createProduct/updateProduct/getProduct/listProducts/deleteProduct` in `apps/api/src/services/catalog/products.ts` — E1 and H1 should call it rather than re-deriving the option matrix. Contracts additions (additive): `upsertVariantInput`, `updateVariantInput`, `variantParams`, and `updateProductInput` now takes variants with an optional `id`.
2026-08-28T14:40Z | WS-B | For B2/B3/B4/B5: variant ids are STABLE across an option edit (matched by id, then by option values), so inventory levels and image `variantIds` survive. `inventoryQuantity` on a variant is the SUM of its `InventoryLevel` rows and is read-only here — B4 owns writing it through the adjustment service, and the field on `createVariantInput` is currently accepted and ignored. `PUT /products/:id` with `images` REPLACES the image rows wholesale; B2 should take that over when images become uploads.
2026-08-28T14:40Z | WS-B | Verified for everyone writing multi-step writes: the `dbForShop` extension DOES apply inside `db.$transaction(async (tx) => …)` — a bogus tenant sees 0 rows from `tx`. You still have to pass `shopId` in `data` for Prisma's types (WS-D's earlier note), but you do not have to hand-scope `where` inside a transaction.
2026-08-28T14:40Z | WS-B | For WS-G (G1): `apps/api/src/services/catalog/events.ts` resolves `@merchant/config/queue` at runtime and no-ops while G1 is unmerged, so products/create|update|delete start firing the moment PR #11 lands — no edit needed on our side. Delete that indirection then. Note PR #11 is currently DIRTY (conflicts with main).
2026-08-28T14:40Z | WS-B | Heads-up: `apps/api` dev binds :3001 and several worktrees share one Postgres/Redis. If you get EADDRINUSE, another agent's api is up — run yours with `API_PORT=30xx pnpm --filter @merchant/api dev`. Tests are unaffected (every suite makes its own shop).
2026-08-28T13:35Z | WS-H | CLAIM H1 seed demo data (Aurora Supply Co.) | branch ws-h/seed-demo-data
2026-08-28T13:20Z | WS-H | DONE H1 seed demo data — `pnpm db:reset` now builds Aurora Supply Co.: 32 products (30 active + 1 draft + 1 archived) / 133 variants, 2 locations, 4 collections, 25 customers, 40 orders over 60 days, 3 discounts, mock processor, published aurora theme, ~5.8k analytics events + 59 days of rollups. Runs in ~5s.
2026-08-28T13:20Z | WS-H | Seed is now modular (`prisma/seed/{catalog,orders,customers,collections,discounts,analytics,shop,inventory}.ts` + `data/`) so it rebases in small pieces — it touches every domain and is the likeliest file to race. Add your model to the module that owns it, not to index.ts.
2026-08-28T13:20Z | WS-H | `packages/db` test script now loads the root .env (`dotenv -e ../../.env -- vitest run`), same pattern as migrate/seed. CI's job env still wins — dotenv never overrides an already-set variable. Without it `pnpm test` could not reach Postgres locally.
2026-08-28T13:20Z | WS-H | WS-B/C/G: the seed asserts its own invariants in `prisma/seed/seed.test.ts`, which runs the real seed against Postgres on every PR. If you add a required column without a default, that test goes red before your UI does — fix the seed module, don't skip the test.
2026-08-28T13:20Z | WS-H | Useful demo handles: customer `jane@example.com` / `password123` (E5 login), discount `WELCOME10` (H2 flow c), collection `featured` (F1 presets). Orders are #1001–#1040 and `OrderSequence.next` is 1041. Two orders are cancelled/voided, two partially refunded, six paid-but-unfulfilled.
2026-08-28T13:20Z | WS-H | WS-F (F2): the seeded aurora theme renders through `renderPage` with real data, but products do not appear yet — `featured-collection` and `product-grid` are still the F1 placeholders. The data reaching them is correct; it is the sections that are pending.
2026-08-28 13:40 | WS-C | CLAIM C2 (orders API: create service, list, detail, cancel, timeline) | branch ws-c/orders-api
2026-08-28 14:20 | WS-C | DONE C2 orders API. For E3: `createOrder(db, shopId, input, { actor })` from `apps/api/src/services/orders/create.ts` — input is `createOrderInput` in `@merchant/contracts/orders` (totals arrive computed and MUST balance `subtotal - discountTotal + shippingTotal + taxTotal === total`, every amount in the order currency, or it throws a 400). It claims the order number, snapshots lines, writes the `order_placed` timeline entry and the discount redemptions, and returns the full `OrderDetail`. For C5: `GET /admin/api/orders` (tabs all|open|unfulfilled|unpaid|closed, `?query=` order number/email/customer name, sortKey createdAt|total|orderNumber, cursor = an order id), `GET /:id` (lines, events, customer, payments), `PATCH /:id` (note/tags/email/shippingAddress), `POST /:id/cancel`, `POST /:id/events`. For C4: reuse `services/orders/list.ts` `listOrders(db, query)` for the customer's order table — do not fork the tab rules. For WS-G: `apps/api/src/services/orders/notify.ts` is the seam for `orders/create` + `orders/cancelled` webhooks and the confirmation email; wire `emitWebhookEvent`/`enqueueOrderConfirmationEmail` there when G1 lands. For WS-B (B4): `cancel.ts` restocks by hand for now — replace `restockLines` with the adjust service, keeping the adjustment-per-level-write invariant.
2026-08-28T15:00Z | WS-B | CLAIM B4 locations & inventory API (adjustment service) | branch ws-b/inventory-api
2026-08-28T16:00Z | WS-B | DONE B4 locations & inventory. THE stock write path is `apps/api/src/services/inventory/adjust.ts` — import it, do not touch `inventoryLevel` directly: `adjust(db, {variantId, locationId, delta, reason, referenceId, actor})`, `adjustMany(db, inputs)` (all-or-nothing batch), `setAvailable/setMany` for absolute counts. All take a `dbForShop(shopId)` client, so seed and worker can call them exactly like a request handler can. Routes: `/admin/api/locations` (CRUD) and `/admin/api/inventory` (index + POST /adjust + POST /set), all `requirePermission('products')`.
2026-08-28T16:00Z | WS-B | WS-C (C3): fulfillment decrements are `adjustMany(db, lines.map(l => ({variantId, locationId, delta: -l.quantity, reason: 'sold', referenceId: fulfillmentId})))`. It throws `conflict` if a `deny` variant would go negative and rolls the WHOLE batch back, so you never half-fulfil. WS-E (E3): same call with the order id as referenceId; check stock with `GET /admin/api/inventory` or read `InventoryLevel` directly (reads are fine, writes are not).
2026-08-28T16:00Z | WS-B | WS-H (H1): your seed is currently writing `InventoryAdjustment` rows by hand with `inv_`-prefixed ids and a person's name in `actor` (saw them in the shared dev DB). Switch to `adjust()` — you get the level, the history row and the id prefix (`inv_adj_`) for free, and the demo then exercises the same path the admin does. Levels are lazy, so seeding a location costs nothing until you stock it.
2026-08-28T16:00Z | WS-B | The local Postgres is shared across worktrees and got `db:reset` under me mid-smoke-test. Not a problem for tests (every suite makes its own shop) but if your manual curl data vanishes, that is why.
2026-08-28 15:05 | ROOT | If your PR has NO checks at all (not red, not pending), it is CONFLICTING — GitHub never built the merge commit pr-checks runs against, so it will sit silent with auto-merge armed forever. Run `pnpm sync` from that worktree. `pr-health.yml` now labels these `needs-rebase` and comments on them within a minute of any push to main.
2026-08-28 15:40 | ROOT | Ran the pr-health rebase bot by hand against the stuck PRs: #19 (admin shell) and #20 were dead with zero checks for ~1h and are now rebased, mergeable and running CI. #11 (ws-g) has a REAL code conflict, not a log-file one — its agent has to resolve that by hand. #26 (ws-b collections) likewise conflicted in apps/api/test/helpers.ts.
2026-08-28T14:35Z | WS-F | CLAIM F3 themes API + AI generation job | branch ws-f/themes-api-ai-job. Racing G1 (#11 is DIRTY): building everything except the enqueue wiring first, will bind to `enqueue(QUEUES.ai, JOB_NAMES.aiThemeGeneration, …)` from @merchant/config/queue once it lands.
2026-08-28T15:05Z | WS-F | WS-A: signup now calls `installInitialTheme(shop.id)` (two lines in `routes/auth/index.ts`) so a new store opens on the published aurora preset — the onboarding hook SPEC §12 asks for. It never throws and never blocks the response; flagging it because it is your file.
2026-08-28T15:05Z | WS-F | WS-G: `apps/api/src/services/themes/generation.ts` holds a TEMPORARY private BullMQ producer for the `ai` queue, because `@merchant/config/queue` (PR #11) had not landed. It uses your exact queue and job names (`QUEUES.ai` / `'ai-theme-generation'`), so when #11 merges the body collapses to one `enqueue(...)` call and the connection goes away. Happy for you to do that collapse in passing, or ping me.
2026-08-28T15:05Z | WS-F | WS-F (F4): the builder API is `/admin/api/themes` — GET `/versions`, GET `/versions/:id`, POST `/versions/:id/{publish,restore}`, POST `/presets/:name/apply`, GET `/preview-token?versionId=`, GET/POST `/conversation`. POST /conversation returns 202 `{jobId, message}`; poll GET /conversation until the pending assistant message flips to complete/failed. With no ANTHROPIC_API_KEY it answers in chat and returns `jobId: null` — presets still apply, so the builder is demoable either way.
2026-08-28T15:05Z | WS-F | WS-E (E2): verify `?preview=` with `verifyPreviewToken` from `apps/api/src/services/themes/preview-token.ts` — it returns `{shopId, themeVersionId}` or null (expired/tampered/garbage all look the same). Tokens live 15 minutes.
2026-08-28T15:20Z | WS-F | Follow-up worth doing when migrations are quiet: `builder_conversations` wants `@@unique([shopId])` (theme.prisma is WS-F's). Two simultaneous first messages currently converge via a read-after-create in `getOrCreateConversation` rather than a constraint; the index would make it atomic. Not urgent — the window is one double-click on a shop's very first builder message.
2026-08-28T15:35Z | WS-F | DONE F3 | PR #27
2026-08-28T14:00Z | WS-A | CLAIM A3 (admin shell: Frame/TopBar/Navigation, login + signup, API client) | branch ws-a/admin-shell. Lands apps/admin/src/{app,components/shell,lib}/**. Every admin UI issue (A4, B5, B6, C5, C6, D4, F4, G3) is blocked until this merges; they will import `src/lib/api.ts`.
2026-08-28T15:10Z | WS-A | DONE A3 — admin shell. What every admin UI issue needs from it: (1) put your page at apps/admin/src/app/store/[slug]/<area>/page.tsx, mark it 'use client', and replace the ComingOnline placeholder that is already there — the Frame, TopBar and nav come from the layout, do not re-render them. (2) Data goes through apps/admin/src/lib/api.ts: `useApiQuery<T>(key, path)` / `useApiMutation<TResult, TInput>(method, path)`; they already send credentials, the x-requested-with CSRF header on mutations, and parse the SPEC §5 envelope into `ApiError` (`.code`, `.field`, `.fieldErrors` for Polaris `error` props). Never call fetch directly. (3) `useSession()` from lib/session.ts gives user + shop. (4) `<PageSkeleton />` while loading, `useToast().show('Product saved')` for toasts, `<SaveBar dirty saving onSave onDiscard />` for dirty forms — all from components/shell/. (5) Do NOT pass onClick+router.push to Polaris links; AppProvider's linkComponent already makes every `url` a client-side Next navigation. Global search fans out to /admin/api/{products,orders,customers}?query=&limit=5 and shows nothing for groups whose endpoint 404s, so B1/C2/C4 light it up for free when they land.
2026-08-28T15:35Z | WS-A | A3 merged as PR #19. A4, B5, B6, C5, C6, D4, F4, G3 are unblocked — see the A3 DONE line above for how to build a page inside the shell.
2026-08-28T14:55Z | WS-E | CLAIM E1 storefront API + cart service | branch ws-e/storefront-api-cart. Lands `apps/api/src/routes/storefront/**` + `services/cart/**`, additive contracts in storefront.ts/cart.ts (incl. GET /storefront/api/theme carrying the full ThemeDoc). E2/E3/E5/G2 unblock on this.
2026-08-28T15:10Z | WS-E | DONE E1 storefront API + cart service. Endpoints: GET /storefront/api/{shop,theme,products,products/:handle,collections/:handle/products}, POST/GET /cart + POST /cart/lines + PUT|DELETE /cart/lines/:lineId, POST /events. All Host-resolved, no auth. Read endpoints carry STOREFRONT_CACHE_CONTROL; cart and signed theme previews are `no-store`.
2026-08-28T15:10Z | WS-E | WS-E (E2): `GET /storefront/api/theme` returns `{ themeVersionId, theme, isPreview }` with the FULL ThemeDoc, so a page renders in one hop — you do not need /shop first for the theme. Contracts additions (additive): `storefrontThemeResponse`, `storefrontCollectionProductsResponse`, `StorefrontCollection`.
2026-08-28T15:10Z | WS-E | WS-F (F3/F4): build builder preview links with `signThemePreview(themeVersionId)` from `apps/api/src/services/storefront/theme.ts`, then hit the storefront with `?preview=<that>`. It is an HMAC over SESSION_SECRET; nothing else can mint one, and an invalid token silently falls back to the published theme. `@merchant/theme-engine` is now a devDependency of apps/api, so F3 can import the engine there.
2026-08-28T15:10Z | WS-E | WS-B (B3): the storefront resolves collection membership from `CollectionProduct` rows for smart collections too — please materialize smart membership into that table rather than evaluating rules at read time (DECISIONS.md).
2026-08-28T15:10Z | WS-E | WS-E (E3): the cart deliberately has NO discount endpoint and computes subtotal only — shipping, tax and discounts are yours. `Cart.discountCode` is carried through the contract but never set here. Cart lines store variantId+quantity only and reprice on read, so snapshot the cart into `Checkout.cartSnapshot` at creation.
2026-08-28T15:10Z | WS-E | Insufficient stock returns `conflict` (409) with `field: "quantity"` — SPEC §5 fixes the error-code set and there is no 422 in it. The check runs against the line's TOTAL quantity after a merge, not the increment.
2026-08-28T15:10Z | WS-E | Raised Fastify `pluginTimeout` to 60s in `apps/api/src/app.ts` (WS-A's file, one line, flagged rather than silently kept). The 10s default is a boot budget that autoload spends dynamically importing every route file; on a cold Vitest cache — exactly CI after `pnpm install` — the growing route tree blew it and the whole suite failed with "Plugin did not start in time" instead of anything diagnostic. It bounds startup only, never a request.
2026-08-28T15:10Z | WS-B | CLAIM B3 collections API (manual + smart) | branch ws-b/collections-api. B2 (files/media) is still free and does not overlap.
2026-08-28T15:55Z | WS-B | DONE B3 collections API | PR #26. Importable without HTTP for E1/F1/H1: `listCollections/getCollection/listCollectionProducts/createCollection/updateCollection/deleteCollection/updateCollectionProducts` in `apps/api/src/services/catalog/collections.ts`, plus `smartCollectionWhere(ruleSet) -> Prisma.ProductWhereInput` if you want to resolve a rule set against your own product query. Routes: GET/POST `/admin/api/collections`, GET/PUT/DELETE `/:id`, GET/POST `/:id/products` — all `requirePermission('products')`.
2026-08-28T15:55Z | WS-B | For E1/B6: `GET /admin/api/collections/:id/products` takes `?sortOrder=` to override the stored order (the storefront "Sort by" dropdown) and pages with an OPAQUE OFFSET cursor, not the id cursor the products index uses — do not try to read a product id out of it. Smart rules that the translator refuses are a 400 on SAVE, so a stored rule set always resolves; see DECISIONS.md for the refused (column, relation) pairs.
2026-08-28T15:55Z | WS-B | `apps/api/src/services/catalog/products.ts` gained `productsMatching(db, currency, {where, orderBy, skip, take})` and `productsByIds(db, currency, ids)` — use these rather than re-deriving the product DTO if you need product rows in a non-standard order.
2026-08-28T15:55Z | WS-B | B2 (files/media, MinIO presign) is STILL UNCLAIMED and overlaps nothing in B3/B4. Verified against H1's seed: the four demo collections all resolve correctly through the new endpoints.
2026-08-28T16:30Z | WS-B | CLAIM B2 file uploads (MinIO presigned) + product media | branch ws-b/files-media
2026-08-28T17:15Z | WS-B | DONE B2 files & media. `POST /admin/api/files/presign` — body `{filename, contentType, sizeBytes}` (contract `packages/contracts/src/files.ts`), returns `{uploadUrl, publicUrl, key, expiresInSeconds}`. The browser PUTs the file to `uploadUrl` with a matching `content-type` header, then stores `publicUrl`. Verified end to end against the real MinIO: presign → PUT → anonymous GET returns the identical bytes as `image/png`, a tampered signature is 403, and the browser CORS preflight already passes with no compose change.
2026-08-28T17:15Z | WS-B | WS-B (B5): the media card is `presign` → `fetch(uploadUrl, {method:'PUT', headers:{'content-type': file.type}, body: file})` → put `publicUrl` on `images[].url` in the B1 product PUT. Do NOT post bytes to the API. Note B1 currently REPLACES the image rows wholesale on a product PUT, so send the full desired image list each save.
2026-08-28T17:15Z | WS-B | WS-F (F3) / WS-A (A4): the same endpoint takes theme assets and logos — it is not product-specific, and the key layout already namespaces by shop. Allowed types are the five in the contract (jpeg/png/webp/gif/svg+xml), 20 MB.
2026-08-28T17:15Z | WS-B | Two environment gotchas that cost me time, for whoever hits them next: (1) after rebasing onto #17/#18 you MUST `pnpm install` or `@merchant/db` typecheck fails on missing `@merchant/theme-engine/presets` — stale node_modules, not a code break. (2) `.env` now points Postgres at :5433 with a per-worktree database (#18); if the Docker daemon is down, `pnpm dev` reports a Prisma init error that looks like a code bug and is not.
2026-08-28 14:45 | WS-A | CLAIM A2 tenancy suite | branch ws-a/tenancy-suite
2026-08-28 15:05 | WS-A | DONE A2 — apps/api/test/tenancy.test.ts: two shops, HTTP + client layers, covering the ops the pure-function tests could not: OrThrow variants, count/aggregate/groupBy, updateMany/deleteMany with empty where, the upsert P2002 edge, nested-create stamping overriding a wrong shopId, and everything again inside $transaction (previously only asserted in a prose note here). Runs in ~3s. Sabotage-verified: removing the AND injection fails 12/13. Also: dbForShop now FAILS CLOSED — an operation outside WHERE_OPS/DATA_OPS, or a model without shopId, throws instead of passing through unscoped.
2026-08-28T18:00Z | WS-B | CLAIM B5 admin products index + product form | branch ws-b/products-admin-ui
2026-08-28T19:30Z | WS-B | DONE B5 admin products index + product form. `/store/{slug}/products` (IndexFilters tabs + search, IndexTable with thumb/status/inventory/price/vendor, bulk status + delete, cursor pagination), `/products/new` and `/products/{id}` (two-column form, media card on B2 presign, option builder + variant table, contextual save bar, delete confirm). Pure state/conversion logic is `apps/admin/src/lib/product-draft.ts` with 13 tests.
2026-08-28T19:30Z | WS-B | WS-H (H2): stable selectors for smoke flow (a) — the title field is `name="title"`, the description `name="description"`, and the save action is the Polaris ContextualSaveBar's "Save" button. The index's primary action is "Add product"; a row navigates on click.
2026-08-28T20:15Z | WS-B | VERIFIED B5 in a real browser (index, form, option builder, save, toast). HOW TO SEE THE ADMIN, since this cost me an hour: the browser pane only loads `_next/static/*` for the origin of a REGISTERED preview server. `preview_start {name:'dev'}` registers `localhost:3000` — so you must browse `http://localhost:3000`, NOT `http://admin.lvh.me:3000`, or every asset is ERR_BLOCKED_BY_CLIENT and you get unstyled HTML with no JS. The API then has to allow that origin: set `ADMIN_URL`/`API_URL` to the localhost pair in your (gitignored) `.env` before starting, or CORS rejects `/auth/me`. If another worktree holds :3000, run your own pair on free ports and add a temporary url-only entry to `.claude/launch.json` (url + no command = attach). Clear the `_merchant_session` cookie between stacks — a cookie minted by another worktree's API fails signature checks against yours and looks exactly like a broken login.
2026-08-28T19:30Z | WS-B | WS-B (B6): the product form deliberately omits a Collections picker — `createProductInput` has no collection ids, so it could not save. If you add collection membership to the contract, the Product organization card is where it goes.
2026-08-28T20:15Z | WS-B | Two bugs the browser found that the unit tests had not: (1) the option-value input lost every value but the last when several arrived in one change event (a paste of "S, M, L", or fast typing) — values are now merged in a single call, `addOptionValues`, with a regression test; (2) the Description field showed raw `<p>` markup on every seeded product — simple paragraph HTML is now unwrapped for editing and re-wrapped on save, byte-identically, while anything richer is left as HTML rather than flattened.
2026-08-28 12:05 | WS-G | CLAIM G1 queue producer + webhook delivery + order email | branch ws-g/worker-webhooks-email
2026-08-28 12:55 | WS-G | DONE G1 — producer (`@merchant/config/queue`: `enqueue`, `emitWebhookEvent`, `enqueueOrderConfirmationEmail`), webhook delivery + order email jobs, echo receiver (`pnpm --filter @merchant/worker run echo`) and `… run emit`. Callers: import from `@merchant/config/queue`; do NOT construct a Queue yourself.
2026-08-28 12:55 | WS-G | [schema] `WebhookDelivery.eventId` + `@@unique([subscriptionId, eventId])`, migration `002_wsg_webhook_delivery_event_id`. Pull main before you generate a migration.
2026-08-28 12:55 | WS-G | First write through `dbForShop`: Prisma's generated create input still requires `shopId`, even though the tenant extension stamps it. Pass it explicitly in `data` — it is overwritten with the same value. `where` clauses still need nothing (CLAUDE.md §6).
2026-08-28 15:05 | WS-G | G1 landed as PR #11. WS-F: the shared producer is now on main — `apps/api/src/services/themes/generation.ts` can collapse to `enqueue(QUEUES.ai, JOB_NAMES.aiThemeGeneration, payload)` from `@merchant/config/queue` and drop its own Queue/connection. Job name strings already match, so nothing is broken until you do.
2026-08-28T16:05Z | WS-E | CORRECTION to my earlier F3/F4 note: ignore `signThemePreview` — it is deleted. F3's `signPreviewToken(shopId, versionId)` in `apps/api/src/services/themes/preview-token.ts` is the one scheme, and `GET /storefront/api/theme?preview=<token>` now verifies exactly that. F3's landed first-class version (shop-bound, expiring) beat mine, so E1 adopted it rather than leaving two HMAC formats that would never have matched.
2026-08-28T15:25Z | WS-E | CORRECTION to my earlier B3 note: ignore the request to materialize smart membership. B3 resolves it on read and exports `smartCollectionWhere(ruleSet)` for exactly this; the storefront now uses it. Before this fix a smart collection returned ZERO products on the storefront while the admin showed the right set — caught by a regression test, not by review.
2026-08-28T15:25Z | WS-E | `getStorefrontCollection()` now returns `{ id, collection, membership, sortOrder }`, where `membership` is a `Prisma.ProductWhereInput`. E2/E5: pass it to `listStorefrontProducts({ membership })` rather than a collectionId — that is what keeps manual and smart collections on one code path.
2026-08-28 15:40 | WS-H | Rollups now include `begin_checkouts` (one metric per funnel stage). WS-G (G2/G3): the dashboard's funnel.reachedCheckout reads THIS metric on closed days — derive rollup metric names as `${eventType}s` and the seed baseline and your job cannot disagree.
2026-08-28T16:20Z | WS-A | A3 fully complete — PR #39 finished the search keyboard navigation that #19 left out (arrow keys walk grouped results as one list, Enter opens, Escape closes). Also added detail placeholders at /store/{slug}/{products,orders,customers}/[id] because search results link straight there and 404'd without them — B5/C5/C6 should REPLACE those three files, not add alongside them.
2026-08-28 17:30 | WS-G | CLAIM wiring G1 into the order + catalog seams — fills C2 `services/orders/notify.ts` (orders webhooks + confirmation email currently no-op) and removes B1 `services/catalog/events.ts` runtime indirection | branch ws-g/wire-order-notifications
2026-08-28 17:55 | WS-G | DONE wiring — G1's producer is now reachable from the real flows. `services/orders/notify.ts` emits orders/create|paid|cancelled and enqueues the confirmation email (it was an empty body, so NONE of that fired before). `services/catalog/events.ts` is a thin typed wrapper again — B1's runtime-import indirection is gone. `routes/admin/payments/payments.ts` passes `{ onPaid: notifyOrderPaid }` to `chargeSavedCard` (D3's seam). Verified end to end against the seeded stack: all four topics delivered with verified HMAC, email in Mailpit with correct totals.
2026-08-28 17:55 | WS-G | For E3: `notifyOrder` is already called by `createOrder`/`cancelOrder`, so checkout gets webhooks + the confirmation email for free. If you want the thank-you link IN the email, call `enqueueOrderConfirmationEmail(shopId, orderId, orderStatusUrl)` yourself instead — the seam passes null, which omits the button rather than linking at a guessed URL.
2026-08-28 17:55 | WS-G | For whoever wires charges in checkout (E3) and refunds (C3): pass `{ onPaid: notifyOrderPaid }` from `services/orders/notify.ts` into `charge()`/`chargeSavedCard()`, or `orders/paid` will not fire on that path.
2026-08-28T17:05Z | WS-F | CLAIM F4 admin AI builder (chat + preview + publish) | branch ws-f/builder-admin-ui
2026-08-28T18:10Z | WS-F | WS-A: `apps/admin/next.config.ts` now also publishes `NEXT_PUBLIC_STOREFRONT_ORIGIN` (one additive line, same pattern and same reason as your `NEXT_PUBLIC_API_URL`) — the builder's preview iframe needs the storefront origin in the browser. Flagging it because it is your file.
2026-08-28T18:10Z | WS-F | WS-E (E2): the builder preview currently renders your placeholder page, because `/`, `/products/[handle]` and `/collections/[handle]` do not exist on the storefront yet. The plumbing on my side is done and verified — correct origin, tenant resolved from the Host, signed `?preview=` token accepted — so the preview starts showing real themed pages the moment E2 lands, with no change here. Verify the token with `verifyPreviewToken` from `apps/api/src/services/themes/preview-token.ts`.
2026-08-28T18:10Z | WS-F | WS-H (H2): flow (d) is walkable now — admin → Storefront → Apply a preset → preview updates → Publish → "Theme published" toast and the Live badge. Stable selectors: the iframe is `iframe[title="Storefront preview"]`, the toolbar buttons are Desktop/Mobile/Version history/Publish by text, and the preset rows each have an `Apply` button.
2026-08-28T18:20Z | WS-F | DONE F4 | PR #44 — workstream F is complete (F1 theme engine, F2 marketing sections, F3 themes API + AI job, F4 builder UI).
2026-08-28 18:20 | WS-G | CLAIM G2 analytics ingestion, rollup, query API | branch ws-g/analytics-pipeline. Absorbing E1's POST /storefront/api/events beacon (their file header hands it over). Note: nothing writes a server-side `purchase` event at order creation yet — adding that as part of G2.
2026-08-28 18:55 | WS-G | DONE G2 analytics pipeline. For G3: ONE call, `GET /admin/api/analytics?from=&to=` -> `analyticsDashboardResponse` (summary incl. comparison, salesOverTime/ordersOverTime/sessionsOverTime, topProducts, salesByChannel, funnel), plus `GET /admin/api/analytics/live` -> `{visitors, ordersToday}` for the "Right now" card. Both `requirePermission('analytics')`. Closed days read rollups, open days aggregate raw — callers do not care which.
2026-08-28 18:55 | WS-G | Rollup runs every 5 min as a BullMQ repeatable registered at worker boot; it rewrites today + yesterday. Metric vocabulary is the SEED's seven (incl. `begin_checkouts`) — if you add a metric you must backfill history or the chart steps. Verified the job reproduces the seed's numbers exactly for a seeded day.
2026-08-28 18:55 | WS-G | For WS-E/E3: the analytics beacon now DROPS browser-sent `purchase` events (forgeable revenue on an unauthenticated endpoint). The real one is written by `recordPurchaseEvent` in `apps/api/src/services/analytics/record.ts`, called from `createOrder` — so checkout gets it for free. Do not re-emit purchase from the storefront.
2026-08-28 16:15 | WS-C | CLAIM C3 (fulfillment + refund API) | branch ws-c/fulfillment-refund
2026-08-28 17:10 | WS-C | DONE C3 fulfillment + refund. For C5: `POST /admin/api/orders/:id/fulfillments` ({locationId, lineItems:[{lineItemId,quantity}], trackingNumber?, trackingUrl?, trackingCompany?, notifyCustomer?}) -> 201 OrderDetail; `POST /:id/refunds/calculate` -> {lineItems[], shippingAmount, subtotal, total, maximumRefundable} — show this in the refund form BEFORE committing, it is the same arithmetic the refund uses; `POST /:id/refunds` ({lineItems, shippingAmount?, reason?, note?, restock, idempotencyKey?}) -> 201 OrderDetail. Order detail now really returns `fulfillments` and `refunds` (they were [] under C2). All three services live in `apps/api/src/services/orders/{fulfill,refund,detail}.ts`.
2026-08-28 17:10 | WS-C | WS-D + WS-H, this breaks the demo: refunding a SEEDED order fails with `conflict: Unknown transaction mock_ch_1040`. The mock processor's ledger is in-memory per process, so a `processorTxnId` written by the seed is unknown to the running API and `SimulatedProcessor.refund` rejects it. H2 flow (b) is safe (it charges in the same process it refunds in), but a human clicking Refund on any seeded order in the admin hits it. Cheapest fix is in `packages/pay/src/adapters/simulated.ts`: treat an unknown txn as a captured one rather than a failure.
2026-08-28 17:10 | WS-C | Paid off the C2 stub: `cancel.ts` now restocks through B4's `adjustMany` instead of writing levels by hand. The adjustment `reason` for a cancel/refund restock is `restock` (B4's vocabulary), not `restocked`.
2026-08-28T16:45Z | WS-E | CLAIM E3 checkout API (lifecycle, shipping, taxes, complete) | branch ws-e/checkout-api. Lands `apps/api/src/routes/storefront/checkouts/**` + `services/checkout/**`, additive contracts in checkout.ts. E4 and H2 flows (b)(c) unblock on this.
2026-08-28T16:05Z | WS-E | DONE E3 checkout API. POST/GET/PUT /storefront/api/checkouts(/:token), GET /:token/shipping-rates, POST /:token/complete. All Host-resolved, no auth — the token in the URL is the credential. E4 and H2 flows (b)(c) unblock.
2026-08-28T16:05Z | WS-E | WS-E (E4): a decline is HTTP 200 `{status:'failed', message, code}` — render it inline, the checkout stays open and is payable again. Send a fresh `idempotencyKey` (min 8 chars) per Pay-now click: the same key double-submits safely, a new key is how a retry after a decline reaches the processor. `PUT` returns `rejectedDiscount:{code,reason}` for a bad coupon; totals are unchanged and it is NOT an HTTP error.
2026-08-28T16:05Z | WS-E | WS-C (C4): checkout creates customers by case-folded email and increments `ordersCount`/`totalSpent` itself (`findOrCreateCustomer` in services/checkout/complete.ts). Please take both over when the customers service lands — nothing else maintains those columns and the customers index sorts on them.
2026-08-28T16:05Z | WS-E | WS-G: the order confirmation email is enqueued by C2's `notifyOrder`, not by checkout — I removed E3's duplicate. Note `orderStatusUrl` is always null as a result, so the email omits its button; E3 holds the checkout token but createOrder has no field to carry it. Worth threading if the button matters.
2026-08-28T16:05Z | WS-E | Anyone adding a rate-limited route: the plugin wants `timeWindow`, not `windowMs`. `{ config: { rateLimit: RATE_LIMITS.x } }` silently inherits the global 1s window because the unknown key is ignored — spell it out as auth/index.ts does.
2026-08-28T16:05Z | WS-E | `deleteTestShops` now also clears orders, payments, customers, checkouts, discounts and processor configs (additive). Checkout is the first suite to create them and none cascade from Shop; without it an unscoped assertion sees a previous run's rows.
2026-08-28T21:00Z | WS-B | CLAIM B6 collections, inventory & locations admin pages | branch ws-b/collections-inventory-ui
2026-08-28T22:30Z | WS-B | DONE B6 collections, inventory & locations pages — the last issue in workstream B. Collections index + form (manual product picker with reorder, smart condition builder with a LIVE matching-products preview, image via B2 presign), Inventory index (location switcher, inline-editable Available cells), Settings → Locations (list, add/edit dialog, delete guarded). Pure logic in `apps/admin/src/lib/{collection-rules,inventory-edits}.ts` with 21 tests.
2026-08-28T22:30Z | WS-B | API additions other workstreams may want: `POST /admin/api/collections/preview` resolves an UNSAVED rule set through the same translator as a saved smart collection (F3's AI builder can use this to show what a generated collection would contain). `Location.stockedVariantCount` is on the location DTO now. And deleting a location that holds stock is a 409 — it was previously allowed and would have silently erased that stock.
2026-08-28T22:30Z | WS-B | WS-H (H2): browse-coverage selectors — collections index primary action is "Create collection", the inventory cells are number inputs labelled `Available for {product}`, and the locations page is at `settings/locations`. The inventory Save button appears only when a cell differs from the server value.
2026-08-28T22:30Z | WS-B | Preview recipe update (B5's entry still applies): if another worktree already holds :3000, `preview_start {name:'dev'}` will attach you to THEIR server and you will be testing their code — check `lsof -a -p <pid> -d cwd`. Run your own pair on free ports and add a temporary url-only entry to `.claude/launch.json` to attach. Also: background dev servers started via the agent's task runner get SIGTERM'd; `nohup … & disown` survives.
2026-08-28T19:05Z | ROOT | The in-app browser only renders `localhost` origins — an `*.lvh.me` page loads its HTML but every subresource is blocked (ERR_BLOCKED_BY_CLIENT), so the admin and storefront come up unstyled and never hydrate. Three of the four launch.json targets pointed at lvh.me, so "see the app" did not work for anyone. Added `dev-localhost` + `storefront-localhost` targets and `pnpm dev:localhost` (same stack, three env overrides). Existing targets, `.env` and every documented URL are unchanged.
2026-08-28 16:20 | WS-H | Seed imagery is now curated per product (seed/data/images.ts): handle → two pinned Unsplash photos, verified visually. Adding a product? Pick its two photo ids there — seed.test.ts goes red on the placeholder fallback. Also replaced the six theme-preset picsum images (WS-F files, imagery only — flagged here rather than left inconsistent with the catalog).
2026-08-28 18:05 | WS-C | Fixed the two things C3 flagged, both in packages/pay (WS-D: the change is small and your assertions all still hold — 143 pay tests green). 1) Refunding a SEEDED order works now: the simulated ledger adopts a transaction it never issued instead of rejecting it, refund-only. Verified live: refunding seeded order #1040 over HTTP returns partially_refunded. 2) The mock/maverick ledgers are globalThis-keyed, so the route tree and a vitest file share one — apps/api refund tests are back on HTTP instead of calling the service directly. E3: you can now test checkout completion, double-charge idempotency included, over HTTP.
2026-08-28 18:30 | WS-C | CLAIM C4 (customers API) | branch ws-c/customers-api
2026-08-28 19:15 | WS-C | DONE C4 customers. For E3: `findOrCreateByEmail(db, shopId, {email, firstName?, lastName?, phone?, acceptsMarketing?})` from `apps/api/src/services/customers/customers.ts` -> `{id, email, created}`. Idempotent per shop, case-insensitive, and safe under two checkouts completing on the same email at once (the loser of the unique-index race returns the winner's row instead of throwing after the card was charged). For C6: `GET /admin/api/customers` (?query= name/email/phone, ?segment=all|returning|new|abandoned-checkout, ?acceptsMarketing=true|false, ?tag=, cursor pages), `POST /`, `GET/PUT/DELETE /:id`, `GET /:id/orders` (reuses C2's list shape and tab rules). `ordersCount`/`totalSpent` come back derived on both index and detail — do NOT read the columns of the same name off the row, they are always 0.
2026-08-28 19:15 | WS-H | The seed creates ZERO Checkout rows, so the admin's "Abandoned checkouts" segment is correctly but visibly empty in the demo. If you want that tab to show something, seed a couple of `status: 'open'` checkouts with `completedOrderId: null`, an email matching a seeded customer, and `createdAt` inside 72h.
2026-08-28 19:15 | WS-C | Note for anyone adding routes under `apps/api/src/routes/admin/orders/`: that directory still uses `index.ts`, which autoload treats as the whole directory and which silently skips siblings. Add to the existing file, or rename it to `orders.ts` first (as collections/customers do).
2026-08-28T19:15Z | WS-E | CLAIM E2 storefront pages | branch ws-e/storefront-pages. Lands `apps/storefront/**` except `/checkouts` (E4's) and `/account` (E5's): layout + theme vars, `/`, `/products/[handle]`, `/collections/[handle]`, `/search`, `/cart`, preview passthrough, analytics beacons. E4 and H2 flows (b)(d) unblock on this.
2026-08-28T19:55Z | WS-E | DONE E2 storefront pages. `/`, `/products/[handle]`, `/collections/[handle]`, `/search`, `/cart`, `/checkout` (creates the checkout and redirects to E4), plus layout chrome, 404, preview passthrough and analytics beacons. All Server Components through F1's `renderPage`; three client leaves (product form, cart line controls, beacon). E4 and H2 flows (b)(d) unblock.
2026-08-28T19:55Z | WS-E | WS-E (E4): `/checkouts/[token]` is yours and is the ONLY route left unowned under apps/storefront — `/checkout` (singular) already exists as a route handler that creates the checkout from the cart cookie and redirects to `/checkouts/{token}` on the shop's own host. Build redirects with `absoluteUrl()` from `lib/api.ts`, never `new URL(path, request.url)`: that yields `localhost` and drops the tenant.
2026-08-28T19:55Z | WS-E | WS-F (F4): the builder preview now renders real themed pages. `?preview=<token>` is picked up by `src/middleware.ts` and applied to BOTH the layout tokens and the page sections, so colours and fonts change too — a token that fails verification silently falls back to the published theme. Verified end to end against a signed draft token.
2026-08-28T19:55Z | WS-E | Anyone calling the API from the storefront: the shop MUST be in the URL hostname (`storefrontApiUrl()` in `apps/storefront/src/lib/api.ts`). Node's fetch drops an explicit `host` header, so a header-based client silently resolves no tenant and 404s everything.
2026-08-28T19:55Z | WS-E | `apps/storefront` now has a `vitest.config.ts` that loads the root `.env` via `process.loadEnvFile` — same reason `next.config.ts` does, and it does not override an already-set variable, so CI's job env still wins.
2026-08-28T19:55Z | WS-E | Perf (SPEC §10, production build, seeded shop): TTFB / 59ms · product 113ms · collection 55ms · search 90ms · cart 19ms — all inside the 300ms budget. Dev-server numbers are 2–5x higher and are not the budget.
2026-08-28T16:50Z | WS-A | CLAIM A4 settings hub (general, staff, shipping, taxes, checkout) | branch ws-a/settings-hub. Replaces my A3 placeholder at /store/{slug}/settings. Touches apps/api/src/routes/admin/settings/** and apps/admin/src/app/store/[slug]/settings/**; shared Shop/settings schema changes will be additive only.
2026-08-28T17:30Z | WS-A | DONE A4 settings hub. API: `/admin/api/settings/{general,taxes,checkout,shipping-rates,staff}` behind requirePermission('settings'). E3 — read `GET /admin/api/settings/shipping-and-tax?subtotal=<minor units>`; it returns `{rates, tax}` with rates ALREADY narrowed to the cart (inclusive bounds, cheapest first) so you do not reimplement the conditions. Admin: hub grid at /store/{slug}/settings plus general/taxes/checkout/shipping/staff/plan/notifications. New shared UI: `components/settings/use-settings-form.ts` (load → draft → save bar → toast) and `settings-page.tsx` — reuse them for any settings-shaped form. D4: I left a placeholder at settings/payments, B6: one at /locations — REPLACE those two files, do not add alongside. New id prefix `ship_`. New session helper `destroySessionsForUser(id)` — call it whenever you change a staff user's role or permissions.
2026-08-28 19:10 | WS-G | CLAIM G3 admin Analytics dashboard + Home | branch ws-g/analytics-home-ui
2026-08-28 19:50 | WS-G | DONE G3 Analytics dashboard + Home. Analytics: range picker (Today/7/30/90), four metric cards with delta chips, Recharts sales area chart, top products, sales-by-channel, conversion funnel, live view polling /live every 30s. Home: greeting, setup guide (4 checks from real state), today's metrics.
2026-08-28 19:50 | WS-G | [admin] `useApiQuery` gained an optional `refetchInterval` (additive, WS-A's lib/api.ts) for genuinely live cards — G4's delivery log can use it too. polaris-viz was tried and REMOVED: it renders nothing under React 19. Charts are Recharts + `--p-*` tokens; do not re-add polaris-viz without checking that an svg actually appears.
2026-08-28T19:40Z | WS-C | CLAIM C5 orders admin UI (index, detail, fulfill, refund) | branch ws-c/orders-admin-ui
2026-08-28T20:30Z | WS-C | DONE C5 orders admin UI. Index (`/orders`), detail (`/orders/[id]`), fulfil (`/[id]/fulfill`) and refund (`/[id]/refund`). Shared bits worth reusing: `orders/_components/status.ts` maps every financial/fulfillment enum to Shopify's exact badge wording and tone (pinned by `status.test.ts`) and has `remainingToFulfil`/`remainingToRefund`/`capturedTotal`.
2026-08-28T20:30Z | WS-A | The Orders nav badge needed a count source: `admin-frame.tsx` now fetches `?tab=open&limit=50` and passes `openOrders` into the `AdminNavigation` prop you had already designed for it. The list endpoint is cursor-paginated with no total (SPEC §5), so the badge reads "50+" past a page rather than inventing a number. Two additive lines in your file — flagging it because it is yours.
2026-08-28T20:30Z | WS-H (H2) | Flow (b) is walkable: order → Fulfill items → "Partially fulfilled" → Refund → "Partially refunded", timeline picks up `fulfillment_created` and `refund_created`. Note it only works once PR #51's mock-ledger fix is in — before it, refunding any SEEDED order fails with `conflict: Unknown transaction mock_ch_...`.
2026-08-28T23:15Z | WS-B | Added `.claude/skills/resolve-issue/SKILL.md` — the issue-solving loop as actually practised across B1/B2/B4/B5/B6: claiming, what to read when contracts and issue prose disagree, tests that catch something (plus mutation-checking, and its limits), the browser-verification recipe that the pane/port/cookie traps make non-obvious, and the environment failures that each cost an hour. Read it before picking up an issue.
2026-08-28T21:15Z | ROOT | Root-caused the "admin logs you out mid-test" symptom several agents will hit: all worktrees share one SESSION_SECRET but each has its own Redis slot, and sessions live in Redis. When another worktree owns :3001, your cookie passes ITS signature check and then misses in ITS Redis — so you get a 401 "session has expired" and a bounce to /login with nothing actually wrong. Reproduced with two APIs identical except REDIS_URL (/8 -> 200, /9 -> 401). Documented in CLAUDE.md §1; `pnpm stack status` already names the worktree holding each port.
2026-08-28 19:40 | WS-C | CLAIM C6 (customers + discounts admin UI, plus the discounts CRUD API) | branch ws-c/customers-discounts-ui
2026-08-28 20:30 | WS-C | DONE C6 customers + discounts admin UI, and the discounts CRUD API that came with it. Pages: `/store/{slug}/customers` (segment tabs, search, derived orders/amount-spent columns), `/customers/{id}` (last order, order history, customer card, default address + modal, tags, notes, one save bar), `/customers/new`, `/store/{slug}/discounts` (split create menu per type, derived status badge), `/discounts/new?type=`, `/discounts/{id}`. API: `/admin/api/discounts` GET/POST/GET:id/PUT/DELETE, `requirePermission('discounts')`, codes stored uppercase, status derived at read.
2026-08-28 20:30 | WS-C | H2 flow (c): WELCOME10 already exists in the seed and renders Active on the discounts index — no setup needed for that flow. Verified in a browser against the seeded stack.
2026-08-28 20:30 | WS-C | The three admin placeholders C6 owned are now replaced (customers index, customers/[id], discounts). `orders/[id]` and `products/[id]` placeholders are still C5's and B5's to replace.
2026-08-28T20:30Z | WS-E | CLAIM E4 checkout UI + hosted card fields + confirmation | branch ws-e/checkout-ui. Lands `apps/storefront/src/app/checkouts/**` only — the last unowned route under apps/storefront. H2 flows (b)(c) unblock on this.
2026-08-28T21:30Z | WS-E | DONE E4 checkout UI. `/checkouts/[token]` (express placeholder → Contact → Delivery → shipping radios → Payment → Pay now, grey summary sidebar) and `/checkouts/[token]/thank-you`, both per PARITY.md. Card fields post the PAN straight to `/vault/tokenize` cross-origin; only `card_tok_…` reaches our server. H2 flows (b)(c) unblock.
2026-08-28T21:30Z | WS-E | LANDMINE for every storefront client component: do NOT use `crypto.randomUUID()`. It is secure-context only, so it is undefined on `http://{slug}.lvh.me:3002` — the documented dev URL — and works on `localhost` and in prod, which is the worst way for a bug to hide. Use `randomId()` from `apps/storefront/src/lib/random-id.ts` (`crypto.getRandomValues`). E4 hit this on Pay now; E2's analytics beacon was silently falling back too.
2026-08-28T21:30Z | WS-E | WS-H (H2): flow (b)/(c) walks storefront → add to cart → /checkout → fill Contact/Delivery → pick a rate → card `4242…` → thank-you shows `Confirmation #NNNN`. Decline with `4000000000000002`: red banner atop Payment, checkout stays open and payable. Note the shipping-rate list only appears once the address is complete (PARITY), so fill the address before asserting on rates.
2026-08-28T21:30Z | WS-E | The root layout renders a bare white shell for `/checkouts/**` (path comes from `src/middleware.ts` as `x-pathname`). If you add a storefront-wide chrome element, it will NOT appear on checkout — that is deliberate.
2026-08-28T21:30Z | WS-E | Contracts addition (additive): `completedOrderNumber` on `checkoutSchema`, populated by E3 only for a completed checkout. The thank-you page survives a refresh on it.
2026-08-28 14:35 | WS-QA | Integration review pass over main (all workstreams). Verified live: seeded-order refund works over HTTP (#51's fix holds), builder preview renders real themed pages, storefront purchase flow works API-side through order #1043. Broken for a human: /checkouts/{token} 404s after "Check out" (E4 unclaimed), and Orders/Customers/Discounts/Collections/Inventory/Analytics/Apps/Payments admin pages are still ComingOnline placeholders (C5/C6/B6/G3/G4/D4).
2026-08-28 14:35 | WS-QA | CLAIM checkout↔orders seam fixes | branch ws-qa/checkout-order-seams — orders/paid never fired on the storefront purchase path (charge() got no onPaid, and the seam needs an orderId that does not exist until after the charge); confirmation email button was unreachable (notifyOrder enqueued with no orderStatusUrl and the BullMQ job id dedupes a second enqueue); complete.ts still used its own pre-C4 findOrCreateCustomer and wrote the denormalized customer counters C4 deliberately ignores.
2026-08-28 14:40 | WS-G | Your G3 PR #54 is CONFLICTING with zero pr-checks — the silent-unmergeable state pr-health warns about. It needs `pnpm sync` from your worktree; Home and Analytics are the two biggest visible holes in the admin right now.
2026-08-28T14:47Z | WS-A | CLAIM A5 production stack (prod compose, Caddy, GHCR push, CustomDomain fallback) | branch ws-a/production-stack
2026-08-28T14:47Z | WS-E | CLAIM E5 customer accounts | branch ws-e/customer-accounts. Lands apps/api/src/routes/storefront/customers/** + apps/storefront/src/app/account/**, additive CUSTOMER_SESSION_COOKIE in config/constants.ts. Optional path — guest checkout stays default.
2026-08-28T15:53Z | WS-E | DONE E5 customer accounts | PR pending. Storefront endpoints: POST /storefront/api/customers/{register,login,logout}, GET|PUT /me, GET /me/orders — Host-resolved, customer session in a separate signed cookie (CUSTOMER_SESSION_COOKIE, Redis csess:*, per-shop payload; services/storefront/customer-sessions.ts). Register CLAIMS a guest row (password set on C4's find-or-create result; 409 if an account exists — never overwrites). Pages: /account/{login,register} + /account (order history, default-address form). For E4 (checkout prefill): GET /storefront/api/customers/me returns { customer: { email, firstName, lastName, defaultAddress, ... } } (storefrontCustomerSchema in contracts/customers.ts); forward the shopper's CUSTOMER_SESSION_COOKIE cookie server-side like the cart cookie — apps/storefront/src/app/account/session.ts customerCookieHeader() is reusable. For H1: seeded jane@example.com has ZERO orders, so the demo account page shows an empty order history — attach 2-3 demo orders to jane (or set a passwordHash on a customer who has orders) to make the E5 demo read real.
2026-08-28 20:15 | WS-G | CLAIM G4 apps, Admin REST API tokens, webhooks UI | branch ws-g/apps-admin-api
2026-08-28 20:55 | WS-G | DONE G4 apps + Admin REST API + webhooks UI. `/admin/api/apps` (CRUD, rotate-token, uninstall, webhooks CRUD, send-test-event, deliveries). Public `/api/{products,orders,customers}` — Bearer + `requireScope` from `apps/api/src/lib/scopes.ts`, per-token rate limit, envelope-identical to `/admin/api/*` because it calls the same B1/C2/C4 services. Admin UI at store/[slug]/apps.
2026-08-28 20:55 | WS-G | Token and webhook secret are returned ONCE (create/rotate, webhook create) and never readable after — tests assert the read paths cannot echo them and that only a hash is stored. Rotating or uninstalling kills the old token immediately, asserted end-to-end through the Bearer plugin.
2026-08-28 20:55 | WS-G | `test/helpers.ts` `createApiToken()` mints apps with `scopes: []` and takes no scopes argument; Bearer tests that need scopes update the row by token hash (see `test/admin-api.test.ts`). A `scopes` parameter would be a tidier fix for whoever owns A2 next.
2026-08-28T23:59Z | WS-B | Critical-review fixes (branch ws-b/critical-review-fixes): (1) form-shaped product PUT no longer wipes variant fields it does not carry [contracts]; (2) option rename keeps variant ids/skus/stock, client + server; (3) the real Locations page now lives at /store/{slug}/locations, REPLACING A4's placeholder as instructed (settings/locations removed — nothing linked it); (4) smart-collection condition value field holds raw typed text (the minor-units round-trip made "$25" untypeable and ate spaces in text conditions); (5) products index gained the B5-mandated sort control and vendor filter; (6) inventory adjust polices only decrements, so a restock can dig a deny variant out of a negative balance; plus small parity fixes (sold-out reads "0 in stock", smart-collection sort select no longer desyncs from what is saved).
2026-08-28T21:45Z | WS-D | CLAIM D4 payments settings UI + charge saved card | branch ws-d/payments-settings-ui
2026-08-28T22:40Z | WS-D | DONE D4 payments settings UI + charge saved card. `/store/{slug}/settings/payments`: providers card (mock one-click, stripe secret-key modal verified via verifyCredentials, maverick creds-optional simulated), disconnect with confirm, routing rules editor (weight %, brand multi-select, min/max amount, up/down reorder, client-side <=100-per-condition-group validation mirroring the server, contextual save bar, wholesale PUT). Pure draft logic in `settings/payments/routing-draft.ts` (7 tests). Client sorts cardBrands before PUT so equivalent rules land in one server-side weight group.
2026-08-28T22:40Z | WS-D | New endpoint: `GET /admin/api/payments/payment-methods?customerId=` (requirePermission('orders'), default-first) — feeds the order page's card list. Contracts additions (additive): `PaymentMethod` type, `listPaymentMethodsQuery`. Also fixed `connected` derivation: simulated maverick no longer renders an Error badge (DECISIONS.md).
2026-08-28T22:40Z | WS-C | Your order detail page gained two lines (flagged, per the D4 issue's mount instruction): import + `<ChargeSavedCard order={order} onCharged={refresh} />` in the right column. It renders nothing unless the order's customer has a saved card; on charge it toasts and invalidates ['order', id] so your payment card picks up the new row. Component lives in `settings/payments/charge-saved-card.tsx` (WS-D's).
2026-08-28T22:40Z | WS-H | Seed now saves cards for the first three repeat buyers (one holds Visa+Mastercard, e.g. marcus.oyelaran@example.com) plus jane, via `prisma/seed/pay.ts` — the vault blob is sealed inline because db cannot import pay (DECISIONS.md). Demo beat: open any order of those customers -> Payment methods card -> Charge -> prefilled amount -> "payment collected" toast, new captured Payment on the order. Verified live against the seeded stack (charge captured through the real vault/router; 70/30 routing split survives a refresh).
2026-08-28T22:45Z | WS-H | CLAIM H2 Playwright smoke (5 flows) | branch ws-h/e2e-smoke
2026-08-28T23:55Z | WS-H | H2 flow (b) exposed and fixed a WS-C gap (small change in apps/api/src/services/orders/refund.ts + one Tax row in the C5 refund page; all 7 fulfillment-refund tests green, one added): refunds never included tax, so financialStatus could not reach `refunded` on a taxed order. Caveat for pre-fix refunds: the SEED's partially-refunded orders recorded tax as per-line percentOf, which can differ from the allocate share by 1¢ on multi-line orders — refunding their remainder can land a cent short of (stuck partially_refunded) or over (409) the order total. New orders compose exactly.
2026-08-28T23:55Z | WS-H | H2 note for CI: playwright.config.ts now probes the Next apps by localhost port and maps `*.lvh.me → 127.0.0.1` inside the browser, but the storefront SSR still resolves `{slug}.lvh.me` via the runner's DNS for its API fetches — if lvh.me public DNS ever flakes on a runner, that is the remaining exposure (fix would be a hosts entry or STOREFRONT_BASE_DOMAIN override in the workflow).
2026-08-28T23:59Z | WS-H | DONE H2 Playwright smoke — all five flows real, green 3x locally + pnpm verify | PR #74. Config hardening: port-based webServer probes + browser host-resolver rule for *.lvh.me. Includes the refund-tax fix (see the WS-H lines above) — [contracts]: refundCalculationSchema gains taxAmount.
2026-08-28T15:58Z | WS-E | E5 is PR #75 (auto-merge armed) — supersedes the 'PR pending' note above.
2026-08-28T16:20Z | WS-H | CLAIM H3 polish sweep, README, demo script | branch ws-h/polish-sweep. Parity audit + cross-cutting empty-state/skeleton/toast licence, README quickstart, docs/DEMO.md. Supersedes the unpushed ws-h/polish-readme-demo claim (zero work on it).
2026-08-28T20:20Z | WS-H | DONE H3 polish sweep, README, demo script | branch ws-h/polish-sweep. Parity audit of all 37 admin pages + storefront + checkout against PARITY.md, the cross-cutting empty-state/skeleton/toast sweep, README (demo walkthrough + Production architecture) and a new timed docs/DEMO.md. `pnpm verify` green.
2026-08-28T20:20Z | WS-H | Real bugs this sweep found and fixed, in case they recur: a FULLY refunded order's payment card read "Partially refunded" and a PENDING one read "Paid" (the heading was a boolean ternary, not `financialStatus`); `Fulfilled` was a green badge on the order detail and grey everywhere else; the order header had NO Refund action at all (Cancel sat where Refund belongs); fulfill/refund painted a blank white page on a load failure; a discount's summary card computed fixed amounts with `Math.round(Number(value) * 100)` — a float multiply, wrong for zero-decimal currencies; Settings' shipping-rate and staff Delete were unconfirmed one-click destructive actions that ALSO opened the edit modal underneath (Polaris `ResourceItem` fires `onClick` for any click in the row — wrap row buttons in a stop-propagation div, as inventory already did); Home rendered a blank screen on first paint (`if (!session) return null`) and flashed "0 of 4 tasks complete" on a fully-set-up store.
2026-08-28T20:20Z | WS-H | WS-A (A3): page titles are now per-section `layout.tsx` files, not the shell. If you add a nav section, add a `layout.tsx` beside its pages with `export const metadata = { title: 'X' }` or the tab falls back to "Merchant". See DECISIONS.md for why the client-effect version had to go and for the `template` inheritance trap.
2026-08-28T20:20Z | WS-A / WS-B / WS-C (A5, B5, B6, C5, C6, G4) | Left for you, all confirmed and NOT fixed by me: (1) pagination "1-50 of N" is impossible on every admin index because `paginated()` in `packages/contracts/src/common.ts` returns no total — needs a `totalCount` on the envelope plus each list service; already noted in DECISIONS. (2) The orders index Customer column shows the EMAIL, not a name — `orderSummarySchema` carries no customer name (WS-C). (3) The orders index passes `filters={[]}` though `listOrdersQuery` already accepts `financialStatus`/`fulfillmentStatus` (WS-C, C5). (4) Products, customers, discounts, collections, inventory, locations and apps indexes are NOT `fullWidth`; PARITY says index tables are full-width and only orders passes it. (5) `PageSkeleton` renders `SkeletonPage primaryAction`, so pages whose real header has no primary action flash a phantom button (WS-A).
2026-08-28T20:20Z | WS-H (H1) | The seed still gives `jane@example.com` ZERO orders, so the E5 account page demoes an empty order history. I made that empty state deliberate rather than papering over it, but the demo reads better if you attach 2-3 orders to jane or set a passwordHash on a customer who already has some.
2026-08-28T20:20Z | WS-H | Two things DEMO.md documents as presenter steps because they are real gaps in Definition of Done #3: a brand-new shop has no connected processor and `shippingRates: []`, so its checkout cannot complete until the presenter connects Mock Gateway (Settings -> Payments) and adds a shipping rate. Also SPEC §17 says CI "builds and pushes images" — `.github/workflows/main-checks.yml` builds with `push: false` and no registry login, so publishing is the one part not yet true.
2026-08-28T20:20Z | WS-H | Soft-404 worth knowing about, pre-existing and NOT introduced here: a missing product or collection on the storefront renders the correct 404 PAGE but with HTTP 200, because the layout's shell flushes before the page calls `notFound()`. User-visible behaviour is right; only the status code is wrong. Unknown-shop `/` correctly 404s.
2026-08-29T00:20Z | WS-D | Review fixes on branch ws-d/review-fixes (critical-review of workstream D): charge idempotency hardening (UI key-per-attempt + server replay fingerprint), orders/paid on capture not authorize, order financialStatus settled by charge-saved-card/capture, stale refund-reservation expiry + refund idempotency to the processor, Settings→Payments error states + inline validation (blank weight, negative bounds), currency-aware money inputs. [contracts]: chargeSavedCardInput gains orderId/checkoutId (additive); routing conditions amounts tightened to non-negative. FOR E3/E4: `savePaymentMethod` (packages/pay/src/router.ts) still has NO caller — the spec'd `savePaymentMethod?` flag on checkout complete was never built, so real customers cannot save cards (seed-only today).
2026-08-28 | WS-G | CLAIM post-review fixes across G (from critical-review of workstream G) | branch ws-g/review-fixes. Scope: webhook-subscription soft delete (delivery history really does survive, as the UI already promises), targeted "send test event" (stop fanning a test at every same-topic subscription incl. other apps), emit customers/create (currently a dead topic), refunds/create body gains refund {id, amount}, Analytics "today" read from raw events instead of the ≤5-min-stale rollup, plus copy/pagination nits. Touches C4's services/customers/customers.ts (two one-line emit calls via a new services/customers/events.ts wrapper — same pattern as catalog/events.ts) and services/orders/{notify,refund}.ts (the seams G already fills).
2026-08-28 | WS-G | DONE post-review fixes | branch ws-g/review-fixes. Webhook subscription delete is now SOFT (deletedAt; @@unique([appId,topic,url]) dropped — migration 004_wsg) so the delivery log really survives, as the delete dialog promises; "send test event" targets its one subscription (emitWebhookEvent {subscriptionId}, optional in the job schema, worker narrows) instead of blasting every same-topic endpoint incl. other apps; customers/create now actually fires (from C4's createCustomer + findOrCreateByEmail-when-created via new services/customers/events.ts); refunds/create carries refund {id, amount: Money}; Analytics reads TODAY from raw events even though the worker upserts today's rollup (was up to 5 min stale, and visibly disagreed with the Live card); dead `granularity` dropped from analyticsRangeQuery [contracts]; delivery log pages at 50 per §5; two lying UI strings fixed. New tests: worker webhook-deliver job selection (targeted + soft-deleted, real PG + real receiver), apps soft-delete history/re-subscribe, analytics today-from-raw (verified failing before the fix). pnpm verify green.
2026-08-29T00:35Z | WS-D | Review fixes are PR #78 (auto-merge armed) — see the WS-D line above for the E3 handoff (savePaymentMethod has no caller).
2026-08-28T17:10Z | WS-H | CLAIM e2e hardening (critical-review follow-up) | branch ws-h/e2e-hardening — deepen flow (a) with edit-and-resave (the PUT path PR #66 actually regressed on), complete payment in flow (c), move flow (d) to a scratch shop so pnpm e2e stops defacing the demo theme.
2026-08-28T17:15Z | WS-H | DONE e2e hardening — all six green 3x locally against an isolated prod-build stack on alt ports (worktree DB). Flow (a) now edits one variant price post-create and asserts the other three survive the form-shaped PUT + the $24.00 – $26.50 index range; flow (c) pays and asserts $26.53 on the thank-you page; flow (d)+(e) share signupFreshShop. NOTE for whoever fights ports next: three different sessions' dev stacks were squatting 3000-3002 AND 4001 today; `pnpm stack status` names holders, and a prod-build stack on free ports (API_PORT/next start --port + API_URL/ADMIN_URL/STOREFRONT_BASE_DOMAIN env) is how this was verified on the 8GB machine without joining the war.
2026-08-28T17:40Z | WS-F | CLAIM ai-queue shared-producer collapse (critical-review follow-up) | branch ws-f/ai-queue-shared-producer
2026-08-28T17:55Z | WS-F | DONE ai-queue collapse — generation.ts delegates to enqueue(QUEUES.ai, JOB_NAMES.aiThemeGeneration, …); worker job + JobDefinition.name now compile-pinned to JOB_NAMES. Verified: pnpm verify green, plus a LIVE round trip (real Redis db 7, worker running, no ANTHROPIC_API_KEY): producer enqueued, worker consumed by name, parsed payload with aiThemeJobPayload, resolved the pending bubble to the apology — status pending→failed observed in builder_conversations.
2026-08-28T20:50Z | WS-H | WS-C (C5): `orderSummarySchema` gains an optional `customer: { firstName, lastName } | null` (additive, defaults to null — no migration, no breaking change). The orders index was rendering the EMAIL in Shopify's Customer column because the summary carried no name; `listOrders` now joins `customer: { select: { firstName, lastName } }` and the admin falls back to the email only for a guest order or a customer with neither name set. `listOrders` is the sole caller of `toOrderSummary`, so there is no path left that produces a nameless summary — if you add one, join the relation or the column silently regresses to emails. C4's customer detail table reuses `listOrders` and gets this for free.
2026-08-28T20:50Z | WS-B / WS-C / WS-G | Your index pages now pass `fullWidth` (products, collections, inventory / customers, discounts / apps), matching orders and PARITY.md. `/locations` is intentionally left narrow — see DECISIONS.md.
2026-08-28T18:10Z | WS-H | CLAIM H4 UX/motion parity audit | branch ws-h/ux-motion-parity. Announcing the cross-cutting motion licence (same boundary as H3): motion/transition/micro-interaction fixes in ANY app during this sweep — logic changes still go to the owning workstream via this log. First PR writes the PARITY.md "Motion & interaction" section, then admin sweep in traffic order (Home, orders, products, tail), then storefront+checkout.
2026-08-28 20:15 | WS-C | CLAIM critical-review fixes (orders/customers/discounts, API + admin UI) | branch ws-c/critical-review-fixes
2026-08-28 21:10 | WS-C | DONE critical-review fixes [contracts] [schema] — the two criticals: storefront sales no longer decrement stock twice (fulfil nets out checkout's reservation), and refunds are idempotent end-to-end (key persisted on Refund, migration 004_wsc_refund_idempotency; admin sends a per-attempt nonce, not a form-shape hash). Plus: refundedTotal increment (no RMW race), row-locked fulfil, cancel claim via updateMany, cancel-after-full-refund works, fulfillmentStatus counts refunded units, discount usage give-back on cancel, deterministic discount ordering, free-shipping minimum post-discount, oncePerCustomer engine support, orders index filters compose with AND (tab+search no longer clobber), customers delete guard + full-name search + address CRUD + contact edit modal, honest error states across all C pages, payment card no longer says "Paid"/"Outstanding" on cancelled orders. `pnpm verify` green.
2026-08-28 21:10 | WS-C | WS-E, to activate oncePerCustomer at checkout: pass `priorUsage` into both applyDiscounts calls in services/checkout/totals.ts when a customer is known — `Object.fromEntries((await db.discountRedemption.groupBy({ by: ['discountId'], where: { customerId }, _count: true })).map(c => [c.discountId, c._count]))`. Omit for guest checkout (today's behavior). Rejection surfaces as `usage_limit`, which your message map already renders.
2026-08-28 21:10 | WS-C | WS-E, seam note: fulfil detects checkout reservations via `sold` adjustments whose referenceId is the ORDER id — it relies on afterSale's checkoutId→orderId re-point. If that best-effort update ever fails, that order's fulfilment decrements again. Webhook consumers: `refunds/create` payload gained additive `refund: { id, amount }` — it previously carried only the order (with the order TOTAL, not the refund amount).
2026-08-28 21:32 | WS-E | oncePerCustomer is now WIRED at checkout (supersedes WS-C's relay note above): priceCheckout resolves the checkout email to a customer and passes their DiscountRedemption counts as priorUsage into computeCheckoutTotals — guests and emails with no customer row skip both reads and keep the old behavior exactly. End-to-end test in checkout.test.ts ("refuses a oncePerCustomer code the same email already redeemed"). Done by the WS-C fix session with user sign-off, as a two-file seam change in apps/api/src/services/checkout/{totals,checkout}.ts.
2026-08-28T19:55Z | WS-H | DONE H4 UX/motion parity | PR pending. PARITY.md gains the binding "Motion & interaction" section — read it before touching any animation. Fixes landed under the licence: keepPreviousData on every index + analytics query (new optional flag on useApiQuery — use it on any new index page), always-mounted modals in settings/payments + locations, PageSkeleton layout="detail" for the 7 two-column detail pages, bespoke analytics skeleton (280px chart reservation, controls render during load), builder ThinkingDots→Spinner, preview iframe survives device toggle, storefront global prefers-reduced-motion collapse, checkout de-animated (skeleton pulse + Pay-now hover transition removed), slideshow dots stop animating width, collection-list hover zoom clipped. Verified in-browser on localhost stack (ports 4100-4102) + all 6 smoke flows green. Handoffs: WS-C/C5 — orders index is selectable={false}; Shopify has row checkboxes + bulk actions there (static drift, not motion). WS-E/E2 — variant select never swaps the gallery image; contracts' variant.imageUrl is defined but read nowhere (PDP gallery is a static Server Component). Optional: use the itemCount already returned by cart actions for an optimistic header-badge bump. WS-G/G2 — analytics 7-day range showed summary cards all $0.00 while the chart plotted nonzero daily revenue; summary vs series disagree for sub-30d ranges.
2026-08-29T08:15Z | WS-A | DONE A5 production stack | PR #86 — `BASE_DOMAIN=localhost docker compose -f docker-compose.prod.yml --profile mail up -d --build` brings up the whole platform behind Caddy (internal CA locally, LE notes in deploy/caddy/Caddyfile): https://admin.localhost, https://demo.localhost, with /storefront/api/* and /vault/* peeled off to api on every shop origin so the beacon and hosted card fields work on 443. One-shot db-setup runs migrations + seed-if-empty, so replicas never race and a restart never reseeds; verified with two api replicas. main-checks now pushes ghcr.io/pauljurgens11/shopify/{api,admin,storefront,worker}:{sha,latest}. Custom domains resolve end to end: tenancy falls back to CustomDomain for hosts outside the base domain, CORS admits registered domains, Caddy mints per-host certs on demand. For H3 (README "Production architecture"): the compose header documents the scale path, BASE_DOMAIN contract, secret regeneration (SESSION_SECRET, VAULT_MASTER_KEY), and the admin-image caveat (NEXT_PUBLIC_* bakes at build → rebuild admin for a new domain via the compose build args).
2026-08-29T08:15Z | WS-A | NOTE for WS-E (per A5's coordination instruction): filled the TODO in apps/storefront/src/lib/tenant.ts — a host outside STOREFRONT_BASE_DOMAIN now resolves through GET /storefront/api/shop on that hostname (the API's tenancy fallback owns the CustomDomain table), cached via Next fetch revalidate:60. Nothing else in your tree was touched.
2026-08-29 10:20 | WS-QA | Repo review pass over main (887ae0b). Ran it, not read it: all mandatory suites green (829 unit tests, tenancy 13/13, pay 147, discount math 35), fulfil + refund flows live in the admin, builder preset-apply + no-key chat fallback live, storefront cart→checkout live. Confirmed #84's refund idempotency fix by replaying identical refund POSTs (one PaymentRefund, no double-record) — before the pull the same test double-recorded, so that fix is load-bearing.
2026-08-29 10:20 | WS-QA | Filed C7 (orders index passes filters={[]} though the API composes financialStatus/fulfillmentStatus — H3's item 3, still unfixed), E6 (completeCheckoutInput.saveCard accepted-and-ignored, savePaymentMethod has zero callers — real shoppers cannot save cards; either wire it or delete the field), H5 (seed dead-ends: jane has no orders, no seeded app/webhook/deliveries, abandoned-checkouts segment always empty). Also still true on main: A5 unstarted (no prod compose/Caddy, CI push:false), H4 claimed 2026-08-28T17:40Z with zero code on any branch, PageSkeleton phantom primaryAction (H3 item 5).
2026-08-29 10:20 | WS-QA | CLAIM checkout money-format fixes | branch ws-qa/checkout-money-format — checkout-view.tsx + order-summary.tsx hand-rolled `amount / 100` formatters (wrong for zero-decimal currencies, at the exact page a shopper decides to pay) now use config's format(); collections tab "Smart" relabelled "Automated" to match its own type column; worktree-env.sh now FAILS when Redis slots are exhausted instead of silently writing db 0 (the DECISIONS-208 bug bit two live sessions today — both reviews shared main's session keyspace and clobbered each other's logins).
2026-08-29 11:05 | WS-QA | CLAIM thank-you receipt freeze | branch ws-qa/thank-you-receipt — REGRESSION found live, introduced by #85: priceCheckout reprices COMPLETED checkouts, so the shopper's own DiscountRedemption trips oncePerCustomer and the thank-you page silently drops the discount (charged $180.65, receipt showed $200.73, rejectedDiscount usage_limit on the shopper's own code). e2e flow (c)'s final $26.53 assert would catch it on a quiet machine. Fix: a completed checkout serves the ORDER's recorded totals + discountCodes verbatim (it is a receipt, not a quote) — also freezes the receipt against later tax/shipping/discount edits. Regression test added and mutation-checked (fails on the old code). Includes the checkout.test.ts float-multiply cleanup.
2026-08-29 11:20 | WS-QA | Second repo-review pass (independent of the 10:20 one; ran, not read): full purchase driven through the live stack — cart → checkout → WELCOME10 → /vault/tokenize → complete → order #1041 → captured mock payment → one `sold` adjustment → confirmation email in Mailpit; fulfil + partial refund ($69.44 with allocated tax) walked in the admin UI. Independently found the #85 thank-you regression (receipt showed $34.99 for a $32.39 charge, rejectedDiscount usage_limit) — #89 landed the better receipt-freeze fix while my #91 was in flight, so #91 is closed superseded. Confirmed main-checks is GREEN again as of run 33241651265; it had been red since #85.
2026-08-29 11:20 | WS-QA | Unstuck #88 (CONFLICTING, zero checks — the pr-health trap): merged origin/main into its branch, resolved scripts/worktree-env.sh to main's side (both sides had fixed the same DECISIONS-208 redis-slot bug independently), pushed without force; auto-merge fired and #88 is on main.
2026-08-29 11:20 | WS-QA | Filed E7 (theme publish never revalidates the storefront's 60s theme cache — DECISIONS-224's accepted gap, but the Publish modal promises "right now" and the demo beat looks broken for up to ~2 min; small E-owned revalidation hook specced). Seed now mints shipping-rate ids with ship_ not evt_ (only the seed was wrong; the A4 service already used newId('shippingRate')). Left for WS-A, small: apps/admin/src/lib/search.ts fetchGroup still swallows ALL errors to [] — justified while B1/C2/C4 were unmerged, now it eats 500s and global search looks empty instead of broken.
2026-08-29T11:00Z | WS-H | CLAIM critical-review fixes (seed timestamps/rollups/refund-tax, e2e demo-pollution cleanup, skeleton fullWidth, DECISIONS-239 enforcement) | branch ws-h/critical-review-fixes
2026-08-29T11:30Z | WS-H | DONE critical-review fixes | branch ws-h/critical-review-fixes. Seed: history-end clamp for corrections/fulfillments/refunds/cancels, refund tax seeded via the engine's allocate split (seeded partials now compose to exactly `total` when the remainder is refunded), refund timeline event stamped at the Refund row's instant, every closed day gets rollup rows (60/60, was 59), inv_adj_/conv_ id prefixes, seed-if-empty completion gate, seed.test gains sold-vs-fulfilled reconciliation + day-coverage + timestamped fingerprint, and packages/db has a vitest.config so the CLAUDE.md single-file recipe loads DATABASE_URL. E2E: flows (a)/(c) clean up (delete product / refund with restock) so pnpm e2e stops defacing the demo (§8), Monochrome Apply scoped to its row, webServer stdout piped, CI resolves *.lvh.me via local dnsmasq (closes the SSR DNS exposure logged 2026-08-28). API (WS-C handoff, same seam as the H2 tax fix): unitTaxValues falls back to unit-count weights when taxTotal>0 with zero taxable net — an order stuck short of `refunded` otherwise; regression test added. Admin: bulk delete removed from customers/discounts per DECISIONS (WS-C: your indexes changed — pull before editing), PageSkeleton fullWidth on the 7 index pages (zero-shift skeletons), Home shows a critical Banner instead of fake $0.00 when analytics fails. PARITY §Motion hover bound aligned to the logged 300ms.
2026-08-29T12:10Z | WS-E | CLAIM E6 checkout save-card (wire completeCheckoutInput.saveCard to D3's savePaymentMethod + the E4 checkbox) | branch ws-e/checkout-save-card
2026-08-29T12:10Z | WS-C | CLAIM C7 orders index filters (Payment status / Fulfillment status ChoiceLists wired to listOrdersQuery's financialStatus/fulfillmentStatus) | branch ws-c/orders-index-filters
2026-08-29T12:55Z | WS-C | DONE C7 orders index filters | branch ws-c/orders-index-filters. Payment status / Fulfillment status ChoiceLists on `IndexFilters`, mapped to C2's `financialStatus` / `fulfillmentStatus`; labels are `satisfies Record<OrderSummary['financialStatus'], string>` so a typo or a new enum member is a typecheck failure, not a 400 at runtime (both mutations verified). Also fixed two things the filters exposed: Cancel/Escape used to keep a filter applied while hiding its pill, and filtering to zero showed the illustrated "Your orders will show up here" with the whole filter row gone — no way back. Verified live on a localhost stack (admin :4200 / api :4201): Paid+Unfulfilled = 6 rows matching the API and the DB, pills clear individually and via Clear all, tab+search+sort+paging all still compose. WS-B (B5): your products index Vendor filter passes `shortcut: true`, which Polaris 13.9.5's FiltersBar ignores — the filter is buried behind "Add filter" and never renders as a pill (confirmed live). One-word fix: `pinned: true`. Env note for whoever lands here next: all 15 Redis slots in `worktree-env.sh` are taken by finished worktrees, so `pnpm worktree:env --migrate` fails outright — I created my own database by hand and took redis db 7 (empty, its worktree's branch is merged). Someone should archive the dead worktrees.
2026-08-29 11:45 | WS-QA | CLAIM H5 demo seed gaps | branch ws-h/demo-seed-gaps
2026-08-29 11:55 | WS-QA | DONE H5 | jane now owns orders #1016/#1027 (fulfilled) + #1037 (unfulfilled) — pinned indices in seed/orders.ts with the RNG draw kept so every other order is unchanged; new seed/apps.ts installs "Warehouse Sync" (token stored SHA-256-hashed, orders/create subscription at the echo receiver's http://localhost:4100/webhooks, 3 success deliveries whose payloads name real seeded orders); new seed/checkouts.ts adds 2 open checkouts on yesterday (always <48h — C4's 72h segment window can never age them out mid-demo) for marcus + priya. resetDemoData now clears apps/webhooks/checkouts. Verified live: /admin/api/customers?segment=abandoned-checkout lists both, Apps shows the app, jane's /storefront/api/customers/me/orders returns all three. Seed suite 39/39 green incl. determinism-twice and four new invariants.
2026-08-29 11:30 | WS-QA | CLAIM E7 theme publish revalidation | branch ws-e/publish-revalidation
2026-08-29 11:40 | WS-QA | DONE E7 | publish now busts the storefront's theme cache: theme fetch tagged `theme:{slug}` (apps/storefront lib/shop.ts), new POST /api/revalidate route verifies an HMAC token (packages/config/revalidate-token.ts, 60s TTL, SESSION_SECRET) and revalidateTag's it; the API's publish route fire-and-forgets the ping (services/themes/revalidate.ts, 3s timeout, never throws). Measured live on an isolated stack: publish → storefront flip in 1.3s (was 40–120s); bad/foreign/absent token all 403. Caveat: dev-mode FIRST hit compiles the route for 8s and the ping times out — falls back to the old 60s window by design; prod has no compile. e2e flow (d)'s 90s publish poll can shrink in a follow-up once this is on main.
2026-08-29 12:30 | WS-F | Raised the AI-builder timeout ladder for larger models: SDK per-call 120s→240s (ai-theme-generate.ts), STALE_PENDING_MS 5→9 min (api themes/conversation.ts), POLL_GIVE_UP_MS 6→10 min (admin use-builder.ts). These three are ONE ladder — worst-case model time (2 semantic attempts x the SDK timeout) must stay under the sweep, which must stay under the poll give-up. Raise one, raise all three, or the sweep flips a still-running generation to Failed and the worker later overwrites a bubble nobody is polling (replaceMessage matches on id, it does not re-check status). Each constant now names the other two in its comment. Prompted by ANTHROPIC_MODEL=claude-fable-5, whose turns run well past Sonnet's; the old 120s ceiling turned a slow generation into the apology message.
2026-08-29T12:50Z | WS-QA | CLAIM repo-review fixes (index filter pills, skeleton phantom button) | branch ws-qa/demo-fixes. Found by driving main in a browser: (1) WS-B's products Vendor filter never rendered — `shortcut: true` is ignored by Polaris 13.9.5's IndexFilters, only `pinned` is read, so the filter bar showed a bare "Add filter" and the `?vendor=` query B1 supports was unreachable from the UI; (2) `PageSkeleton` always passed `primaryAction`, so Orders, Home, Inventory, Marketing, Storefront and every form page flashed a phantom grey button that then vanished (H3's item 5, filed 2026-08-28, still open); (3) products' `cancelAction` kept the vendor filter while hiding its pill — reachable only once (1) was fixed, so fixed in the same PR. All three verified in-browser before and after, on a localhost stack.
2026-08-29T12:25Z | WS-E | DONE E6 checkout save-card | PR #101. `saveCard` is now read: `services/checkout/complete.ts` calls D3's `savePaymentMethod` from inside `afterSale` after a successful charge, swallowing its own errors (the money has moved — a save must never fail the order). THE RULE, for anyone touching this: a card is filed only against a customer session for THIS shop whose id equals the customer the order resolved to. Checkout always creates a customer by email, so "has a customer" is not the test, and E5's register CLAIMS a guest row by email — saving on the typed address alone would hand the next person to sign up on it a stranger's card. Rows are deduped on brand+last4+expiry, not the vault token (fresh per checkout). New export for anyone who needs an optional storefront session read: `currentCustomerId(request, shopId)` from `services/storefront/customer-sessions.ts` — returns null instead of throwing; E5's `requireCustomer` now shares it.
2026-08-29T12:25Z | WS-E | WS-D (D4) / WS-H (H5): the Charge-saved-card block is no longer seed-only — a shopper who ticks "Save this card for future purchases" at checkout produces a real `PaymentMethod`, and `GET /admin/api/payments/payment-methods?customerId=` returns it unchanged. H5's seed gaps still stand on their own; this just means the repeat-billing demo works on a customer you create live.
2026-08-29T12:25Z | WS-E | LANDMINE for anyone editing `/checkouts/[token]`: the page now prefills a signed-in shopper's email (E5's prefill handoff, finally claimed) with a one-shot `updateCheckout` guarded on `!checkout.email`. That prefill is load-bearing for E6 — the save-card box only renders while the typed email still matches the account, so removing the prefill silently makes the checkbox unreachable rather than visibly broken. `payForCheckout` now forwards the CUSTOMER_SESSION_COOKIE alongside the cart cookie; both are httpOnly, so the server action is the only hop that can.
2026-08-29 13:30 | WS-B | Collection detail (`/store/{slug}/collections/{id}`) rebuilt to `docs/parity/collection-detail.md`, all six deltas: inline-editable title/description over a 145px image drop zone in a heading card with no card title; a `Collection items` card carrying the count badge, the `"Add conditions or products to populate your collection"` helper, the status filter chip + `Clear all`, the conditions builder and a 4-up product grid; a right rail whose `Products` heading has the sort popover and whose bordered group holds `Add condition` / `Add products` with `Exclude` below; `Duplicate` / `View` / `More actions ⌄` in the page header and no primary button. New files under `collections/_components/`: `collection-header-card`, `collection-image`, `collection-items`, `collection-items-card`, `products-rail`; `image-card.tsx` is gone (the image moved into the heading card) and `rules-builder` is no longer a card of its own. Verified live against a seeded stack — every flow driven in a browser, membership/reorder/conversion/duplicate confirmed with `pnpm db:query`.
2026-08-29 13:30 | WS-B | Skeleton scope now matches the capture: only the item grid skeletonises (4 grey tiles + caption bars) while the page header, both cards and the filter chip stay rendered, and the badge shows the collection's saved `productCount` rather than flashing `0`. Anyone doing the same for another detail page: the pattern is a page-level `PageSkeleton` for the entity fetch only, and a region skeleton for the slow secondary query.
2026-08-29 13:30 | WS-B | New pure module `apps/admin/src/lib/collection-edits.ts` (`membershipEdit`, `statusChipLabel`, `STATUS_CHOICES`) with `collection-edits.test.ts` — 10 tests, mutation-checked. `membershipEdit(before, after, ordered)` returns `null` when there is nothing to send and never carries positions unless the collection sorts manually; reuse it if another surface ever edits collection membership.
2026-08-29T12:05Z | WS-G | DONE dashboard parity (docs/parity/dashboard.md deltas 1-5) | branch claude/exciting-ardinghelli-e313ad. Home is now built from `dashboard.md` rather than from the onboarding capture: both Home and /analytics share a filter PILL row (date range, comparison period, currency), four metric tiles with dotted-underline tooltip labels and an inline delta that reads `—` rather than vanishing, a two-series `Total sales over time` line chart (comparison dashed in a lighter tint, dot legend centred below), a `Total sales breakdown` list card, and `No data for this date range` as the per-card empty state everywhere. The date-range control is the real popover: preset rail + two-month DatePicker + typed date fields + Cancel/Apply, nothing committed until Apply. [contracts] additive: `comparisonSalesOverTime`, `salesBreakdown`, `comparisonSalesBreakdown` — see DECISIONS. Note for anyone using `MetricCard`: `help` is now an optional prop; pass it and the label gets Shopify's dotted underline + tooltip, omit it and the label renders plain (marketing's three tiles are untouched).
2026-08-29T12:20Z | WS-B | DONE product-form parity | branch claude/product-form-parity-58c056. Rebuilt `/products/new` and `/products/:id` against docs/parity/product-form.md: breadcrumb header (icon › title) + ⋯ menu instead of `backAction`, rich-text Description (contenteditable + execCommand toolbar: block format, B/I/U, colour, alignment, lists/indent/link/clear, Show HTML) replacing the textarea, Price / Inventory / Shipping as left-column cards above Variants with Shopify's collapsed-pill pattern (Compare-at, Charge tax, SKU, Barcode, Sell when out of stock), Search engine listing card (page title, meta description, URL handle — the only place `handle` is sent), Collections in Product organization, lazy Type/Vendor/Tag suggestion comboboxes, `Unsaved product` save bar, Save pinned bottom-right. [contracts][api] `productSchema.collectionIds` is new and the product write now syncs MANUAL collection membership — pull before your next products change. HANDOFFS: WS-A — `SaveBar` gained an optional `message` prop (default unchanged). WS-H/e2e — flow (a) now fills the description by role and saves via the new `saveViaSaveBar` helper, because the form has two Save buttons like Shopify's. WS-C/WS-B/WS-E — the breadcrumb header is on the product form only; orders/customers/collections detail pages still render the older arrow-button header.
2026-08-29T13:35Z | WS-QA | FILED H6 (docs/issues/H6-next-start-standalone.md, in INDEX): both Next apps set `output: 'standalone'` for the Dockerfiles, and both `package.json` `start` scripts — which `e2e/playwright.config.ts` boots the admin and storefront with — run `next start`, which Next prints "does not work with output: standalone" on every run. Not cosmetic: on that stack the storefront's Add-to-cart Server Action lands server-side but its `revalidatePath` never reaches the client (POST 200 then `net::ERR_ABORTED`), so the button sticks on "Adding…" forever and the header cart badge stays stale — the exact thing `cartRequest`'s revalidate comment claims to prevent. Identical steps under `pnpm dev` are fine, so the DEMO path is unaffected and the Docker images (which run `server.js`) are fine; what is affected is that the mandatory §14 suite runs a third, unsupported configuration and stays green through a visible storefront bug. Left for WS-H rather than fixed here because the two candidate fixes both touch A5's production image path.
2026-08-29T13:35Z | WS-QA | Two smaller things left NOT fixed, both cosmetic, for whoever is next in these files. WS-A: `apps/admin/src/app/icon.svg` is still the pre-#97 "Merchant" bag mark — nothing links to it (layout.tsx pins `icons: { icon: '/favicon.svg' }`), but Next still serves it at `/icon.svg`, so it is dead code carrying the superseded brand; delete it. WS-A/WS-G: `apps/admin/public/favicon.svg` and `apps/admin/public/shopify-bag.svg` are byte-identical duplicates of the same mark — keep one. Also still open from H3's 2026-08-28 list: `paginated()` in packages/contracts/src/common.ts returns no total, so "1-50 of N" remains impossible on every admin index (needs a `totalCount` on the envelope plus every list service; too wide for a QA PR).
2026-08-29T13:20Z | WS-QA | Doc drift from #97 (Shopify branding) and #102 (Home is now the dashboard), found by driving main in a browser: DEMO.md Beat 1 told the presenter to point at a Home setup guide that no longer renders on the seeded store — OnboardingCard returns null once all four checks pass, which is exactly Aurora's state — and DEMO.md still said to call the platform "Merchant" and never say the other company's name. README's Notes claimed "the Shopify name and logo are not used", and its walkthrough described the old Home and the old Analytics card set. All corrected against the running app; the setup-guide beat is now framed as the Aurora/Northwind contrast (Beat 9 still shows it at 1 of 4, which I confirmed is still true on a fresh shop's code path). WS-H owns both files — pull before your next edit.
2026-08-29T14:40Z | WS-H | DONE H6 | branch ws-h/next-start-standalone. `output: 'standalone'` now gated on `NEXT_OUTPUT`, set in both Dockerfiles; `next start` boots both apps clean (warning gone, verified), and `NEXT_OUTPUT=standalone` still emits `.next/standalone/apps/<app>/server.js` for the images (verified on both, and the emitted storefront server was booted and served a PDP). See DECISIONS for why the alternative fix was rejected.
2026-08-29T14:40Z | WS-H | CORRECTION to my own H6 writeup of 13:35Z, and FILED E8. The add-to-cart hang I blamed on `output: 'standalone'` is NOT caused by it — I had a `next dev` and a `next start` sharing one `apps/storefront/.next`, which clobbers the build; that is what produced the 400s on static chunks. Re-measured cleanly with Playwright, 5 runs per condition: `next dev` 5/5 settled, `next start` 0/5, and the standalone `server.js` the Docker image runs 1/5. So it is a real production bug in the storefront, now docs/issues/E8. Excluded (each disabled alone, rebuilt, 5 runs): revalidatePath, the middleware's request-header rewrite, `cookies().set()` in the action, a duplicate React, and missing chunks. The server is innocent — replaying the action POST with curl returns a complete correct text/x-component stream in 0.05s; the browser reports the same request 200 then net::ERR_ABORTED with no console error. WS-E: E8 carries the harness and the next three things to try. Note for anyone testing this: never run `next dev` and `next start` against the same app directory.
2026-08-29 13:55 | WS-B | `RichTextField` (products/_components) is now shared with the collection form and takes an optional `labelHidden`. It is the admin's one description editor; if a third surface needs it, move it to `src/components/` rather than adding a second. `lib/description-html.ts` is gone with its test — nothing unwraps html into plain text any longer, so anyone reaching for `htmlToText`/`textToHtml` should ask why their field is not a `RichTextField`.
2026-08-29T14:40Z | WS-B | DONE index parity (docs/parity/index-tables.md, all five deltas) | branch ws-b/parity-index-detail. `components/shell/index-chrome.tsx` gives every index the THREE distinct empty states the capture describes — kind A centred, kind B (Products) left-aligned promo, and kind C (magnifier, no button) for filtered-to-zero, which every index had been serving the illustrated "add your first product" state for. Plus `Learn more about <resource>` under all seven index cards, chrome-first skeletons (header, card, tabs and filter row render immediately; `IndexTableSkeleton` fills the table body, and the empty-state flags are gated on `!loading`), and Collections/Customers take their captured search placeholders. Bulk actions already replace the header row in place — verified live, no change needed. Shared: `RichTextField` moved to `components/` (two consumers now) and the breadcrumb header hoisted to `components/shell/page-breadcrumb.tsx`, which is the follow-up #107 and #104 both asked for in comments; product form, new customer and collection detail now render one component. SCOPE CUT MID-FLIGHT: this branch also rebuilt the collection detail and the customer form, and #108 and #107 landed better versions of both while it was open — the rebase takes theirs wholesale and keeps only the index work plus the two hoists. Verified live on an isolated stack (admin :4300 / api :4301, own DB + redis): kind C state, footer link, in-header bulk actions and the chrome-first index skeleton all confirmed in the browser.
2026-08-29T14:40Z | WS-B | DONE index parity (docs/parity/index-tables.md, all five deltas) | branch ws-b/parity-index-detail. `components/shell/index-chrome.tsx` gives every index the THREE distinct empty states the capture describes — kind A centred, kind B (Products) left-aligned promo, and kind C (magnifier, no button) for filtered-to-zero, which every index had been serving the illustrated "add your first product" state for. Plus `Learn more about <resource>` under all seven index cards, chrome-first skeletons (header, card, tabs and filter row render immediately; `IndexTableSkeleton` fills the table body, and the empty-state flags are gated on `!loading`), and Collections/Customers take their captured search placeholders. Bulk actions already replace the header row in place — verified live, no change needed. Shared: `RichTextField` moved to `components/` (two consumers now) and the breadcrumb header hoisted to `components/shell/page-breadcrumb.tsx`, which is the follow-up #107 and #104 both asked for in comments; product form, new customer and collection detail now render one component. SCOPE CUT MID-FLIGHT: this branch also rebuilt the collection detail and the customer form, and #108 and #107 landed better versions of both while it was open — the rebase takes theirs wholesale and keeps only the index work plus the two hoists. Verified live on an isolated stack (admin :4300 / api :4301, own DB + redis): kind C state, footer link, in-header bulk actions and the chrome-first index skeleton all confirmed in the browser. SUPERSEDED mid-flight: WS-C landed customer-form parity in #107 while this branch was open, so the rebase takes their `/customers/new` wholesale and this branch only rewires it to the shared breadcrumb their own comment asked for.
2026-08-29 14:50 | WS-QA | Third repo-review pass (main @ 407a040→0e50c6e), ran not read: pnpm verify green (10/10; api 372), pay 147/147, tenancy 15/15, discount math 37/37, e2e 6/6 in 11.3s against an isolated prod-build stack (standalone server.js, ports 4300-4302). Demo beats 1-9 all driven in a browser: login, dashboard Home ($6,559.11/24 exactly per DEMO.md), orders C7 filters (Escape clears — good), preset apply → Publish → storefront flip in <2s (E7 holds), full checkout with WELCOME10 (decline then $26.53 pay, receipt keeps the discount), refund $26.53 → Refunded/net $0.00, analytics Today $26.53/1 order, webhook fired+retried 5x (exhausted only because the seeded :4100 receiver port was foreign), confirmation email in Mailpit, Northwind signup → setup guide 1 of 4 → empty isolated admin + themed storefront, jane@example.com sees her 3 seeded orders.
2026-08-29 14:50 | WS-QA | Landed: #111 (en-US date pinning — orders index/detail, timeline, version history rendered host-locale "28 Aug at 23:45" beside Apps' en-US; "1 items" plurals; 3 hand-rolled minor→major money conversions now call toDecimal; discount form RangeError on cleared start date → field error; stale pre-#97 Merchant icon.svg deleted), #112 (orders list phantom last-page cursor → limit+1 like every other service; complete.ts now records shippingLine.shippingRateId instead of null), #113 (payment_captured was a producer-less enum member — createOrder writes it for paid orders, DEMO beat 6 points at it).
2026-08-29 14:50 | WS-QA | Independently reproduced E8's add-to-cart hang on the standalone server.js (200 then ERR_ABORTED, "Added to your cart." renders, transition never settles) — corroborates WS-H's 14:40Z measurement from a second clean stack; E8 stands, nothing added to it. FILED E9 (applying a discount clears a still-qualifying selected shipping rate; sidebar reverts to "Enter shipping address" — seen live at the exact page a shopper pays on) and B7 (productImageSchema.variantIds: three API readers, zero writers, and the product form ERASES the field on every save — PDP gallery swap is structurally unreachable; absorbs H4's variant.imageUrl handoff).
2026-08-29 14:50 | WS-QA | Left for owners, confirmed not fixed: WS-C — fulfill page has no carrier field though the contract carries trackingCompany end-to-end (card shows a bare number where Shopify shows "UPS · 1Z…"); orders index selectable={false} vs Shopify's checkboxes (H4 already logged). contracts hygiene — webhooks.ts lines 8-52 are a dead superseded vintage of apps.ts (worker cites its .max(2000) as if binding), orderListResponse/productListResponse/deliveryListResponse are stale unused shapes (orderListResponse lacks the customer field the live endpoint returns); shopSchema.onboarding is furniture (Home derives from real state, nothing reads/writes it). root — CLAUDE.md §6 says dbAdmin is legal in exactly 3 places, biome.json allowlists 7 (all justified; update the prose or trim the list).
2026-08-29 14:50 | WS-QA | Env traps for the next reviewer: (1) worktree-env slots stay exhausted — I hand-made merchant_repo_review_skill_d78732 + shared redis db 12 (dead ws-b-collections-inventory-ui slot). (2) The in-app browser SHARES one cookie jar across parallel sessions, and _shopify_session lives on host "localhost" port-blind — two sessions' stacks on any localhost ports clobber each other's admin logins (bounced twice mid-review). Isolation that works: build the admin with API_URL=http://127.0.0.1:4301 and browse 127.0.0.1 — separate cookie host, peers stay on localhost. (3) e2e against a custom-port stack needs NEXT_PUBLIC_API_URL set too, or the cleanup adminApi calls silently hit api.lvh.me:3001 and flows (a)/(c) fail on teardown only.
2026-08-29T16:33Z | WS-E | CLAIM E8 storefront Server Actions never settle in a production build | branch ws-e/action-hang-production
2026-08-29T18:05Z | WS-E | DONE E8 storefront Server Actions never settle in a production build. Root cause is NOT the abort the issue leads with: React reads the action's whole flight and closes it cleanly on every run (CDP, 27,955/27,955 bytes) and still does not commit the tree — buffer the identical bytes into one chunk and it commits 6/6, stream them and it commits 1/6. Fix removes the dependency instead of racing it: no `revalidatePath` in cart actions, plain `useState` instead of `useTransition` in the two cart islands, header badge driven by the `itemCount` the action already returns (`apps/storefront/src/lib/cart-count.ts` + `components/cart-count.tsx`, module store + useSyncExternalStore), cart page reloads after a write because F1's `cart-page` section renders the line total and subtotal outside the `cartLine` slot. Measured 5/5 add-to-cart idle, 5/5 badge, 5/5 second-add-without-reload, 5/5 stepper — on `next start` AND on the standalone `server.js` (was 1/5). Checkout still gets the plain shell; middleware and next.config are unchanged.
2026-08-29T18:05Z | WS-E | E8 handoffs, each verified against the code, not guessed. **WS-E/E5 (`apps/storefront/src/app/account/actions.ts:140`)**: `updateProfileAction` is the only remaining `revalidatePath` in any Server Action in the repo, and `forms.tsx` renders its result through `useActionState` — same exposure E8 had, so the "Saved." banner and the `Saving…` label can stick on a production build. The other three account actions do not revalidate and are fine; `logoutAction` is safe because it navigates. **WS-E/E4 (`components/checkout/checkout-view.tsx:88`)**: every checkout save funnels through one `useTransition`, and `saving` gates both "Pay now" (:332) and discount "Apply" (:342). It survives today only because `updateCheckout` calls no `revalidatePath` — an action that revalidates nothing has no re-render to commit, and those settle 8/8 in my probes. Do not add a `revalidatePath` there. **Anyone**: `apps/admin` is immune — 36/36 admin pages are `'use client'` fetching through React Query against the REST API; it has zero Server Actions, zero `useTransition`, zero `router.refresh()`.
2026-08-29T18:05Z | WS-E | Explanation for my own 17:20Z observation (2): `/checkouts/[token]` keeping `loading.tsx` on screen while `document.body.innerHTML` already held the real markup is the SAME failure, one surface over — a streamed response whose late content never gets committed by the client. It is not the in-app browser. The buffered-vs-streamed experiment above is the cheap way to confirm any future sighting: intercept the response, hand React one chunk, and see whether the symptom disappears.
2026-08-29T18:05Z | WS-E | Testing note for everyone. `pnpm e2e` needs the shared 3000–3002; if the main stack owns them, a config with no `webServer` pointed at your own ports runs the real specs against your build — and the admin needs `NEXT_PUBLIC_API_URL` too, not just `API_URL`, or `adminApi()` silently goes to `api.lvh.me:3001` and every admin-API step fails with ECONNREFUSED. Also: `pkill -f "next start"` does not match a running Next server — it renames itself to `next-server (v15.5.24)`. Kill it by the PID on the port.
