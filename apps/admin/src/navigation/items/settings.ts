import type { NavItem } from '../types.ts';
export const settings: NavItem = {
  key: 'settings',
  label: 'Settings',
  url: '/settings',
  icon: 'SettingsIcon',
  permission: 'settings',
  position: 'bottom',
};
