'use client';

/**
 * Placeholder for the Analytics page. Replaced by the issue named below — a nav
 * item that 404s is a KPI failure, so every destination renders something real
 * (CLAUDE.md §8).
 */
import { ComingOnline } from '../../../../components/shell/page-skeleton.tsx';

export default function AnalyticsPage() {
  return <ComingOnline title="Analytics" description="The analytics dashboard lands with G3." />;
}
