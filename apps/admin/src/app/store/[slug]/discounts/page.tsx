'use client';

/**
 * Placeholder for the Discounts page. Replaced by the issue named below — a nav
 * item that 404s is a KPI failure, so every destination renders something real
 * (CLAUDE.md §8).
 */
import { ComingOnline } from '../../../../components/shell/page-skeleton.tsx';

export default function DiscountsPage() {
  return (
    <ComingOnline
      title="Discounts"
      description="Discount codes and automatic discounts land with C6."
    />
  );
}
