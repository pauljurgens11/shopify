'use client';

/**
 * Placeholder for the Products page. Replaced by the issue named below — a nav
 * item that 404s is a KPI failure, so every destination renders something real
 * (CLAUDE.md §8).
 */
import { ComingOnline } from '../../../../components/shell/page-skeleton.tsx';

export default function ProductsPage() {
  return (
    <ComingOnline title="Products" description="The product index and product form land with B5." />
  );
}
