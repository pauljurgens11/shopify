'use client';

/**
 * Placeholder for the Collections page. Replaced by the issue named below — a nav
 * item that 404s is a KPI failure, so every destination renders something real
 * (CLAUDE.md §8).
 */
import { ComingOnline } from '../../../../components/shell/page-skeleton.tsx';

export default function CollectionsPage() {
  return (
    <ComingOnline title="Collections" description="Manual and smart collections land with B6." />
  );
}
