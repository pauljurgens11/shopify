'use client';

/**
 * Placeholder for the Drafts page. Replaced by the issue named below — a nav
 * item that 404s is a KPI failure, so every destination renders something real
 * (CLAUDE.md §8).
 */
import { ComingOnline } from '../../../../../components/shell/page-skeleton.tsx';

export default function OrdersDraftsPage() {
  return <ComingOnline title="Drafts" description="Draft orders land with C5." />;
}
