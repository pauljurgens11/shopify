'use client';

/**
 * Placeholder for the Apps page. Replaced by the issue named below — a nav
 * item that 404s is a KPI failure, so every destination renders something real
 * (CLAUDE.md §8).
 */
import { ComingOnline } from '../../../../components/shell/page-skeleton.tsx';

export default function AppsPage() {
  return (
    <ComingOnline title="Apps" description="Private apps and Admin API tokens land with G4." />
  );
}
