'use client';

/**
 * Placeholder for the Settings page. Replaced by the issue named below — a nav
 * item that 404s is a KPI failure, so every destination renders something real
 * (CLAUDE.md §8).
 */
import { ComingOnline } from '../../../../components/shell/page-skeleton.tsx';

export default function SettingsPage() {
  return <ComingOnline title="Settings" description="Store settings land with A4." />;
}
