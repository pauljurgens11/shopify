# deps
node_modules/
.pnpm-store/

# builds
dist/
.next/
out/
.turbo/
*.tsbuildinfo

# env — never commit (SPEC §15)
.env
.env.*
!.env.example

# test artifacts
test-results/
playwright-report/
blob-report/
coverage/

# local infra volumes
.docker-data/

# os / editor
.DS_Store
Thumbs.db
*.log

# local dev stack runtime (pid files, logs)
.local/

# claude code — personal overrides (settings.json and launch.json ARE committed)
.claude/settings.local.json
.claude/*.local.json
