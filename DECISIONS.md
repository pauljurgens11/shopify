# DECISIONS

Append-only. One line per decision. Never edit or delete an existing line.

Format: `YYYY-MM-DD | WS{X} | decision — rationale`

Log here when you: resolve a SPEC.md ambiguity, use the Polaris 20-minute escape hatch, make a breaking `packages/contracts` change (log BEFORE the change), stub against another workstream after being blocked >30 min, or build a minimal version of something SPEC.md §2 lists as out of scope.

---

2026-08-28 | WS-A | Internal packages export TypeScript source (`exports: "./*": "./src/*.ts"`) with no build step — removes cross-package build ordering, which is the most common way a parallel-agent monorepo deadlocks.
2026-08-28 | WS-A | Prisma multi-file schema (`prisma/schema/*.prisma`, one file per domain) instead of a single `schema.prisma` — a single schema file would be the single hottest merge conflict in the repo.
2026-08-28 | WS-A | `@node-rs/argon2` instead of `argon2` — prebuilt binaries, so no node-gyp toolchain in CI or in agent sandboxes. Same argon2id algorithm, satisfies SPEC §8.
2026-08-28 | WS-A | `packages/contracts` has no barrel `index.ts`; consumers import subpaths (`@merchant/contracts/products`) — a shared barrel is a guaranteed conflict point and hurts tree-shaking.
2026-08-28 | WS-A | Admin navigation, theme sections, and worker job registries are pre-created complete from SPEC §9/§12/§13 — agents fill leaf files instead of editing a shared registry array.
2026-08-28 | WS-A | PR-required CI is limited to lint + typecheck + unit + tenancy (fast); Playwright e2e and docker build run post-merge on `main` — required checks that take 10 min would serialize 20 agents behind the merge queue.
2026-08-28 | WS-A | React 19 with Polaris 13 despite its `react@^18` peer range — verified the 13.9.5 build uses no findDOMNode, defaultProps, or propTypes (the three things React 19 removed). SPEC §3 locks both Polaris 13 and Next 15, and Next 15 App Router requires React 19, so the peer range is stale rather than a real conflict. Allowed explicitly via pnpm.peerDependencyRules.
2026-08-28 | WS-A | Root `.env` is loaded explicitly per app — `--env-file` for api/worker, `process.loadEnvFile()` in next.config for admin/storefront. Next only reads `.env` from its own directory, so a single root `.env` (SPEC §5) is otherwise invisible to it.
2026-08-28 | WS-A | Admin pages are Client Components; Polaris needs React context and breaks the build inside a Server Component. Verified: admin and storefront both build clean on Next 15.5 + React 19.
2026-08-28 | WS-A | Postgres is published on host port 5433, not 5432 — a stock Postgres or another project's container commonly already owns 5432, and the resulting bind error is a confusing first-run failure. Container port is unchanged.
2026-08-28 | WS-A | Prisma generates to its DEFAULT output, not a custom dir — a custom path is imported relatively, so bundlers inline the CJS client and its dynamic require("fs") throws under ESM. As `@prisma/client` it stays external everywhere.
2026-08-28 | WS-A | api and worker run from TypeScript source under tsx in production; no bundler. @fastify/autoload discovers routes by scanning the filesystem, so a single-file bundle has no route tree, and bundling also inlines Prisma/argon2 and breaks them. Costs ~1s startup compile; buys prod behaviour identical to dev. Verified by running both in NODE_ENV=production.
2026-08-28 | WS-A | Next apps build with `output: 'standalone'` + `outputFileTracingRoot` at the workspace root — pnpm's symlinked node_modules does not survive a cross-stage Docker COPY, and without the tracing root standalone silently omits workspace packages.
2026-08-28 | ROOT | Development plan lives as parallel-safe issue files in docs/issues/ (README = protocol, INDEX = grab list) — agents claim via docs/AGENT-LOG.md, one issue = one PR, tests named per issue land with the feature.
2026-08-28 | WS-A | Admin page routes live at apps/admin/src/app/store/[slug]/… (no route-group prefix) — WORKSTREAMS.md globs updated to match; URL mirrors admin.shopify.com/store/{slug} per SPEC §6.
2026-08-28 | WS-A | Autoload maps routes/admin→/admin/api and routes/storefront→/storefront/api via dirNameRoutePrefix — on-disk layout in WORKSTREAMS.md stays, URLs land on SPEC §5/§10 paths.
2026-08-28 | WS-A | dbForShop deep-stamps nested create/createMany/connectOrCreate via Prisma DMMF relation metadata, scopes Shop reads to id=shopId, scopes OrderSequence, memoizes clients per shop — nested connect and $queryRaw remain review-covered (see tenant.ts header).
2026-08-28 | WS-A | Session cookie NAME is constants.SESSION_COOKIE only; SESSION_COOKIE_NAME env var removed — one source of truth before auth lands.
2026-08-28 | WS-A | fromDecimal parses decimal strings digit-wise (BigInt), never via float multiply — Math.round(1.005*100)===100 was a live off-by-one-cent bug.
2026-08-28 | WSC | Engine `discountTotal` covers line discounts only and `shippingTotal` is already net of `shippingDiscount` — callers compute `subtotal - discountTotal + shippingTotal + tax`; folding free shipping into `discountTotal` (as C1's parenthetical suggested) double-counts it in checkout totals and does not match how Shopify shows shipping as "Free".
2026-08-28 | WSC | Discounts apply in the order products → order → shipping, each taking its percentage off what the previous ones left on the line — two stacked 10% automatics take 19%, not 20%.
2026-08-28 | WSC | `amount_off_products` with a `fixed` value is one pool taken off the eligible lines and split with `allocate`, not "$X off each item" — same code path as `amount_off_order`, so cents balance identically for both.
2026-08-28 | WSC | Only the code the shopper actually typed can be rejected; any other code-bearing row the caller passes is ignored silently — a rejection the shopper never triggered would render as a bogus checkout error.
2026-08-28 | WSC | Discount date window is authoritative and the denormalized `status` column only vetoes via `disabled` — a scheduled discount whose start time has passed is live without waiting for a job to update the row.
2026-08-28 | WSC | Minimum subtotal/quantity requirements are measured over the discount's own eligible lines, pre-discount, and a non-empty cart matching no eligible line rejects as `minimum_not_met` (Shopify's "not valid for the items in your cart").
2026-08-28 | WS-D | Vault ciphertext/iv/authTag stay in the three VaultCard columns rather than one packed base64 string (D1 wording) — the schema already has the columns, and ProcessorConfig mirrors them for credentials.
2026-08-28 | WS-D | `tokenizeCard(db, shopId, card)` takes shopId even though `dbForShop` stamps it: Prisma's generated create input still requires the column at the type level, so every tenant write must pass it (the extension overrides whatever is passed). Reads do not — `getCard(db, cardTokenId)` is scoped by the client alone.
2026-08-28 | WS-D | /vault/tokenize resolves its shop from the request Origin subdomain and is unauthenticated by design (SPEC §11) — at that point in checkout there is no session, only a storefront origin; abuse is bounded by RATE_LIMITS.checkoutPayment.
2026-08-28 | WSF | `renderSection(section, data)` takes a `SectionDataContext` — core sections need live product/collection/cart data, and interactivity arrives as slots WS-E owns (Server Components have no state). The registry map in `sections/index.tsx` is untouched; that map is what the no-edit rule protects.
2026-08-28 | WSF | Button appearance is derived at token time (`--theme-button-bg/-fg/-border` in `themeCssVariables`), not inside `ThemeButton` — CSS cannot branch on the *value* of a custom property, and a Server Component section has no access to tokens.
2026-08-28 | WSF | Presets are authored as zod *input* and parsed on every `presetThemeDoc()` call — fills every schema default, makes an invalid preset a CI failure rather than a broken storefront, and hands each caller an independent object to mutate.
2026-08-28 | WSF | The storefront cart CTA targets `/checkout` (Shopify's own path); WS-E creates the checkout there and redirects to WS-E4's `/checkouts/{token}`. Sections never build checkout URLs themselves — `shared/urls.ts` is the one place routes are written.
2026-08-28 | WS-D | `ProcessorAdapter` takes `creds` per call rather than binding them at construction (pre-existing, logged now per issue D2) — routing is per-merchant, so one process serves many shops' credentials; a per-shop adapter instance means a per-shop SDK client cache and a place for one tenant's key to serve another.
2026-08-28 | WS-D | `ProcessorAdapter.authorize` also takes `CardMaterial` (PAN + cvc), declared in `packages/pay/src/adapter.ts` and deliberately NOT in `packages/contracts` — SPEC §11's sketch passes only `cardTokenId`, but an adapter cannot charge a token. The router detokenizes and hands the material one hop; both ends are inside `packages/pay`, so no PAN-bearing type is reachable from api/admin/storefront.
2026-08-28 | WS-D | The `…0119` outage card hard-fails on `mock` but APPROVES in `maverick` simulated mode — a router failover is only demonstrable end-to-end if the second processor is healthy for the same card. Deliberate asymmetry, not a bug.
2026-08-28 | WS-D | `maverick.verifyCredentials({})` returns true — simulated mode is a working mode that the demo routes real checkouts through, so it reports connected. `maverickMode(creds)` is the live/simulated discriminator for the D4 settings badge, and every simulated approval carries `raw.simulated === true`.
2026-08-28 | WS-D | Stripe `StripeInvalidRequestError`/`StripeIdempotencyError` map to `declined`+`processing_error`, not `hard_failure` — the contract's `hard_failure` is `retryable: true` by construction, and failing over on our own bad request either repeats the bug or double-charges. Same for issuer-side `try_again_later`/`issuer_not_available`: the issuer answered, and the next processor reaches the same issuer.
2026-08-28 | WS-A | Session payload is a SNAPSHOT of role+permissions taken at login, not re-read per request — a DB round trip per admin call to catch a permission edit is not worth it. A4 must destroy a staff user's sessions when it changes their role or permissions.
2026-08-28 | WS-A | signupInput.shopSlug is now OPTIONAL (additive loosening, no consumers yet): omitted, the slug is derived from shopName and de-duplicated Shopify-style (`aurora-supply-co`, `-2`, `-3`…). An explicitly supplied slug that is taken is still a 409, so the signup form can offer a "store URL" field.
2026-08-28 | WS-A | Login with an email that exists in several shops and no `shopSlug` → 400 `invalid_request` with field `shopSlug`, rather than picking one. Email is unique per shop, not globally (SPEC §7).
2026-08-28 | WS-A | CSRF is enforced ONLY on mutations whose shop was proved by a session cookie (`/admin/api/*`). Any non-empty `x-requested-with` passes — a cross-origin fetch that sets a custom header needs a preflight, which CORS already refuses, so pinning an exact value buys nothing. Bearer and Host-resolved requests are exempt; `/auth/login` stays curl-able.
2026-08-28 | WS-A | Admin API tokens are NOT cached in the tenancy resolver (only the storefront slug→shop lookup is, 30s TTL) — uninstalling an app has to cut it off on the next request, and Bearer traffic is not hot enough to pay for a revocation window.
2026-08-28 | WS-A | ApiError is detected by a `Symbol.for` brand (`isApiError`) instead of `instanceof`: @fastify/autoload imports route files with a plain dynamic import, so under Vitest routes and the error handler can hold two copies of errors.ts and every ApiError a route threw rendered as a 500.
2026-08-28 | WS-A | @fastify/rate-limit's errorResponseBuilder returns an ApiError (it throws whatever the builder returns), so 429s take the normal error path — status and SPEC §5 body come from one place, and the special-case 429 branch in error-handler.ts is gone.
2026-08-28 | WS-A | Default API_URL is http://api.lvh.me:3001, not localhost:3001 — the SameSite=Lax session cookie is only sent to an API that shares a site with the admin (`*.lvh.me`); a localhost API drops it on every XHR and the admin looks logged out. Storefront callers use `{slug}.lvh.me:3001`, because that Host is what resolves their tenant.
2026-08-28 | WS-D | The payment router takes `db: TenantClient` directly and its tests run against a real Postgres (CI already starts one) instead of hiding persistence behind a port — idempotency dedupe is a unique index and the refund cap is a SUM over PaymentRefund rows; both are database facts, and a fake store would have proved neither.
2026-08-28 | WS-D | Routing weights are validated per *identical conditions* group (canonicalised JSON), not globally: rules only compete when their conditions overlap, full overlap analysis is not worth building, and "split my Visa traffic 60/40" is the case a merchant actually creates.
2026-08-28 | WS-D | All-zero routing weights fall back to the merchant's own position order rather than routing nowhere — an untouched slider is not an instruction.
2026-08-28 | WS-D | Added `routingRule: 'rule'` to `packages/config` ID_PREFIXES (additive) — reusing `proc_` for a rule id makes logs lie about what an id refers to, which is the whole reason the prefix scheme exists.
2026-08-28 | WS-D | Order-level refunds stay C3's route (`POST /admin/api/orders/:id/refunds`); D3 exposes `refundPayment()` and does NOT add `/admin/api/payments/:id/refund`, so the refund cap and the PaymentRefund row are computed in exactly one place.
2026-08-28 | WS-D | `POST /admin/api/payments/charge-saved-card` requires `orders`, not `settings` — it moves a customer's money, so it belongs to whoever works orders. Processor/routing config remains `settings`.
2026-08-28 | root | `pnpm stack` (scripts/stack.sh) always drives the MAIN checkout, from whichever worktree you invoke it in, and gives it its own database `merchant_main` rather than the shared `merchant`. Every worktree points at one Postgres container, so agents running `db:reset` or applying an unmerged migration would otherwise make main's stack throw on columns main does not have. `MERCHANT_SHARED_DB=1` opts back into the shared database. The stack also reclaims :3000/:3001/:3002 from stale dev servers belonging to this repo's other worktrees, naming each one it kills.
2026-08-28 | WSF | The theme engine has exactly two client leaves — `SlideshowControls` (dots + autoplay) and `InertForm` (newsletter/contact). Every section stays a Server Component; interactivity the storefront owns arrives through F1's slots instead.
2026-08-28 | WSF | Newsletter and contact forms acknowledge the visitor client-side rather than posting anywhere — SPEC §2 rules out marketing email and nothing in scope receives a contact message, and a submit that reloads the page reads as broken where an inline "thanks" does not.
2026-08-28 | WSF | A grid whose handles resolve to nothing renders a skeleton that keeps the grid's shape instead of disappearing — model-authored handles go stale the moment a merchant renames a collection, and a section that vanishes reads as a broken page in the builder preview.
2026-08-28 | WSF | The slideshow track is addressed by React ref, not a DOM id — a theme may place two slideshows on one page, and a shared id makes the second one's dots drive the first one's track.
2026-08-28 | WS-B | The variant table is DERIVED from the option matrix on every write, never taken verbatim: `POST/PUT /admin/api/products` expands options to the cartesian product (first option varies slowest, `"S / Black"`), and a payload variant is matched to a combination by its `id` first and its option values otherwise. That is what makes "add a size" additive — surviving rows keep their ids, so B4's inventory levels and B5's inline edits survive an option edit. A combination the caller did not send inherits price/flags from their first variant but never its sku or barcode.
2026-08-28 | WS-B | `updateProductInput` treats `undefined` as "leave alone" and `[]` as "reset": omitting `variants` keeps the existing rows, sending `variants: []` collapses to the single `Default Title` variant. Same for `options` and `images`. A product PUT that only changes `status` therefore cannot touch the variant table.
2026-08-28 | WS-B | Ceilings on the option builder, matching Shopify: 3 options, 100 values per option, 100 variants per product — over the limit is `invalid_request`, not a 40k-row insert.
2026-08-28 | WS-B | A price whose `currencyCode` is not the shop's is `invalid_request`, not silently reinterpreted. Multi-currency is out of scope (SPEC §2), so the only way to send one is a client bug, and money columns are amounts in the shop currency.
2026-08-28 | WS-B | Renaming a product does NOT move its handle — the storefront URL only changes when `handle` is sent explicitly, and an explicit clash is a 409 (`field: "handle"`) rather than a silent rename. A DERIVED handle still walks `-2`, `-3`… like Shopify. Product handles use their own slugifier (`services/catalog/handles.ts`), not WS-A's `lib/slug.ts`, whose 3-character minimum and `store-xxxx` fallback are shop-URL rules.
2026-08-28 | WS-B | `ID_PREFIXES.option = 'opt'` added to `packages/config/src/ids.ts` (additive): `ProductOption.id` has no default and the SPEC §5 list has no entry for it, as with `cart`/`ful`/`ref`/`img` already there.
2026-08-28 | WS-B | `createVariantInput.inventoryQuantity` is ACCEPTED AND IGNORED by the products API: stock only moves through B4's adjustment service, so writing it here would create levels with no `InventoryAdjustment` history. Reads report the sum of existing levels. B4 owns wiring the field through.
2026-08-28 | WS-H | The seed's mock `ProcessorConfig` leaves `encryptedCredentials` null instead of sealing dummy creds (H1 step 8): the mock adapter takes no credentials, and `packages/db` cannot import `packages/pay` to seal a pair — pay already depends on db, and turbo rejects the resulting package cycle outright (verified). D3/D4 should treat "mock, enabled, no credentials" as the connected state for that processor.
2026-08-28 | WS-H | Seeded free shipping is a shipping *rate* with a $150 minimum, not a discount applied to the shipping line — that keeps `discountTotal` purely line-level, so `Σ lineItem.totalDiscount === order.discountTotal` holds exactly. The automatic free-shipping Discount row still exists for C6's index and E3's checkout; the seeded history just does not route through it.
2026-08-28 | WS-H | Seeded history ends at the end of yesterday (`OLDEST_HISTORY_DAY = 1`) rather than including today. Anchoring to whole UTC days makes the seed deterministic per date — clamping "today" rows against the current instant made content depend on the clock time the seed ran at — and it keeps today genuinely open, which is the split the dashboard's rollups-plus-today's-raw-events design assumes (SPEC §13).
2026-08-28 | WS-H | The seed derives every `InventoryLevel.available` from the sum of its `InventoryAdjustment` rows rather than writing levels directly, and sold-out variants are produced by negative `correction` adjustments. The level and its history are therefore incapable of disagreeing, which is what CLAUDE.md §9's "never a raw update" rule is protecting.
2026-08-28 | root | Every worktree gets its OWN Postgres database (`merchant_<worktree>`) and Redis db index via `pnpm worktree:env` — they previously all shared `merchant` on db 0. This is correctness, not tidiness: ws-g's `ALTER TABLE webhook_deliveries ADD COLUMN "eventId" TEXT NOT NULL` is correct on its branch and breaks every other worktree's Prisma client the moment it is applied, and `pnpm db:reset` (a documented command) drops the database out from under whoever is mid-test-run. Cost is ~10 MB of disk per worktree and zero RAM. Ports 3000/3001/3002 stay SHARED on purpose: the collision is the only thing stopping nine Next dev stacks from running at once on an 8 GB machine that already pages.
2026-08-28 | WSC | `createOrder` asserts `subtotal - discountTotal + shippingTotal + taxTotal === total` and that every amount is in the order's currency, then stores the totals verbatim — it records, it never prices (C1/E3 do). The assert turns an upstream pricing bug into a 400 at the moment it happens instead of an order that is a cent wrong forever.
2026-08-28 | WSC | Order numbers are claimed with `UPDATE order_sequences SET next = next + 1 RETURNING` inside the order transaction; the row itself is created before the transaction opens with INSERT … ON CONFLICT DO NOTHING, because a failed INSERT inside the transaction would roll the whole order back.
2026-08-28 | WSC | Index tabs, with no "archived" flag in the schema: `closed` = cancelled OR fully fulfilled, `open` = its complement, `unfulfilled`/`unpaid` additionally exclude cancelled orders. Cursor pages order by the sort key then `orderNumber` — never by `id`, because ULIDs minted in the same millisecond are not ordered and the page boundary would drift.
2026-08-28 | WSC | Order creation writes a `DiscountRedemption` per applied code as well as incrementing `usedCount` (the counter alone cannot answer "has THIS customer used it"), and uses `updateMany` so a discount deleted between pricing and payment cannot roll back a paid order.
2026-08-28 | WSC | Cancel restocks inside `services/orders/cancel.ts` rather than through B4's adjust service, which is not on main yet — the invariant it preserves is B4's own: never move an InventoryLevel without writing its InventoryAdjustment. Swap it for the adjust service when B4 lands.
2026-08-28 | WSC | Order lifecycle webhooks/emails go through `services/orders/notify.ts`, a seam that currently does nothing because G1's producer is not on main. When G1 lands, that one file forwards to `emitWebhookEvent` / `enqueueOrderConfirmationEmail`; it must never throw, since the order is already committed by then.
2026-08-28 | root | Root `.npmrc` sets `package-import-method=hardlink`. Measured on this machine: a worktree's node_modules shares 1 MB of 839 MB with the pnpm store under the default import method, and 753 MB of 753 MB when hardlinked — so each of ten worktrees was costing a real ~840 MB instead of ~0. The tradeoff is that node_modules files share inodes with the store, so editing one in place would change it for every worktree; patch through pnpm.patchedDependencies instead. `pnpm stack disk` reports which worktrees still hold their own copy.
2026-08-28 | WS-B | Quantities move with `available = available + delta` in SQL inside a transaction, never read-then-write. That statement holds the row lock until commit, so simultaneous decrements serialise — a read-modify-write loses 4 of 8 concurrent sales (measured; `test/inventory.test.ts` asserts the sum). Everything that changes stock imports `services/inventory/adjust.ts`; a bare `inventoryLevel.update` anywhere else is the CLAUDE.md §9 landmine.
2026-08-28 | WS-B | Inventory batches are ALL-OR-NOTHING and lock in a sorted key order. A fulfillment that cannot decrement its third line must not have decremented the first two, and two batches touching the same levels in opposite orders would otherwise deadlock.
2026-08-28 | WS-B | `InventoryLevel` rows are created LAZILY on first stock movement, not backfilled when a location is added — adding a location to a 30-product store would otherwise fan out a row per variant for stock that is not there. `GET /admin/api/inventory` reports 0 for a missing level, so the table never shows a hole.
2026-08-28 | WS-B | `adjust` refuses to take a `deny` variant below zero (409 `conflict`); a `continue` variant is allowed to go negative, because that is exactly what overselling is. `set` takes an absolute count, is `>= 0` by contract, and records the delta it had to apply — so `set` and `adjust` leave the same audit trail.
2026-08-28 | WS-B | Deleting the last location is a 409: a shop with nowhere to hold stock cannot fulfil. Deleting any other cascades its levels; the `InventoryAdjustment` history carries no foreign key and deliberately outlives what it describes.
2026-08-28 | WS-B | `actor` on an adjustment is stamped from the session by the route, never taken from the request body — the history drawer's "who" must not be client-supplied.
2026-08-28 | WS-B | Contracts additions (additive): `setInventoryInput`, `bulkSetInventoryInput`, `adjustInventoryBody`/`setInventoryBody` (both endpoints take one change or a batch), `inventoryLevelsResponse`, `inventoryAdjustmentSchema`, `InventoryRow`. New id prefix `inv_adj` for adjustments, alongside the existing `card_tok` precedent for a multi-segment prefix.
2026-08-28 | ROOT | GitHub does not apply `.gitattributes` merge drivers when computing PR mergeability — verified by merging the same two commits with and without `.gitattributes` present. Concurrent appends to `DECISIONS.md`/`docs/AGENT-LOG.md` therefore make PRs CONFLICTING on the server even though they rebase cleanly locally, and a conflicting PR gets NO `pr-checks` run at all (there is no merge commit for `pull_request` to run against), so it fails silently rather than red. Mitigated by `.github/workflows/pr-health.yml` (labels + comments every unmergeable PR) and `pnpm sync` (rebase + push); the optional `rebase` job in that workflow does it automatically once `AGENT_PAT` exists — it cannot use `GITHUB_TOKEN`, whose pushes do not trigger workflows.
2026-08-28 | root | No MCP database connector. Agents read the database through `pnpm db:query`, which runs psql inside the compose container with `default_transaction_read_only=on` — Postgres refuses the write, so the guarantee does not depend on an agent's judgement or a server's SQL parsing. An MCP server would add tool schemas to every one of ~20 agents' context windows to expose what `psql` already does.
2026-08-28 | root | `.claude/settings.json` and `.claude/launch.json` are committed, not local-only: the permission allowlist, the npm/yarn+force-push denials and the "no commit on main" hook are repo rules (CLAUDE.md §4, §9), and every worktree agent should inherit them. Personal overrides go in the gitignored `.claude/settings.local.json`.
2026-08-28 | WSF | The AI builder forces a single `set_theme` tool call whose input schema IS `themeDocSchema` (via zod-to-json-schema, `$refStrategy: 'none'`) — so the section catalog and every `.describe()` in contracts reaches the model as prompt text, and the schema cannot drift from what the renderer accepts. Refs are inlined because tool-use handles a flat schema far better than an internally-referenced one.
2026-08-28 | WSF | Generation validates against `themeDocSchema` + `validateThemeDoc` AND against the shop's real product/collection handles; a handle the shop does not have is a validation failure, not a render-time empty block. One retry with the errors fed back, then an apologetic chat message — the job never throws, because a thrown job retries on the queue and the merchant watches a spinner forever.
2026-08-28 | WSF | Preview tokens are HMAC-SHA256 over `{shopId, versionId, expiry}` with SESSION_SECRET, 15-minute TTL, compared in constant time — an unpublished theme must not be reachable on the public storefront by guessing an id.
2026-08-28 | WSF | `installInitialTheme` publishes the aurora preset on signup and deliberately never throws: a store that opens on a blank page is bad, a signup that 500s because the theme table hiccuped is worse. WS-A's signup handler calls it in two lines.
2026-08-28 | WS-A | Polaris AppProvider gets a `linkComponent` that renders Next's `<Link>` (apps/admin/src/components/shell/polaris-link.tsx) — Polaris emits a plain `<a href>` for every `url` prop, so without it each left-nav click was a full document load with a white flash. One provider-level hook makes every link in every workstream's pages a client-side navigation; do NOT also call router.push in an onClick, that navigates twice.
2026-08-28 | WS-A | The admin's API base URL is republished as NEXT_PUBLIC_API_URL by apps/admin/next.config.ts from API_URL, rather than added to packages/config/env.ts — env.ts is server-only (it parses DATABASE_URL and VAULT_MASTER_KEY) and the parity check forbids a .env.example key with no schema entry.
2026-08-28 | WS-A | Nav selection is computed in apps/admin/src/lib/nav.ts and passed to Polaris explicitly instead of relying on Polaris's URL matching — Products has to stay selected on /collections and /inventory, which are sibling URLs rather than children of /products.
2026-08-28 | WS-A | apiFetch turns an unreachable API into ApiError(0, 'internal', 'Could not reach the server…') — callers branch on ApiError, and a raw "Failed to fetch" in a Polaris banner tells a merchant nothing.
2026-08-28 | WS-A | PARITY.md's global-search line corrected: the results panel anchors under the search FIELD at roughly its width (current Shopify + Polaris TopBar.searchResults), not full-bar-width as originally written.
2026-08-28 | WS-E | The storefront reads collection membership from the `CollectionProduct` join table for smart collections as well as manual ones — it never evaluates a rule set. Shopify materializes smart membership the same way, and it keeps the collection page one indexed join instead of a rule interpreter on the hot path. That makes it B3's job to keep the join rows in step with a smart collection's rules; `collectionProductIds` in `services/storefront/collections.ts` is the single place to change if B3 ever moves to evaluating at query time.
2026-08-28 | WS-E | `price-asc|price-desc|best-selling` are ranked in application code (`rankedIds` in `services/storefront/products.ts`), not by an `orderBy`: price lives on the variant table and best-selling on order line items, and Prisma can express neither as a product-level sort. The whole matching set is ranked and then paged, which is the right trade while a live catalogue is tens to low hundreds of products (SPEC §2 has no bulk catalogue). The cursor stays a product id, so callers cannot tell the two paging strategies apart.
2026-08-28 | WS-E | The theme preview parameter is `<themeVersionId>.<hmac-sha256 over SESSION_SECRET>`, minted by `signThemePreview()` in `services/storefront/theme.ts`. A raw `?preview=thm_…` would let anyone enumerate a shop's unpublished drafts. An unsigned or tampered token falls back to the published theme rather than erroring, so a stale builder link shows the live store instead of a broken one. F3/F4 build preview links with that helper.
2026-08-28 | WS-E | Storefront cart path is singular `/storefront/api/cart`, not `/carts`. CLAUDE.md §5's plural rule is about collection resources; this is the singleton belonging to one browser, and Shopify's own path is `/cart`.
2026-08-28 | WS-E | Cart lines persist `variantId` + `quantity` only; title, image, price and availability are resolved from the live variant on every read, and a line whose product was deleted or unpublished is dropped rather than rendered. Snapshotting belongs at checkout creation (E3), where the shopper commits to a number.
