# DECISIONS

Append-only. One line per decision. Never edit or delete an existing line.

Format: `YYYY-MM-DD | WS{X} | decision — rationale`

Log here when you: resolve a SPEC.md ambiguity, use the Polaris 20-minute escape hatch, make a breaking `packages/contracts` change (log BEFORE the change), stub against another workstream after being blocked >30 min, or build a minimal version of something SPEC.md §2 lists as out of scope.

---

2026-08-28 | WS-A | Internal packages export TypeScript source (`exports: "./*": "./src/*.ts"`) with no build step — removes cross-package build ordering, which is the most common way a parallel-agent monorepo deadlocks.
2026-08-28 | WS-A | Prisma multi-file schema (`prisma/schema/*.prisma`, one file per domain) instead of a single `schema.prisma` — a single schema file would be the single hottest merge conflict in the repo.
2026-08-28 | WS-A | `@node-rs/argon2` instead of `argon2` — prebuilt binaries, so no node-gyp toolchain in CI or in agent sandboxes. Same argon2id algorithm, satisfies SPEC §8.
2026-08-28 | WS-A | `packages/contracts` has no barrel `index.ts`; consumers import subpaths (`@merchant/contracts/products`) — a shared barrel is a guaranteed conflict point and hurts tree-shaking.
2026-08-28 | WS-A | Admin navigation, theme sections, and worker job registries are pre-created complete from SPEC §9/§12/§13 — agents fill leaf files instead of editing a shared registry array.
2026-08-28 | WS-A | PR-required CI is limited to lint + typecheck + unit + tenancy (fast); Playwright e2e and docker build run post-merge on `main` — required checks that take 10 min would serialize 20 agents behind the merge queue.
2026-08-28 | WS-A | React 19 with Polaris 13 despite its `react@^18` peer range — verified the 13.9.5 build uses no findDOMNode, defaultProps, or propTypes (the three things React 19 removed). SPEC §3 locks both Polaris 13 and Next 15, and Next 15 App Router requires React 19, so the peer range is stale rather than a real conflict. Allowed explicitly via pnpm.peerDependencyRules.
2026-08-28 | WS-A | Root `.env` is loaded explicitly per app — `--env-file` for api/worker, `process.loadEnvFile()` in next.config for admin/storefront. Next only reads `.env` from its own directory, so a single root `.env` (SPEC §5) is otherwise invisible to it.
2026-08-28 | WS-A | Admin pages are Client Components; Polaris needs React context and breaks the build inside a Server Component. Verified: admin and storefront both build clean on Next 15.5 + React 19.
2026-08-28 | WS-A | Postgres is published on host port 5433, not 5432 — a stock Postgres or another project's container commonly already owns 5432, and the resulting bind error is a confusing first-run failure. Container port is unchanged.
2026-08-28 | WS-A | Prisma generates to its DEFAULT output, not a custom dir — a custom path is imported relatively, so bundlers inline the CJS client and its dynamic require("fs") throws under ESM. As `@prisma/client` it stays external everywhere.
2026-08-28 | WS-A | api and worker run from TypeScript source under tsx in production; no bundler. @fastify/autoload discovers routes by scanning the filesystem, so a single-file bundle has no route tree, and bundling also inlines Prisma/argon2 and breaks them. Costs ~1s startup compile; buys prod behaviour identical to dev. Verified by running both in NODE_ENV=production.
2026-08-28 | WS-A | Next apps build with `output: 'standalone'` + `outputFileTracingRoot` at the workspace root — pnpm's symlinked node_modules does not survive a cross-stage Docker COPY, and without the tracing root standalone silently omits workspace packages.
2026-08-28 | ROOT | Development plan lives as parallel-safe issue files in docs/issues/ (README = protocol, INDEX = grab list) — agents claim via docs/AGENT-LOG.md, one issue = one PR, tests named per issue land with the feature.
2026-08-28 | WS-A | Admin page routes live at apps/admin/src/app/store/[slug]/… (no route-group prefix) — WORKSTREAMS.md globs updated to match; URL mirrors admin.shopify.com/store/{slug} per SPEC §6.
2026-08-28 | WS-A | Autoload maps routes/admin→/admin/api and routes/storefront→/storefront/api via dirNameRoutePrefix — on-disk layout in WORKSTREAMS.md stays, URLs land on SPEC §5/§10 paths.
2026-08-28 | WS-A | dbForShop deep-stamps nested create/createMany/connectOrCreate via Prisma DMMF relation metadata, scopes Shop reads to id=shopId, scopes OrderSequence, memoizes clients per shop — nested connect and $queryRaw remain review-covered (see tenant.ts header).
2026-08-28 | WS-A | Session cookie NAME is constants.SESSION_COOKIE only; SESSION_COOKIE_NAME env var removed — one source of truth before auth lands.
2026-08-28 | WS-A | fromDecimal parses decimal strings digit-wise (BigInt), never via float multiply — Math.round(1.005*100)===100 was a live off-by-one-cent bug.
2026-08-28 | WSC | Engine `discountTotal` covers line discounts only and `shippingTotal` is already net of `shippingDiscount` — callers compute `subtotal - discountTotal + shippingTotal + tax`; folding free shipping into `discountTotal` (as C1's parenthetical suggested) double-counts it in checkout totals and does not match how Shopify shows shipping as "Free".
2026-08-28 | WSC | Discounts apply in the order products → order → shipping, each taking its percentage off what the previous ones left on the line — two stacked 10% automatics take 19%, not 20%.
2026-08-28 | WSC | `amount_off_products` with a `fixed` value is one pool taken off the eligible lines and split with `allocate`, not "$X off each item" — same code path as `amount_off_order`, so cents balance identically for both.
2026-08-28 | WSC | Only the code the shopper actually typed can be rejected; any other code-bearing row the caller passes is ignored silently — a rejection the shopper never triggered would render as a bogus checkout error.
2026-08-28 | WSC | Discount date window is authoritative and the denormalized `status` column only vetoes via `disabled` — a scheduled discount whose start time has passed is live without waiting for a job to update the row.
2026-08-28 | WSC | Minimum subtotal/quantity requirements are measured over the discount's own eligible lines, pre-discount, and a non-empty cart matching no eligible line rejects as `minimum_not_met` (Shopify's "not valid for the items in your cart").
2026-08-28 | WS-D | Vault ciphertext/iv/authTag stay in the three VaultCard columns rather than one packed base64 string (D1 wording) — the schema already has the columns, and ProcessorConfig mirrors them for credentials.
2026-08-28 | WS-D | `tokenizeCard(db, shopId, card)` takes shopId even though `dbForShop` stamps it: Prisma's generated create input still requires the column at the type level, so every tenant write must pass it (the extension overrides whatever is passed). Reads do not — `getCard(db, cardTokenId)` is scoped by the client alone.
2026-08-28 | WS-D | /vault/tokenize resolves its shop from the request Origin subdomain and is unauthenticated by design (SPEC §11) — at that point in checkout there is no session, only a storefront origin; abuse is bounded by RATE_LIMITS.checkoutPayment.
2026-08-28 | WSF | `renderSection(section, data)` takes a `SectionDataContext` — core sections need live product/collection/cart data, and interactivity arrives as slots WS-E owns (Server Components have no state). The registry map in `sections/index.tsx` is untouched; that map is what the no-edit rule protects.
2026-08-28 | WSF | Button appearance is derived at token time (`--theme-button-bg/-fg/-border` in `themeCssVariables`), not inside `ThemeButton` — CSS cannot branch on the *value* of a custom property, and a Server Component section has no access to tokens.
2026-08-28 | WSF | Presets are authored as zod *input* and parsed on every `presetThemeDoc()` call — fills every schema default, makes an invalid preset a CI failure rather than a broken storefront, and hands each caller an independent object to mutate.
2026-08-28 | WSF | The storefront cart CTA targets `/checkout` (Shopify's own path); WS-E creates the checkout there and redirects to WS-E4's `/checkouts/{token}`. Sections never build checkout URLs themselves — `shared/urls.ts` is the one place routes are written.
2026-08-28 | WS-D | `ProcessorAdapter` takes `creds` per call rather than binding them at construction (pre-existing, logged now per issue D2) — routing is per-merchant, so one process serves many shops' credentials; a per-shop adapter instance means a per-shop SDK client cache and a place for one tenant's key to serve another.
2026-08-28 | WS-D | `ProcessorAdapter.authorize` also takes `CardMaterial` (PAN + cvc), declared in `packages/pay/src/adapter.ts` and deliberately NOT in `packages/contracts` — SPEC §11's sketch passes only `cardTokenId`, but an adapter cannot charge a token. The router detokenizes and hands the material one hop; both ends are inside `packages/pay`, so no PAN-bearing type is reachable from api/admin/storefront.
2026-08-28 | WS-D | The `…0119` outage card hard-fails on `mock` but APPROVES in `maverick` simulated mode — a router failover is only demonstrable end-to-end if the second processor is healthy for the same card. Deliberate asymmetry, not a bug.
2026-08-28 | WS-D | `maverick.verifyCredentials({})` returns true — simulated mode is a working mode that the demo routes real checkouts through, so it reports connected. `maverickMode(creds)` is the live/simulated discriminator for the D4 settings badge, and every simulated approval carries `raw.simulated === true`.
2026-08-28 | WS-D | Stripe `StripeInvalidRequestError`/`StripeIdempotencyError` map to `declined`+`processing_error`, not `hard_failure` — the contract's `hard_failure` is `retryable: true` by construction, and failing over on our own bad request either repeats the bug or double-charges. Same for issuer-side `try_again_later`/`issuer_not_available`: the issuer answered, and the next processor reaches the same issuer.
2026-08-28 | WS-A | Session payload is a SNAPSHOT of role+permissions taken at login, not re-read per request — a DB round trip per admin call to catch a permission edit is not worth it. A4 must destroy a staff user's sessions when it changes their role or permissions.
2026-08-28 | WS-A | signupInput.shopSlug is now OPTIONAL (additive loosening, no consumers yet): omitted, the slug is derived from shopName and de-duplicated Shopify-style (`aurora-supply-co`, `-2`, `-3`…). An explicitly supplied slug that is taken is still a 409, so the signup form can offer a "store URL" field.
2026-08-28 | WS-A | Login with an email that exists in several shops and no `shopSlug` → 400 `invalid_request` with field `shopSlug`, rather than picking one. Email is unique per shop, not globally (SPEC §7).
2026-08-28 | WS-A | CSRF is enforced ONLY on mutations whose shop was proved by a session cookie (`/admin/api/*`). Any non-empty `x-requested-with` passes — a cross-origin fetch that sets a custom header needs a preflight, which CORS already refuses, so pinning an exact value buys nothing. Bearer and Host-resolved requests are exempt; `/auth/login` stays curl-able.
2026-08-28 | WS-A | Admin API tokens are NOT cached in the tenancy resolver (only the storefront slug→shop lookup is, 30s TTL) — uninstalling an app has to cut it off on the next request, and Bearer traffic is not hot enough to pay for a revocation window.
2026-08-28 | WS-A | ApiError is detected by a `Symbol.for` brand (`isApiError`) instead of `instanceof`: @fastify/autoload imports route files with a plain dynamic import, so under Vitest routes and the error handler can hold two copies of errors.ts and every ApiError a route threw rendered as a 500.
2026-08-28 | WS-A | @fastify/rate-limit's errorResponseBuilder returns an ApiError (it throws whatever the builder returns), so 429s take the normal error path — status and SPEC §5 body come from one place, and the special-case 429 branch in error-handler.ts is gone.
2026-08-28 | WS-A | Default API_URL is http://api.lvh.me:3001, not localhost:3001 — the SameSite=Lax session cookie is only sent to an API that shares a site with the admin (`*.lvh.me`); a localhost API drops it on every XHR and the admin looks logged out. Storefront callers use `{slug}.lvh.me:3001`, because that Host is what resolves their tenant.
2026-08-28 | WS-D | The payment router takes `db: TenantClient` directly and its tests run against a real Postgres (CI already starts one) instead of hiding persistence behind a port — idempotency dedupe is a unique index and the refund cap is a SUM over PaymentRefund rows; both are database facts, and a fake store would have proved neither.
2026-08-28 | WS-D | Routing weights are validated per *identical conditions* group (canonicalised JSON), not globally: rules only compete when their conditions overlap, full overlap analysis is not worth building, and "split my Visa traffic 60/40" is the case a merchant actually creates.
2026-08-28 | WS-D | All-zero routing weights fall back to the merchant's own position order rather than routing nowhere — an untouched slider is not an instruction.
2026-08-28 | WS-D | Added `routingRule: 'rule'` to `packages/config` ID_PREFIXES (additive) — reusing `proc_` for a rule id makes logs lie about what an id refers to, which is the whole reason the prefix scheme exists.
2026-08-28 | WS-D | Order-level refunds stay C3's route (`POST /admin/api/orders/:id/refunds`); D3 exposes `refundPayment()` and does NOT add `/admin/api/payments/:id/refund`, so the refund cap and the PaymentRefund row are computed in exactly one place.
2026-08-28 | WS-D | `POST /admin/api/payments/charge-saved-card` requires `orders`, not `settings` — it moves a customer's money, so it belongs to whoever works orders. Processor/routing config remains `settings`.
2026-08-28 | root | `pnpm stack` (scripts/stack.sh) always drives the MAIN checkout, from whichever worktree you invoke it in, and gives it its own database `merchant_main` rather than the shared `merchant`. Every worktree points at one Postgres container, so agents running `db:reset` or applying an unmerged migration would otherwise make main's stack throw on columns main does not have. `MERCHANT_SHARED_DB=1` opts back into the shared database. The stack also reclaims :3000/:3001/:3002 from stale dev servers belonging to this repo's other worktrees, naming each one it kills.
2026-08-28 | WSF | The theme engine has exactly two client leaves — `SlideshowControls` (dots + autoplay) and `InertForm` (newsletter/contact). Every section stays a Server Component; interactivity the storefront owns arrives through F1's slots instead.
2026-08-28 | WSF | Newsletter and contact forms acknowledge the visitor client-side rather than posting anywhere — SPEC §2 rules out marketing email and nothing in scope receives a contact message, and a submit that reloads the page reads as broken where an inline "thanks" does not.
2026-08-28 | WSF | A grid whose handles resolve to nothing renders a skeleton that keeps the grid's shape instead of disappearing — model-authored handles go stale the moment a merchant renames a collection, and a section that vanishes reads as a broken page in the builder preview.
2026-08-28 | WSF | The slideshow track is addressed by React ref, not a DOM id — a theme may place two slideshows on one page, and a shared id makes the second one's dots drive the first one's track.
2026-08-28 | WS-B | The variant table is DERIVED from the option matrix on every write, never taken verbatim: `POST/PUT /admin/api/products` expands options to the cartesian product (first option varies slowest, `"S / Black"`), and a payload variant is matched to a combination by its `id` first and its option values otherwise. That is what makes "add a size" additive — surviving rows keep their ids, so B4's inventory levels and B5's inline edits survive an option edit. A combination the caller did not send inherits price/flags from their first variant but never its sku or barcode.
2026-08-28 | WS-B | `updateProductInput` treats `undefined` as "leave alone" and `[]` as "reset": omitting `variants` keeps the existing rows, sending `variants: []` collapses to the single `Default Title` variant. Same for `options` and `images`. A product PUT that only changes `status` therefore cannot touch the variant table.
2026-08-28 | WS-B | Ceilings on the option builder, matching Shopify: 3 options, 100 values per option, 100 variants per product — over the limit is `invalid_request`, not a 40k-row insert.
2026-08-28 | WS-B | A price whose `currencyCode` is not the shop's is `invalid_request`, not silently reinterpreted. Multi-currency is out of scope (SPEC §2), so the only way to send one is a client bug, and money columns are amounts in the shop currency.
2026-08-28 | WS-B | Renaming a product does NOT move its handle — the storefront URL only changes when `handle` is sent explicitly, and an explicit clash is a 409 (`field: "handle"`) rather than a silent rename. A DERIVED handle still walks `-2`, `-3`… like Shopify. Product handles use their own slugifier (`services/catalog/handles.ts`), not WS-A's `lib/slug.ts`, whose 3-character minimum and `store-xxxx` fallback are shop-URL rules.
2026-08-28 | WS-B | `ID_PREFIXES.option = 'opt'` added to `packages/config/src/ids.ts` (additive): `ProductOption.id` has no default and the SPEC §5 list has no entry for it, as with `cart`/`ful`/`ref`/`img` already there.
2026-08-28 | WS-B | `createVariantInput.inventoryQuantity` is ACCEPTED AND IGNORED by the products API: stock only moves through B4's adjustment service, so writing it here would create levels with no `InventoryAdjustment` history. Reads report the sum of existing levels. B4 owns wiring the field through.
2026-08-28 | WS-H | The seed's mock `ProcessorConfig` leaves `encryptedCredentials` null instead of sealing dummy creds (H1 step 8): the mock adapter takes no credentials, and `packages/db` cannot import `packages/pay` to seal a pair — pay already depends on db, and turbo rejects the resulting package cycle outright (verified). D3/D4 should treat "mock, enabled, no credentials" as the connected state for that processor.
2026-08-28 | WS-H | Seeded free shipping is a shipping *rate* with a $150 minimum, not a discount applied to the shipping line — that keeps `discountTotal` purely line-level, so `Σ lineItem.totalDiscount === order.discountTotal` holds exactly. The automatic free-shipping Discount row still exists for C6's index and E3's checkout; the seeded history just does not route through it.
2026-08-28 | WS-H | Seeded history ends at the end of yesterday (`OLDEST_HISTORY_DAY = 1`) rather than including today. Anchoring to whole UTC days makes the seed deterministic per date — clamping "today" rows against the current instant made content depend on the clock time the seed ran at — and it keeps today genuinely open, which is the split the dashboard's rollups-plus-today's-raw-events design assumes (SPEC §13).
2026-08-28 | WS-H | The seed derives every `InventoryLevel.available` from the sum of its `InventoryAdjustment` rows rather than writing levels directly, and sold-out variants are produced by negative `correction` adjustments. The level and its history are therefore incapable of disagreeing, which is what CLAUDE.md §9's "never a raw update" rule is protecting.
2026-08-28 | root | Every worktree gets its OWN Postgres database (`merchant_<worktree>`) and Redis db index via `pnpm worktree:env` — they previously all shared `merchant` on db 0. This is correctness, not tidiness: ws-g's `ALTER TABLE webhook_deliveries ADD COLUMN "eventId" TEXT NOT NULL` is correct on its branch and breaks every other worktree's Prisma client the moment it is applied, and `pnpm db:reset` (a documented command) drops the database out from under whoever is mid-test-run. Cost is ~10 MB of disk per worktree and zero RAM. Ports 3000/3001/3002 stay SHARED on purpose: the collision is the only thing stopping nine Next dev stacks from running at once on an 8 GB machine that already pages.
2026-08-28 | WSC | `createOrder` asserts `subtotal - discountTotal + shippingTotal + taxTotal === total` and that every amount is in the order's currency, then stores the totals verbatim — it records, it never prices (C1/E3 do). The assert turns an upstream pricing bug into a 400 at the moment it happens instead of an order that is a cent wrong forever.
2026-08-28 | WSC | Order numbers are claimed with `UPDATE order_sequences SET next = next + 1 RETURNING` inside the order transaction; the row itself is created before the transaction opens with INSERT … ON CONFLICT DO NOTHING, because a failed INSERT inside the transaction would roll the whole order back.
2026-08-28 | WSC | Index tabs, with no "archived" flag in the schema: `closed` = cancelled OR fully fulfilled, `open` = its complement, `unfulfilled`/`unpaid` additionally exclude cancelled orders. Cursor pages order by the sort key then `orderNumber` — never by `id`, because ULIDs minted in the same millisecond are not ordered and the page boundary would drift.
2026-08-28 | WSC | Order creation writes a `DiscountRedemption` per applied code as well as incrementing `usedCount` (the counter alone cannot answer "has THIS customer used it"), and uses `updateMany` so a discount deleted between pricing and payment cannot roll back a paid order.
2026-08-28 | WSC | Cancel restocks inside `services/orders/cancel.ts` rather than through B4's adjust service, which is not on main yet — the invariant it preserves is B4's own: never move an InventoryLevel without writing its InventoryAdjustment. Swap it for the adjust service when B4 lands.
2026-08-28 | WSC | Order lifecycle webhooks/emails go through `services/orders/notify.ts`, a seam that currently does nothing because G1's producer is not on main. When G1 lands, that one file forwards to `emitWebhookEvent` / `enqueueOrderConfirmationEmail`; it must never throw, since the order is already committed by then.
2026-08-28 | root | Root `.npmrc` sets `package-import-method=hardlink`. Measured on this machine: a worktree's node_modules shares 1 MB of 839 MB with the pnpm store under the default import method, and 753 MB of 753 MB when hardlinked — so each of ten worktrees was costing a real ~840 MB instead of ~0. The tradeoff is that node_modules files share inodes with the store, so editing one in place would change it for every worktree; patch through pnpm.patchedDependencies instead. `pnpm stack disk` reports which worktrees still hold their own copy.
2026-08-28 | WS-B | Quantities move with `available = available + delta` in SQL inside a transaction, never read-then-write. That statement holds the row lock until commit, so simultaneous decrements serialise — a read-modify-write loses 4 of 8 concurrent sales (measured; `test/inventory.test.ts` asserts the sum). Everything that changes stock imports `services/inventory/adjust.ts`; a bare `inventoryLevel.update` anywhere else is the CLAUDE.md §9 landmine.
2026-08-28 | WS-B | Inventory batches are ALL-OR-NOTHING and lock in a sorted key order. A fulfillment that cannot decrement its third line must not have decremented the first two, and two batches touching the same levels in opposite orders would otherwise deadlock.
2026-08-28 | WS-B | `InventoryLevel` rows are created LAZILY on first stock movement, not backfilled when a location is added — adding a location to a 30-product store would otherwise fan out a row per variant for stock that is not there. `GET /admin/api/inventory` reports 0 for a missing level, so the table never shows a hole.
2026-08-28 | WS-B | `adjust` refuses to take a `deny` variant below zero (409 `conflict`); a `continue` variant is allowed to go negative, because that is exactly what overselling is. `set` takes an absolute count, is `>= 0` by contract, and records the delta it had to apply — so `set` and `adjust` leave the same audit trail.
2026-08-28 | WS-B | Deleting the last location is a 409: a shop with nowhere to hold stock cannot fulfil. Deleting any other cascades its levels; the `InventoryAdjustment` history carries no foreign key and deliberately outlives what it describes.
2026-08-28 | WS-B | `actor` on an adjustment is stamped from the session by the route, never taken from the request body — the history drawer's "who" must not be client-supplied.
2026-08-28 | WS-B | Contracts additions (additive): `setInventoryInput`, `bulkSetInventoryInput`, `adjustInventoryBody`/`setInventoryBody` (both endpoints take one change or a batch), `inventoryLevelsResponse`, `inventoryAdjustmentSchema`, `InventoryRow`. New id prefix `inv_adj` for adjustments, alongside the existing `card_tok` precedent for a multi-segment prefix.
2026-08-28 | ROOT | GitHub does not apply `.gitattributes` merge drivers when computing PR mergeability — verified by merging the same two commits with and without `.gitattributes` present. Concurrent appends to `DECISIONS.md`/`docs/AGENT-LOG.md` therefore make PRs CONFLICTING on the server even though they rebase cleanly locally, and a conflicting PR gets NO `pr-checks` run at all (there is no merge commit for `pull_request` to run against), so it fails silently rather than red. Mitigated by `.github/workflows/pr-health.yml` (labels + comments every unmergeable PR) and `pnpm sync` (rebase + push); the optional `rebase` job in that workflow does it automatically once `AGENT_PAT` exists — it cannot use `GITHUB_TOKEN`, whose pushes do not trigger workflows.
2026-08-28 | root | No MCP database connector. Agents read the database through `pnpm db:query`, which runs psql inside the compose container with `default_transaction_read_only=on` — Postgres refuses the write, so the guarantee does not depend on an agent's judgement or a server's SQL parsing. An MCP server would add tool schemas to every one of ~20 agents' context windows to expose what `psql` already does.
2026-08-28 | root | `.claude/settings.json` and `.claude/launch.json` are committed, not local-only: the permission allowlist, the npm/yarn+force-push denials and the "no commit on main" hook are repo rules (CLAUDE.md §4, §9), and every worktree agent should inherit them. Personal overrides go in the gitignored `.claude/settings.local.json`.
2026-08-28 | WSF | The AI builder forces a single `set_theme` tool call whose input schema IS `themeDocSchema` (via zod-to-json-schema, `$refStrategy: 'none'`) — so the section catalog and every `.describe()` in contracts reaches the model as prompt text, and the schema cannot drift from what the renderer accepts. Refs are inlined because tool-use handles a flat schema far better than an internally-referenced one.
2026-08-28 | WSF | Generation validates against `themeDocSchema` + `validateThemeDoc` AND against the shop's real product/collection handles; a handle the shop does not have is a validation failure, not a render-time empty block. One retry with the errors fed back, then an apologetic chat message — the job never throws, because a thrown job retries on the queue and the merchant watches a spinner forever.
2026-08-28 | WSF | Preview tokens are HMAC-SHA256 over `{shopId, versionId, expiry}` with SESSION_SECRET, 15-minute TTL, compared in constant time — an unpublished theme must not be reachable on the public storefront by guessing an id.
2026-08-28 | WSF | `installInitialTheme` publishes the aurora preset on signup and deliberately never throws: a store that opens on a blank page is bad, a signup that 500s because the theme table hiccuped is worse. WS-A's signup handler calls it in two lines.
2026-08-28 | WS-A | Polaris AppProvider gets a `linkComponent` that renders Next's `<Link>` (apps/admin/src/components/shell/polaris-link.tsx) — Polaris emits a plain `<a href>` for every `url` prop, so without it each left-nav click was a full document load with a white flash. One provider-level hook makes every link in every workstream's pages a client-side navigation; do NOT also call router.push in an onClick, that navigates twice.
2026-08-28 | WS-A | The admin's API base URL is republished as NEXT_PUBLIC_API_URL by apps/admin/next.config.ts from API_URL, rather than added to packages/config/env.ts — env.ts is server-only (it parses DATABASE_URL and VAULT_MASTER_KEY) and the parity check forbids a .env.example key with no schema entry.
2026-08-28 | WS-A | Nav selection is computed in apps/admin/src/lib/nav.ts and passed to Polaris explicitly instead of relying on Polaris's URL matching — Products has to stay selected on /collections and /inventory, which are sibling URLs rather than children of /products.
2026-08-28 | WS-A | apiFetch turns an unreachable API into ApiError(0, 'internal', 'Could not reach the server…') — callers branch on ApiError, and a raw "Failed to fetch" in a Polaris banner tells a merchant nothing.
2026-08-28 | WS-A | PARITY.md's global-search line corrected: the results panel anchors under the search FIELD at roughly its width (current Shopify + Polaris TopBar.searchResults), not full-bar-width as originally written.
2026-08-28 | WS-E | The storefront reads collection membership from the `CollectionProduct` join table for smart collections as well as manual ones — it never evaluates a rule set. Shopify materializes smart membership the same way, and it keeps the collection page one indexed join instead of a rule interpreter on the hot path. That makes it B3's job to keep the join rows in step with a smart collection's rules; `collectionProductIds` in `services/storefront/collections.ts` is the single place to change if B3 ever moves to evaluating at query time.
2026-08-28 | WS-E | `price-asc|price-desc|best-selling` are ranked in application code (`rankedIds` in `services/storefront/products.ts`), not by an `orderBy`: price lives on the variant table and best-selling on order line items, and Prisma can express neither as a product-level sort. The whole matching set is ranked and then paged, which is the right trade while a live catalogue is tens to low hundreds of products (SPEC §2 has no bulk catalogue). The cursor stays a product id, so callers cannot tell the two paging strategies apart.
2026-08-28 | WS-E | The theme preview parameter is `<themeVersionId>.<hmac-sha256 over SESSION_SECRET>`, minted by `signThemePreview()` in `services/storefront/theme.ts`. A raw `?preview=thm_…` would let anyone enumerate a shop's unpublished drafts. An unsigned or tampered token falls back to the published theme rather than erroring, so a stale builder link shows the live store instead of a broken one. F3/F4 build preview links with that helper.
2026-08-28 | WS-E | Storefront cart path is singular `/storefront/api/cart`, not `/carts`. CLAUDE.md §5's plural rule is about collection resources; this is the singleton belonging to one browser, and Shopify's own path is `/cart`.
2026-08-28 | WS-E | Cart lines persist `variantId` + `quantity` only; title, image, price and availability are resolved from the live variant on every read, and a line whose product was deleted or unpublished is dropped rather than rendered. Snapshotting belongs at checkout creation (E3), where the shopper commits to a number.
2026-08-28 | WS-B | Smart-collection membership is resolved ON READ as a Prisma `where` (`smartCollectionWhere` in `apps/api/src/services/catalog/collections.ts`), never materialized: no republishing job, nothing to go stale, and a 24-product page costs one indexed query. `productCount` is likewise derived — a join count for manual, a `COUNT(*)` over the same clause for smart.
2026-08-28 | WS-B | A (column, relation) pair the translator cannot express is `invalid_request`, never a silently dropped clause — a collection that quietly means something else is worse than one that refuses to save. Refused: any text relation on `tag` other than equals/not_equals (Postgres has no substring operator over `text[]`), any text relation on `price`/`inventory_quantity`, and any ordering relation on a text column. A non-integer `price` condition (`"20.00"`) is refused too: conditions are integer MINOR UNITS, so `2000` is $20.00.
2026-08-28 | WS-B | `inventory_quantity` rules compare stock at a SINGLE location, not the sum across locations — an aggregate over a relation is not expressible as a Prisma filter, and per-location is the number the merchant reads in the inventory table. Divergence only shows on a variant stocked in two places.
2026-08-28 | WS-B | A smart collection must carry at least one condition and a manual one may carry none: an empty rule set would publish the whole catalog under whatever title the merchant typed. Converting a collection to smart DELETES its hand-picked members rather than leaving a list that would reappear on converting back. `sortOrder: 'manual'` on a smart collection is stored as `created-desc` — there are no positions to honour.
2026-08-28 | WS-B | `GET /admin/api/collections/:id/products` pages by an OPAQUE OFFSET cursor, not the id cursor the products index uses. SPEC §5 fixes the `?cursor=` shape, not its contents, and the sort key here can be a related aggregate (cheapest variant, units sold) that no single row id anchors. `best-selling` is the one order that materializes the collection's id list — a product with no sales is absent from the sales aggregate, so it cannot be paged from it; ids only, no product rows.
2026-08-28 | WS-B | Uploads are presigned PUTs straight to MinIO — the API signs a URL and never touches the bytes (CLAUDE.md §9). Key layout is `shops/{shopId}/img_{ulid}-{safe-name}`: the shopId prefix is what makes one shared bucket multi-tenant, and the id means re-uploading `hero.png` never overwrites the image a live product already points at. The filename is sanitised to a single path segment before it reaches the key — `test/files.test.ts` asserts `../../other-shop/x.png` cannot climb out.
2026-08-28 | WS-B | The presign signs ContentType but NOT ContentLength, so the size cap is enforced at the API boundary rather than in the signature. Signing the length makes an upload fail on a one-byte mismatch, which costs the demo more than the cap buys on a local bucket (SPEC §15 — baseline only, don't over-invest).
2026-08-28 | WS-B | B2 follows `packages/contracts/src/files.ts`, not the prose in `docs/issues/B2`: the field is `contentType` (not `mimeType`), the cap is 20 MB (not 10), and `image/svg+xml` is allowed. The contract is the integration surface other workstreams code against, and narrowing it would have been a breaking change for no gain. SVG is served from the MinIO origin (:9000), which holds no session — a script-bearing SVG cannot reach an admin or storefront cookie.
2026-08-28 | WS-B | No docker-compose change was needed for CORS: MinIO already answers the browser preflight (`OPTIONS` → 204 with `Access-Control-Allow-Origin`/`-Methods: PUT`) for any origin, verified against the running container. The B2 issue anticipated extending the `mc` init script; it does not need extending.
2026-08-28 | WS-B | `/admin/api/files/presign` is gated on `requirePermission('products')` and is deliberately product-agnostic — F3 (theme assets) and A4 (shop logo) use the same endpoint and key layout. A dedicated `files` permission area would be a ninth checkbox on A4's staff form for no real separation.
2026-08-28 | WS-A | dbForShop throws on any operation outside WHERE_OPS/DATA_OPS and on any model without a shopId column, instead of passing the query through unscoped — the failure mode of a Prisma upgrade adding an operation (or someone adding a platform-level table) must be a loud error, not silent cross-tenant reads.
2026-08-28 | WS-B | The product form previews the variant matrix CLIENT-side (`apps/admin/src/lib/product-draft.ts`), duplicating the expansion rule the API applies server-side. Deliberate: Shopify's option builder shows the rows before you save, and the alternative is a round trip per keystroke. The duplication is pinned by `product-draft.test.ts`, which asserts the same first-option-slowest ordering the API's own suite asserts — if the two drift, the table silently reshuffles after a save.
2026-08-28 | WS-B | Prices live in form state as STRINGS ("19.99", mid-edit "19.") and become integer minor units exactly once, in `draftToInput` via `fromDecimal`. No float is ever constructed. An empty price saves as 0 rather than blocking the save — a free sample is a real product.
2026-08-28 | WS-B | Stock is written in a SECOND request, to `/admin/api/inventory/set`, after the product save — never in the product payload, which ignores `inventoryQuantity` by design (B1/B4). The quantity field is editable only when the shop has exactly ONE location; with several, the form would have to pick one silently, so it shows the total read-only and points at the Inventory page.
2026-08-28 | WS-B | The products index and the product form hand-build their empty state from Polaris primitives instead of using `EmptyState`, which requires an `image`. The only on-brand illustrations are Shopify's own CDN assets and PARITY.md forbids rendering those; A3 hit the same wall in `ComingOnline`.
2026-08-28 | WS-B | Media reordering is ← / → buttons, not drag-and-drop: the stack has no DnD library and adding one is a stack substitution (SPEC §3). Upload IS drag-and-drop, via Polaris `DropZone`. 20-minute escape hatch, CLAUDE.md §7.
2026-08-28 | WS-B | Polaris `TextField` exposes no `onKeyDown`, so the option-value and tag inputs catch Enter on a wrapping div (the event bubbles out of the real input). Not a custom control — the field is still Polaris.
2026-08-28 | WS-B | The products index footer shows prev/next arrows without "of N": the list API is cursor-based (SPEC §5) and returns no total, and a COUNT on every page load buys only the number. PARITY.md's "1-50 of N" is unmet here on purpose — revisit if a total is ever added to `paginated()`.
2026-08-28 | WS-B | The product form has NO Collections field, though PARITY.md lists one under Product organization: `createProductInput` carries no collection ids, so the control would not save. A cut feature's button is not rendered (CLAUDE.md §8). B3/B6 own wiring collection membership.
2026-08-28 | WS-B | The product form's Description unwraps SIMPLE html (`<p>`/`<br>` only) into plain text for editing and re-wraps it on save; anything richer is shown as raw HTML instead. A rich-text editor is out of scope (B5), but leaving `<p>` tags visible on every seeded product is a parity tell, and flattening real formatting would destroy a merchant's markup on an unrelated edit. The round trip is asserted byte-identical.
2026-08-28 | WS-G | Queue producer lives in `packages/config/src/queue.ts` and imports bullmq/ioredis lazily — importing it must not open a Redis socket, and bullmq ships no `exports` map, so Vite picks its bundler-only ESM build and vitest cannot load it statically.
2026-08-28 | WS-G | Job payload SCHEMAS live in `packages/contracts/src/jobs.ts`, but `queue.ts` cannot import them (contracts already depends on config) — the worker parses every payload with the contract schema and a test asserts the producer's output still satisfies it.
2026-08-28 | WS-G | `emitWebhookEvent`/`enqueueOrderConfirmationEmail` never throw; they return whether the job was queued. A Redis blink must not fail an order save — the worker owns delivery reliability, the producer owns not taking checkout down with it.
2026-08-28 | WS-G | Job payloads carry ids, not snapshots; handlers re-read through `dbForShop`. A retried confirmation email then cannot mail stale totals.
2026-08-28 | WS-G | `WebhookDelivery.eventId` added with `@@unique([subscriptionId, eventId])` — BullMQ retries the whole job, so without it five attempts became five rows, and endpoints that already succeeded got re-POSTed on every retry.
2026-08-28 | WS-G | Order confirmation email carries `orderStatusUrl` in the job payload (E3 holds the checkout token) — omitting the button beats shipping a guessed thank-you URL that 404s in front of a customer.
2026-08-28 | WS-E | CORRECTION to the theme-preview line above: F3 landed `services/themes/preview-token.ts` concurrently, and its token is strictly better than the one E1 shipped — bound to shop AND version, expiring after 15 minutes, constant-time compared before parsing. E1 has deleted `signThemePreview`/`verifyThemePreview` and the storefront now verifies F3's token via `verifyPreviewToken`. One signing scheme, minted by `GET /admin/api/themes/preview-token`, verified by `GET /storefront/api/theme?preview=`. Two independent schemes would have meant the builder preview silently falling back to the published theme.
2026-08-28 | WS-E | CORRECTION to the smart-collection line above: B3 landed resolving smart membership ON READ as a Prisma `where`, materializing nothing. The storefront now calls B3's exported `smartCollectionWhere` instead of reading `CollectionProduct`, so admin and storefront cannot disagree about what is in a collection — reading join rows meant every smart collection a merchant created rendered an empty storefront page. `productCount` is derived from the same clause, filtered to active products. The seed no longer writes join rows for its smart collection; they were dead data that read as if membership were stored.
2026-08-28 | root | Biome noRestrictedImports now bans the `dbAdmin` named import outside the sanctioned files (allowlisted in biome.json overrides) and bans `@prisma/client` outside packages/db — the tenancy boundary (CLAUDE.md §6) is CI-enforced instead of review-enforced; sabotage-verified that both bans fire. Type imports from @merchant/db/client stay legal everywhere.
2026-08-28 | WS-G | `orders/*` webhook bodies carry `total` as a Money object, not the bare integer the internal row holds — a merchant reading `total` off the wire must not have to know our minor-unit convention to charge the right amount.
2026-08-28 | WS-G | `orders/paid` is emitted from D3's Pay-router `onPaid` seam and skipped when the charge has no `orderId`: a bare saved-card charge is a payment, not an order payment, and there is no `payments/*` topic to invent without breaking `webhookTopicSchema`.
2026-08-28 | WSF | The builder's chat log and preview toolbar are hand-built JSX styled with `--p-*` tokens rather than Polaris components — the 20-minute escape hatch (CLAUDE.md §7). Polaris ships no chat transcript or device-frame primitive, and faking one out of `ResourceList` reads as a settings page, not a builder. The page frame, modals, buttons and fields stay Polaris so it is still native to the admin.
2026-08-28 | WSF | The preview iframe targets the real storefront origin (`{slug}.lvh.me:3002`) with a signed `?preview=` token, never a proxy or a server-rendered mock — same renderer, cookies and cache as a shopper, so the preview cannot drift from what ships.
2026-08-28 | WSF | The preset picker is always visible in the chat panel, not only when `ANTHROPIC_API_KEY` is missing. The admin cannot know whether a key exists until it sends a message, and presets are the fastest way to restyle a store regardless — so the no-key path needs no detection and the builder never dead-ends (CLAUDE.md §9).
2026-08-28 | WS-G | Analytics is ONE endpoint (`GET /admin/api/analytics`) returning the whole `analyticsDashboardResponse`, not the four the G2 issue text lists — the contract already describes a single combined response and G3 renders every card at once, so four endpoints would mean four loading states on one screen for no gain. `/live` stays separate because it alone is polled.
2026-08-28 | WS-G | Rollup metric names follow `packages/db/prisma/seed/analytics.ts` (seven metrics incl. `begin_checkouts`), not the G2 issue text (which says `revenue`/`page_views`). The seed writes the closed days and the worker writes the open ones; a different vocabulary would step the chart exactly where history meets live data. Verified: the job reproduces the seed's numbers for a seeded day on all seven.
2026-08-28 | WS-G | `POST /storefront/api/events` DROPS browser-sent `purchase` events. The endpoint is unauthenticated and Host-resolved, so anyone who can curl it could otherwise mint revenue into a merchant's dashboard; the contract already called the server-side one authoritative. Dropped rather than rejected — `purchase` is a legal member of the event enum and a beacon must not fail on a legal payload.
2026-08-28 | WS-G | A date range is measured in DAYS, not elapsed milliseconds, when sizing the comparison window: a range covering the 27th and 28th spans two days but only 24h of clock, so millisecond arithmetic compares it against one day and every delta chip on the dashboard is wrong.
2026-08-28 | WSC | A partial refund of a multi-unit line splits the line's net (price x qty less its allocated `totalDiscount`) across units with `allocate`, and refunds units `[refundedQuantity, +n)` — so successive partial refunds of the same line always sum to exactly one whole refund. Naive `net/quantity` rounding drifts and is caught by the C3 suite.
2026-08-28 | WSC | `Refund.shippingAmount` (new column, default 0) records the part of a refund that came off shipping — without it a second refund cannot tell how much shipping is left, and shipping can be refunded twice while the order total still has room.
2026-08-28 | WSC | Fulfillment takes stock through B4's `adjustMany` BEFORE writing the Fulfillment row, and cancel/refund restock the same way: the adjustment service runs its own transaction (row locks are what make concurrent decrements safe), so it cannot be nested inside the order transaction. Ordering is deliberate — the stock step is the one that can legitimately refuse, and refusing before a Fulfillment exists keeps the two consistent.
2026-08-28 | WSC | `apps/api/test` cannot drive a processor-backed refund over HTTP: `@fastify/autoload` imports route files with a plain dynamic import, so under vitest the route tree and the test file hold separate instances of `@merchant/pay` and therefore separate mock-processor ledgers. Refunds that reach the processor are tested against the service; everything else stays over HTTP.
2026-08-28 | WS-E | A declined card returns HTTP 200 with `{status:'failed', message, code}` rather than an error status. E3's text asks for "a 402-style SPEC error", but SPEC §5 fixes the error-code set and none of them means "the bank said no" — and `completeCheckoutResponse` already models the decline as a response variant. The contract is the integration contract, so it wins; E4 renders the message inline and the checkout stays open.
2026-08-28 | WS-E | Completion claims the checkout BEFORE charging: one `updateMany` flips `open → completed`, and only the winner proceeds. That serializes the whole flow on a row the database already locks, which is what makes a double-clicked Pay button produce one order. Every failure before the charge reopens it; once the card is charged it stays closed even if order recording then fails — a merchant finishing one order by hand beats a second charge.
2026-08-28 | WS-E | Stock is reserved BEFORE the charge, through B4's `adjustMany` (reason `sold`), and released with a `restock` if the charge fails. Taking a shopper's money and then discovering the last unit is gone is worse than telling them it is gone. Only the successful attempt's adjustments are re-pointed at the order — a declined attempt's reserve/release pair is real history, but it is the checkout's, not the order's.
2026-08-28 | WS-E | The payment idempotency key is `${checkoutToken}-${clientIdempotencyKey}`, not the checkout token alone. Keyed on the token alone, D3 returns the existing FAILED payment on a retry and a shopper could never pay after one decline; keyed per attempt, a double-submit of one click still charges once.
2026-08-28 | WS-E | Shipping rates are filtered on the DISCOUNTED subtotal, and a selected rate that stops qualifying is dropped rather than silently kept — so a coupon can price a shopper out of free shipping, which is what Shopify does and what a merchant means by "free over $150". Tax is the flat shop rate applied ONCE to (subtotal − discountTotal); shipping is not taxed.
2026-08-28 | WS-E | E3 maintains `Customer.ordersCount` / `totalSpent` on a completed checkout, and creates the customer by case-folded email. C4 has not landed and C2 records orders without touching the counters, so without this a shopper who just bought reads "0 orders" in the admin. C4 should take both over.
2026-08-28 | WS-B | B6's pages live at `store/[slug]/{collections,inventory}` and `settings/locations`, NOT the `products/collections`, `products/inventory` paths the B6 issue lists. The nav registry (WS-A, structure fixed) already links to the top-level urls and A3's nav test asserts them; the registry wins over the issue's prose.
2026-08-28 | WS-B | Added `POST /admin/api/collections/preview` (+ `previewCollectionInput`): the products an UNSAVED rule set matches, through the same translator a saved smart collection uses. The condition builder needs it — the alternative is re-implementing the rule translator in the browser, and the two would drift the first time a relation changed. Nothing is written; the response's cursor is always null because a preview is a peek, not a page.
2026-08-28 | WS-B | `Location.stockedVariantCount` (additive, read-only) counts variants holding a non-zero quantity there, from one grouped query for the whole list. It is what lets the Locations page grey out Delete without paging the entire inventory.
2026-08-28 | WS-B | Deleting a location that still holds stock is now a 409, not just a greyed button: levels cascade with the location, so deleting one would erase that stock from the shop's totals with no adjustment behind it. The UI mirrors the rule in a tooltip, but the rule lives in the API. (This changed B4's `deleteLocation`, which previously only refused the last location.)
2026-08-28 | WS-B | The smart-collection builder only ever offers (column, relation) pairs the API accepts — `tag contains` is refused server-side, so offering it would hand the merchant a 400 with no explanation. `relationsFor` mirrors the table documented on `collectionRuleSchema` and `collection-rules.test.ts` pins the two together.
2026-08-28 | WS-B | A price CONDITION is integer minor units on the wire like every other Money (SPEC §5): the field shows "20.00" and the rule carries "2000". Converting the wrong way silently builds a collection for products under $0.20.
2026-08-28 | WS-B | Inventory cells write through `POST /admin/api/inventory/set` — the absolute half of B4's service, which records the delta it applied. A cell typed into and restored produces NO adjustment, and an empty or half-typed cell is never sent: every write here is a permanent history row (`inventory-edits.test.ts`).
2026-08-28 | WS-B | The description unwrapping from B5 moved to `apps/admin/src/lib/description-html.ts` and now serves the collection form too, so `<p>` tags stop leaking into either editor. `product-draft.ts` re-exports it, so its callers are unchanged.
2026-08-28 | WS-H | Product/collection/preset imagery is pinned, hand-curated Unsplash photography (seed/data/images.ts) instead of random picsum — the KPI needs "Alpine Merino Crewneck" to show knitwear, and a pinned photo id is exactly as deterministic as a seeded placeholder. Every id verified reachable and on-subject on a contact sheet; primaries unique per product; seed.test.ts fails if a product ships the placeholder fallback. Still an external image host (as picsum was) — self-hosting stays open as a hardening step, and ThemeImage's layout-stable fallback covers offline demos.
2026-08-28 | WSD | `SimulatedProcessor` adopts an unknown transaction on REFUND (only), instead of failing it: the seed writes captured Payment rows carrying processorTxnIds the running process never issued, so the admin's Refund button failed on every seeded order. The Payment row is the authority on the amount and `refundPayment` caps against it before an adapter is reached, so the ledger has nothing useful to add for a txn it never saw. `capture`/`voidAuth` stay strict — they act on an authorization this process should have made, and the seed writes none.
2026-08-28 | WSD | The mock/maverick ledgers are keyed on `globalThis` via `Symbol.for` rather than module scope, for the same reason ApiError is: autoload imports route files outside Vite's transform, so under vitest the route tree and a test file hold two copies of the module — and with two ledgers a charge made in a test is an unknown transaction to the route. This is what lets apps/api test a processor-backed refund over HTTP.
2026-08-28 | WSC | `ordersCount`/`totalSpent` are DERIVED per request from a single `groupBy(customerId, financialStatus)` over non-cancelled orders, and the denormalised columns on `Customer` are deliberately left unused: a counter written from three places (order placed, refunded, cancelled) drifts, and the number it drifts into is the one next to the customer's name. Amount spent counts only paid/partially_refunded/refunded orders, net of `refundedTotal`; an unpaid order counts toward the order count but not toward money spent.
2026-08-28 | WSC | Customer email is stored case-folded and matched case-folded. Postgres unique indexes are case-sensitive, so without it `Ada@shop.com` and `ada@shop.com` are two customers and a completing checkout attaches its order to whichever it happens to find first.
2026-08-28 | WSC | Customer segments resolve to an id/email set with one aggregate query, then filter the index on it — `returning` = more than one live order, `new` = FIRST live order inside 30 days (not "signed up recently"), `abandoned-checkout` = an open Checkout younger than 72h with no completed order, joined by email because `Checkout` carries no customerId. A raw correlated subquery would be one query instead of two but would bypass the tenant client.
2026-08-28 | WSC | A customer always has exactly one default address: if the caller marks several the first wins, if it marks none the first address becomes the default. The storefront picks the default with no tiebreak, so two defaults render differently on every request.
2026-08-28 | WS-E | The storefront's API client puts the shop slug in the URL hostname (`{slug}.lvh.me:3001`) rather than sending a `host` header. Node's fetch drops `host` exactly as a browser does — undici treats it as forbidden — so a header-based client reaches the API as `localhost`, resolves no tenant, and 404s every page while looking like a data problem. Verified, not assumed; `api.test.ts` pins it.
2026-08-28 | WS-E | `?preview=` is carried from the URL into a request header by `src/middleware.ts`, because Next gives pages their search params but NOT layouts — and the layout is where the theme's colours and fonts are applied as CSS variables. Without it a previewed draft renders its own sections wearing the PUBLISHED palette, which is precisely what F4's builder preview exists to show.
2026-08-28 | WS-E | Storefront redirects are built from the request's `Host` header, never from a route handler's `request.url`: that carries the server's own origin (`localhost` in dev, a container name behind a proxy), so `/checkout` redirected to `localhost:3402` and dropped the shop subdomain that resolves the tenant. `absoluteUrl()` in `lib/api.ts` is the one place that builds them.
2026-08-28 | WS-E | Cart mutations are Server Actions, not client fetches: the cart cookie is httpOnly so only the server can read it, only an action or route handler may set the one E1 issues to a first-time shopper, and a browser fetch to `api.lvh.me` would be cross-origin and never send it at all.
2026-08-28 | WS-E | Storefront images stay on F1's `ThemeImage` (`<img loading="lazy">`) rather than `next/image`, against E2's perf-budget wording. F1 documented the reason in the component: theme image URLs point at arbitrary remote hosts, which `next/image` would need a per-shop `remotePatterns` allowlist to serve. Swapping it would mean reworking a shared WS-F component for a marginal gain; the TTFB budget is met without it.
2026-08-28 | WS-A | Shipping rates stay in the `shops.shippingRates` JSON column instead of getting a table (A4 guessed they would need one). A shop has a handful, they are always read together, and every read already holds the Shop row — so no migration, and nine other agents avoid a schema conflict. Writes go through an interactive transaction so two admins saving at once cannot drop one another's rate; `apps/api/test/settings.test.ts` asserts the tx client stays shop-scoped.
2026-08-28 | WS-A | The API stamps the shop's own currency onto every shipping-rate amount rather than trusting the client. A rate stored in another currency makes `compare()` throw, which takes down the checkout shipping step for EVERY cart, not just orders that pick that rate.
2026-08-28 | WS-A | `upsertShippingRateInput` uses `positiveMoneySchema` and requires min <= max. A negative rate would subtract from the order total; min above max is a rate that can never be selected and is invisible to the merchant.
2026-08-28 | WS-A | A shop has exactly one owner: the owner cannot be demoted or deleted, and no one else can be promoted to owner. Ownership transfer is out of scope, and a second owner is a state nothing else handles.
2026-08-28 | WS-A | `destroySessionsForUser(staffUserId)` added to apps/api/src/lib/sessions.ts, backed by a Redis `sess:user:<id>` reverse index. Sessions snapshot role+permissions at login (A1), so editing or removing a staff member is only enforced once their sessions are gone. Known bound: the index TTL is twice the session TTL and is refreshed only at login, so a session slid past that window would survive revocation.
2026-08-28 | WS-G | Charts use **Recharts** styled with Polaris `--p-*` tokens, not `@shopify/polaris-viz` (SPEC §3 sanctioned fallback, G3's 20-minute escape hatch). polaris-viz 16 declares react `^16 || ^17 || ^18`, installs only with peer overrides, and at runtime mounted a correctly-sized ChartContainer that rendered NO svg inside it, with no console error to diagnose. It also cost 133s to compile the page versus 37s for Recharts. Recharts 3.10 needs no peer override on React 19.
2026-08-28 | WS-G | Home's setup guide is derived from REAL state (product count, published theme version, enabled processor, order count) rather than the `Shop.onboarding` JSON blob — a checklist that disagrees with the store is worse than no checklist. Each check is its own query, so one the viewer lacks permission for degrades to "not done" instead of blanking the card. Note: signup publishes the aurora preset, so a brand-new shop legitimately starts at 1 of 4.
2026-08-28 | WSC | Discount codes are stored and matched UPPERCASE, so `(shopId, code)` enforces the case-insensitive uniqueness the C1 engine already assumes when it lowercases both sides at checkout. Storing `WELCOME10` and `welcome10` as two rows would make which one applies depend on row order.
2026-08-28 | WSC | Discount `status` is derived from the date window at read time and the stored column only vetoes via `disabled` — the same rule as the engine's `rejectionReason`, so the index badge and checkout can never disagree. No cron flips rows. The index's `?status=` therefore filters after the page is read, which can return a short page; a date-range WHERE per status would be a second implementation of the rule and is exactly how the two drift apart.
2026-08-28 | WSC | No read-then-write guard before inserting a discount code: the unique index is the check, and a pre-check is both an extra query and a race two concurrent creates can pass. Found by mutation testing — removing the guard changed no behaviour, so it was removed rather than covered by a new test.
2026-08-28 | WS-E | The storefront never calls `crypto.randomUUID()` — it is gated to secure contexts, and the documented dev storefront is `http://{slug}.lvh.me:3002`, plain HTTP. There the function is simply undefined, so "Pay now" threw before it ever reached the vault, while working perfectly on `localhost` (browsers treat it as secure) and behind TLS. `randomId()` in `lib/random-id.ts` uses `crypto.getRandomValues`, which has no such restriction; verified undefined-vs-working on the real origin.
2026-08-28 | WS-E | Checkout opts out of the storefront's theme chrome rather than nesting inside it: the root layout reads the path from a middleware header and renders a bare white shell for `/checkouts/**`. PARITY.md specifies a clean white page with the shop name as a logotype and no navigation, and Shopify's checkout is deliberately not the shop's theme.
2026-08-28 | WS-E | Card display logic (grouping, brand, `MM / YY` parsing) is a small local module rather than an import from `@merchant/pay`: that package reaches into Prisma and the vault key, none of which belongs in a browser bundle. Being wrong there is cosmetic — the vault validates authoritatively — but the `MM / YY` → `expYear` expansion is not, so it is unit-tested.
2026-08-28 | WS-E | The thank-you page reads `completedOrderNumber` off the checkout (additive to `checkoutSchema`, one extra query and only for a completed checkout) rather than the complete response. It has to survive a refresh, and the response is long gone by then.
2026-08-28 | WS-E | No `purchase` beacon is fired from the checkout, against E4's wording: WS-G's landed decision drops browser-sent purchase events as forgeable revenue and writes the real one server-side in `createOrder`. Only `begin_checkout` is emitted, on first load of the checkout.
2026-08-28 | WS-A | Custom-domain tenancy (A5) resolves in the existing Host path, not a new endpoint: `resolveFromHost` falls back to the CustomDomain table when the Host is outside `{slug}.{base}`, and CORS admits registered domains via the same cached lookup (`apps/api/src/lib/custom-domains.ts`) — without the CORS half, the beacon and the checkout's /vault/tokenize POST die on exactly those shops. `verifiedAt` is NOT required to resolve: no verification flow exists anywhere, so requiring it would make every registrable row dead weight.
2026-08-28 | WS-A | The storefront's CustomDomain fallback (A5, in WS-E's `apps/storefront/src/lib/tenant.ts` as the issue directs) asks the API rather than duplicating the table lookup: `GET /storefront/api/shop` on the custom hostname now resolves via the tenancy fallback and carries the slug, cached by Next's data cache. One resolver, two consumers.
2026-08-28 | WS-A | Prod compose (A5): browser and server-side traffic share one route through Caddy — SSR storefront→api travels as `https://{slug}.{BASE_DOMAIN}` (the Host header IS the tenant, so it cannot be swapped for an internal hostname) with Caddy's internal CA trusted via NODE_EXTRA_CA_CERTS and compose network aliases standing in for DNS on BASE_DOMAIN=localhost. `/storefront/api/*` and `/vault/*` are peeled off to api on every storefront host so the beacon and card fields work on 443.
2026-08-28 | WS-A | Prod compose (A5) seeds only an empty database (`prisma/seed/seed-if-empty.ts`): `seedDemo()`'s idempotence means "wipe the demo shop and rebuild", which a stack restart must not do. GHCR images are pushed by main-checks with the DEV public URLs baked into admin (NEXT_PUBLIC_* is build-time); a real domain rebuilds admin through the compose build args — documented in the compose header for H3's README section.
2026-08-28T15:52Z | WS-E | E5: account auth actions return {ok} and the form does a full window.location navigation on success, instead of redirect() inside the server action — an action-embedded refresh re-renders the root layout against its cached shop/theme fetches and dies with Next E192 (notFound in root layout) in dev. Full navigation after an auth change is also what Shopify's own customer login does. Account pages read only resolveShopSlug(), never shopContext() — the layout already resolved the theme; a second fetch buys nothing.
2026-08-28 | WS-G | `emitWebhookEvent` now returns `Promise<string | null>` — the event id it queued, or null if it could not queue — instead of `Promise<boolean>`. G4's "send test event" has to tell the merchant WHICH delivery its click produced, and a bare boolean cannot. Checked every call site first: all three either ignore the result or use it as a truthiness check, and a non-empty id is truthy where the boolean was, so the change is behaviour-preserving.
2026-08-28 | WS-G | Webhook subscription rows on the app detail page are hand-built from `Box` + border tokens rather than `ResourceList`/`ResourceItem` (CLAUDE.md §7 escape hatch). `ResourceItem` requires a row-level `url` or `onClick` — tsc rejects omitting it — and a subscription row has nowhere to navigate; a row that highlights on hover and then does nothing reads as broken. Every action on the row is a button. Empty states follow the existing `EmptyState`-needs-an-image precedent already logged by WS-A and WS-B.
2026-08-28 | WS-G | Admin API scopes follow Shopify's `write_x` implies `read_x`: granting write and then having reads refused is a half-state no merchant expects. Enforced in `apps/api/src/lib/scopes.ts` and mirrored by the UI's scope grid, which adds read when write is checked and drops write when read is unchecked.
2026-08-28 | WS-B | `upsertVariantInput` (PUT /products variants) is `.partial()` WITHOUT create-side defaults, and the service overlays only the DEFINED fields of a mentioned variant onto the existing row — the inherited defaults made an omitted `barcode` parse as explicit `null` and omitted `inventoryPolicy` as `'deny'`, so the admin form (which sends only id/price/sku/optionValues) wiped compareAtPrice/barcode/weight/policy on every save. Loosening: accepts strictly more payloads; only B's own route/service consume the schema.
2026-08-28 | WS-B | Option RENAME preserves variant identity by positional re-key, client and server: when the option count is unchanged, existing rows' optionValues are re-keyed old-name→new-name per position before signature matching. Without it a rename orphaned every row (the form drops ids when regenerating its table) and the PUT deleted+recreated all variants, destroying skus and B4's inventory levels.
2026-08-28 | WS-D | `ProcessorConfig.connected` means "this processor will accept charges", not "credentials are stored": mock and credential-less (simulated) maverick are connected the moment they exist; only stripe requires a stored key. Found in the browser — deriving connected purely from the credential blob showed a freshly connected simulated Maverick as a red Error badge. Pinned by a payments.test.ts assertion.
2026-08-28 | WS-D | `packages/db/prisma/seed/pay.ts` seals the demo saved cards' vault blobs by duplicating pay's AES-256-GCM shape inline (JSON {number,cvc}; ciphertext/iv/authTag base64 under VAULT_MASTER_KEY) — db cannot import pay (package cycle, see the WS-H line above). Safe because SPEC §11 freezes the scheme (one key, no envelope, no rotation); verified by charging a seeded card end-to-end through vault.getCard + the mock adapter.
2026-08-28T23:55Z | WS-H | Refunds now return the refunded units' tax: `calculateRefund` allocates `order.taxTotal` across taxable lines by net, then across units (largest remainder, exact-sum), because tax is stored per order, not per line. Previously items+shipping refunds stranded the tax, so a fully-returned taxed order could never reach `refunded` — the UI even advertised the full order total as "available to refund". Shopify refunds tax with items; the refund summary gained a Tax row and `refundCalculationSchema` a `taxAmount` field (additive).
2026-08-28T20:20Z | WS-H | Admin page titles come from a `layout.tsx` per section directory (12 of them) plus a `title` template on `store/[slug]/layout.tsx`, NOT from a client effect. All 37 admin pages are `'use client'` and cannot export `metadata`; the effect version (`useDocumentTitle`, deleted) lost every race with Next's own metadata pass and left the tab reading a bare "Merchant" on hard loads AND client navigations — verified in a browser with a healthy API and a live session. Rendering a `<title>` from the shell (React 19 hoisting) worked but emitted two `<title>` tags. Note `store/[slug]` must repeat `template: '%s · Merchant'`: a segment whose `title` is a plain string clears the inherited template for everything below it, and its `default` is still run through the ROOT template, so it is `'Home'` not `'Home · Merchant'`.
2026-08-28T20:20Z | WS-H | `store/[slug]/layout.tsx` is now a SERVER component (was `'use client'` for `useParams`); a server layout receives `params` directly and can carry `metadata`. `AdminFrame` below it is unchanged and still a client component.
2026-08-28T20:20Z | WS-H | Orders' `Drafts` nav subitem and `orders/drafts/` are removed. Draft orders are cut (SPEC §2, C2 "do not scaffold them"), PARITY.md's nav lists subitems only under Products, and CLAUDE.md §8 says a cut feature is not rendered at all — an empty state that can never fill is not the lesser evil. Blast radius checked: `nav.test.ts` used `/orders/drafts` only as a sample path (`storeHref` is pure; `isItemSelected` matches via the `/orders` prefix), both repointed at real routes. `ComingOnline` in `page-skeleton.tsx` was its last consumer and is deleted with it.
2026-08-28T20:20Z | WS-H | The footer's newsletter fallback now uses F1's `InertForm` (and the input is `required`). It previously rendered a bare `<button>Subscribe</button>` outside any form with no handler — a dead control on every themed page, live on the seeded demo, since nothing supplies `data.slots.newsletterForm`. Pinned by a `sections.test.tsx` assertion that was mutation-checked against the old markup.
2026-08-28T20:20Z | WS-H | `/account/login` and `/account/register` resolve through `shopContext()` rather than `resolveShopSlug()`. The slug only parses the Host, so an unknown subdomain rendered a working sign-in form for a store that does not exist; they now 404. The layout keeps `optionalShopContext()` — `notFound()` from a ROOT layout is a Next error, not a 404.
2026-08-28T20:20Z | WS-H | H3's clean-machine quickstart was verified WITHOUT `docker compose down -v`. That wipes the Postgres and Redis volumes shared by ~20 live sibling worktrees mid-swarm, which is destructive to other agents' work, not a local reset. Verified instead: `.env.example` mirrors `packages/config/src/env.ts` exhaustively (zero drift both ways, so `cp .env.example .env` needs no edits), `pnpm install`, `pnpm worktree:env --migrate` and a full boot of all four apps from a clean database. A true pristine-volume run needs the swarm quiesced and is the one Definition-of-Done #1 step still owed.
2026-08-28T20:20Z | WS-H | `pnpm worktree:env` can silently produce a broken `REDIS_URL`: when all 15 slots are taken it prints "all 15 Redis slots are taken", then writes `redis://localhost:6379/` with NO db index (i.e. db0) and still exits 0. Sessions live in Redis, so the worktree then shares a keyspace. Slots 3 and 5 were empty in the live server while the allocator's registry called them taken — the registry does not reclaim finished worktrees.
2026-08-29 | WS-D | The `charge-saved-card` and `capture` routes now settle the order: `pending|authorized` → `paid` once captured payments cover `order.total` (route-level, because Order is C's model and pay stays order-agnostic; status-guarded so refunded/voided orders are never resurrected; no partial-paid state exists so under-collection changes nothing). Found in review — the charge block prefills the outstanding balance, then left the badge on "Payment pending" after collecting it.
2026-08-29 | WS-D | `orders/paid` fires when money is CAPTURED, not on approval: `charge()` skips it for authorize-only, `capturePayment` fires the `onPaid` seam (and takes it from the capture route). An authorization has moved no money, and notify.ts always documented capture as the emitter.
2026-08-29 | WS-D | `charge()` refuses a reused idempotency key whose card/amount/currency differ from the original (conflict), mirroring `replayRefund` — required because the admin's charge modal now REUSES its key across transport-failure retries (one key per attempt, minted at modal open, rotated on decline or amount edit) instead of minting per click, which was a double-charge on any lost response.
2026-08-29 | WS-D | A `pending` PaymentRefund older than 15 min is expired under the payment lock (status `failed`, key freed): a reservation whose process died mid-refund otherwise holds the cap and answers "in progress" forever. Safe to retry because the refund's idempotency key now travels to the adapter (Stripe `idempotencyKey`, Maverick `idempotency-key` header) — a same-key retry replays at the processor rather than refunding twice.
2026-08-29 | WS-D | `chargeSavedCardInput` carries `orderId`/`checkoutId` in `packages/contracts` (was a route-local `.extend`) and routing-rule `minAmount`/`maxAmount` are `positiveMoneySchema` — a negative bound can never match a charge, so it stored a rule that silently routed nothing.
2026-08-28 | WS-G | Deleting a webhook subscription is a SOFT delete (`WebhookSubscription.deletedAt`), and `@@unique([appId, topic, url])` is dropped from the schema (no code addressed rows by that key — verified by grep; the service-level duplicate check keeps the 409 for live rows). Reason: `WebhookDelivery` cascades on subscription delete, so a hard delete silently destroyed the delivery log the UI explicitly promises survives ("Past deliveries stay in the log"). Deleted subscriptions stop matching in the worker and disappear from the app's webhook list, but their delivery rows remain readable in the app's log.
2026-08-28 | WS-G | "Send test event" targets ONE subscription: `emitWebhookEvent` gains an optional `{subscriptionId}` (additive; job schema field is optional so queued events from before this change still parse) and the worker narrows its subscription query to it. Without this, a test fired on app A's orders/create subscription also POSTed a `{test:true}` non-order-shaped body at every other subscription on that topic — other apps included — and polluted their delivery logs.
2026-08-28 | WS-G | The Analytics dashboard treats TODAY as an open day even when rollup rows for it exist. The worker upserts today every 5 minutes, so "any day with rollup rows is closed" made the whole dashboard up to 5 minutes stale the moment the worker booted — while the Live card (raw reads) already showed the new order, so the two visibly disagreed during the demo's buy-then-check-analytics beat. SPEC §13 says "rollups + today's raw"; now it is true.
2026-08-28 | WS-G | `refunds/create` webhook body carries `refund: { id, amount: Money }` alongside the shared order body. The uniform orders/* body left a partial refund indistinguishable from a full one — the topic said "refund created" and the only money in the body was the order's FULL original total.
2026-08-28 | WS-G | `customers/create` is emitted from C4's customer service (the shared creation seams: `createCustomer`, and `findOrCreateByEmail` only when it actually created) via `services/customers/events.ts`, a typed wrapper mirroring catalog/events.ts. It was the one WEBHOOK_TOPICS member with no producer — subscribable in the UI, never fired. Service-level so admin, checkout and storefront registration all emit it without three call sites in three workstreams' routes.
2026-08-28 | WS-G | `analyticsRangeQuery.granularity` is REMOVED from the contract. It accepted `hour|day` and the implementation ignored it — daily buckets either way. No caller ever sent it (grep: the admin builds ranges via rangeFor/rangeQueryString, which never set it); an accepted-but-ignored parameter on a public-ish contract is a lie to the next integrator.
2026-08-28 | WS-H | Smoke flow (d) runs on a shop of its own (signupFreshShop helper), not demo: publishing is shop-wide, so the old flow permanently restyled the seeded Aurora theme (a pre-demo `pnpm e2e` defaced the demo store, §8) and raced the parallel flows reading demo's storefront. The 90s publish-poll stays — the builder's preview iframe warms the storefront's 60s published-theme cache even on a brand-new host (the layout ignores ?preview=).
2026-08-28 | WS-F | `enqueueThemeGeneration` collapsed onto WS-G's shared producer, completing its own TEMPORARY-SHAPE note: the private BullMQ Queue/connection and `closeThemeQueue` (zero callers) are gone. The job name was three independent 'ai-theme-generation' literals held together by comments — it is now `JOB_NAMES.aiThemeGeneration` on both sides, and `JobDefinition.name` is typed `JobName`, so producer/consumer drift is a compile error instead of a job the worker has no handler for while the merchant watches a thinking-bubble forever (the enqueue path runs in no CI test — key unset — so the compiler is the only guard). Retention also returns to the shared 1000 — the private 100 silently shortened the deterministic-jobId idempotency window queue.ts documents.
2026-08-28T20:50Z | WS-H | The seven admin index tables are `fullWidth`; `/locations` deliberately is NOT. PARITY.md's "full-width for index tables" applies to the IndexTable indexes (products, collections, inventory, customers, discounts, apps — orders already had it). Locations is a settings sub-page: it has a `backAction` to Settings, renders a hand-built stacked list rather than an IndexTable, and Shopify keeps it narrow. Before this, orders was the ONLY full-width index, so the content area visibly jumped width when navigating between Orders and Products.
2026-08-28 | WS-H | H4 motion: index/analytics queries opt into keepPreviousData via a new optional `keepPreviousData` flag on useApiQuery (additive change to WS-A's lib/api.ts under the H4 cross-cutting licence). Tab/filter/sort/cursor/range changes now keep the previous rows on screen with the IndexFilters spinner instead of re-flashing the whole-page skeleton — isPending only ever fires on first load. Any new index page should pass it too.
2026-08-28 | WS-H | H4 motion: modals must stay mounted and be driven by `open` — conditional `{x ? <Modal/> : null}` kills Polaris's exit transition. Refactored payments ConnectModal and locations LocationDialog to the sticky-content pattern (state keeps the last subject so content doesn't blank during the exit fade). PageSkeleton gains `layout="detail"` (back arrow + two-column) used by all 7 detail/form pages; analytics gets a bespoke skeleton reserving the 280px chart. Builder ThinkingDots (off-token 1.2s keyframes) replaced with Polaris Spinner; builder preview iframe no longer remounts on device toggle.
2026-08-28 | WS-H | H4 motion, storefront/checkout: global prefers-reduced-motion collapse added to storefront globals.css (covers theme-engine sections and the builder preview). Checkout skeletons drop animate-pulse and Pay-now drops its hover transition (checkout is nearly motionless per PARITY §Motion). Slideshow dots lose transition-all (was animating width); collection-list hover zoom clipped by a wrapper (overflow-hidden on an img clips nothing) and both card zooms go 500ms→300ms.
2026-08-28 | WSF | Builder conversation writes are compare-and-set on `updatedAt` (route AND worker — the worker's window is a 30–60s model call, so a plain write-back deletes concurrent messages), the worker resolves every failure to a `failed` bubble on its final queue attempt, GET /conversation sweeps `pending` messages older than 5 min to `failed`, the admin polls to 6 min, and the Anthropic call is bounded (120s, no SDK retries) so a generation always finishes inside the sweep window. One state machine, four coordinated numbers — change them together.
2026-08-28 | WSF | Publish still does NOT bust the storefront's theme cache (SPEC §12's parenthetical): the 60s fetch cache is WS-E's (`apps/storefront` + `services/storefront`), and reaching into it mid-swarm risks their in-flight work. Accepted up-to-60s staleness after Publish for now; needs an E-owned revalidation hook the publish route can call — flagged here rather than fixed cross-boundary.
2026-08-28 | WSF | Onboarding installs the aurora preset instead of SPEC §12's "auto-generate from shop name + industry question" — cut per CLAUDE.md §8 (the preset is instant, never needs a key, and the builder chat covers personalisation one message later). DECISIONS line 99 recorded the never-throws half; this records the dropped generation half.
2026-08-28 | WS-C | Fulfilment is stock-neutral for units the order already decremented: prior `sold` adjustments referencing the order (checkout's reservation, re-pointed by afterSale) are netted per variant and only the shortfall moves at fulfil time. Resolves the aggregate contradiction of the two earlier decisions ("checkout reserves" + "fulfil decrements"), which double-decremented every storefront sale and made two-location fulfilment 409 on stock that existed. Manual/API orders still take stock at fulfil.
2026-08-28 | WS-C | `refundOrder` owns refund idempotency, not the pay router: the key is persisted on `Refund` (nullable, unique per shop — migration 004_wsc_refund_idempotency) and a replay returns the existing refund untouched. Pay's replay alone let a replayed processor call still write a second Refund row, double-count refundedTotal and double-restock. The admin sends a per-attempt nonce (kept only across network-level retries), never a form-content-derived key.
2026-08-28 | WS-C | A fully refunded order can be cancelled and keeps `financialStatus: refunded` (never voided); cancel's restock nets out units that restock:true refunds already returned. Previously refund never closed the order and cancel refused refunded — a fully refunded order was stuck open forever.
2026-08-28 | WS-C | `fulfillmentStatus` counts refunded units as settled (shared `fulfillmentStatusFor`): refunding the unshipped remainder completes the order, and refunded units cannot be re-fulfilled (409). Matches the admin's `remainingToFulfil` semantics.
2026-08-28 | WS-C | Discount application order is deterministic inside the engine — type precedence, then `createdAt`, then `id` — because same-type percentage stacking is order-dependent by a cent and the checkout caller's findMany has no orderBy. And `free_shipping` minimums are evaluated on the POST-line-discount subtotal (percentage/fixed stay pre-discount), so a coupon that prices you below the free-shipping bar no longer has the automatic free-shipping discount zero the paid rate anyway.
2026-08-28 | WS-C | `oncePerCustomer` is enforced by the engine via the optional additive `priorUsage` input (Record<discountId, count>), rejecting as `usage_limit` — no new rejection enum member, existing storefront copy reads correctly. Callers that omit it (guest checkout, and WS-E until wired) keep exactly the old behavior. Cancel gives usage back: usedCount decremented and DiscountRedemption rows deleted in the cancel transaction.
2026-08-28 | WS-C | Customers with orders cannot be deleted (409) — the FK is SET NULL and a bare delete silently orphaned order history. Index-table selection checkboxes were REMOVED from the customers and discounts indexes (orders precedent) rather than inventing bulk actions the API does not have; Orders ▸ Drafts nav item and placeholder page removed — PARITY lists no Drafts subitem and C5 shipped without draft orders.
2026-08-29 | WS-A | Staff MUTATIONS (POST/PUT/DELETE /admin/api/settings/staff) require an owner/admin actor; the `settings` area alone only reads. A `staff` user holding `settings` could otherwise self-promote to admin in one request, collapsing SPEC §8's per-area model. Also closed: `createStaff` now enforces the one-owner rule the PUT path always had (a POSTed second owner was undemotable and undeletable forever), and the owner's permissions row is immutable (owners bypass the map, so writing it was only a repeatable force-logout via session destruction).
2026-08-29 | WS-A | Staff email is case-folded at the contract boundary (login/signup/invite inputs `.transform(toLowerCase)`) — same rule customer email already follows (WSC line above). Postgres compares are case-sensitive, so `Paul@x.dev` at signup and `paul@x.dev` at login were two different people, with no password-reset flow to recover.
2026-08-29 | WS-A | `RESERVED_SHOP_SLUGS = {www, admin, api}` (packages/config): refused at signup and never derived by slugify. `www` never resolves a storefront (lib/host.ts refuses it) and prod Caddy routes `admin.*`/`api.*` to the apps — a shop on these slugs signed up fine and then had no web address.
2026-08-29 | WS-A | dbForShop now refuses create/createMany/upsert on the Shop model (reads/updates stay scoped to id=shopId) and stamps nested `upsert.create` envelopes — both extend line 27's fail-closed rule: a scoped client must not mint tenants, and every nested insert path is stamped, not just create/createMany/connectOrCreate.
2026-08-29 | WS-A | `worktree-env.sh` hard-fails when all 15 Redis slots are taken instead of writing a db-less REDIS_URL and exiting 0 (the WS-H line above documented the trap: die() inside $() only exits the subshell, so the worktree silently landed on shared db 0). Slot reclamation for finished worktrees remains manual.
2026-08-29 | WS-H | Customers/discounts indexes now actually match the WS-C line above ("selection checkboxes REMOVED, orders precedent"): H3's parallel-branch bulk Delete landed after that decision was logged and contradicted it on main — removed (`selectable={false}`), not repaired, because the API deliberately has no bulk actions there and a customer-with-orders delete is a 409 by design. If bulk actions ever return, they must be `Promise.allSettled` + invalidate-in-`finally`; the removed version reported partial destruction as total failure and left deleted rows in the table.
2026-08-29 | WS-H | Clarifying two now-contradictory settled lines: WS-E's "account pages read only resolveShopSlug(), never shopContext()" is superseded FOR /account/login and /account/register only (WS-H line of 2026-08-28T20:20Z) — those two resolve through shopContext() so an unknown subdomain 404s instead of rendering a working sign-in form. Other account pages keep E's rule.
2026-08-29 | WS-H | Seed history now really ends at the end of yesterday: fulfillment/refund/cancel clamps and the sold-out corrections were still stamped against the run instant (violating the whole-UTC-days line of 2026-08-28), and the determinism fingerprint carried no timestamps so it could not notice. Both clamp to `endOfHistory` (23:59:59.999 UTC yesterday) and the fingerprint now includes adjustment timestamps.
2026-08-29 | WS-H | `seed-if-empty` gates on rollup rows, not shop existence: seedDemo creates the Shop first and is not transactional, so a mid-seed crash left a half-built demo that every prod boot then refused to repair. Rollups are written last, which makes them the completion marker; a shop without them is rebuilt.
2026-08-29 | WS-H | Smoke flows (a) and (c) clean up after themselves (product deleted, order refunded with restock, via the session-authenticated admin API): §8's "seed data IS the demo" applies to them exactly as it did to flow (d) — repeated runs were accreting active Smoke Tees in Aurora's storefront and permanently draining Basin Wool Socks (M) stock.
2026-08-29 | shared | Brand is **"Shopify"**, not "Merchant" (owner request), reversing SPEC §1. Admin chrome renders the Shopify name and bag mark: `BRAND_NAME` in `packages/config/constants`, `apps/admin/public/shopify-bag.svg` (top bar via `Frame logo`, login lockup via `BrandLogo`, favicon); `_merchant_*` cookies and `x-merchant-*` webhook headers become `_shopify_*` / `x-shopify-*`. This is a private study clone, never deployed or distributed. NOT renamed: the `@merchant/*` package scope (would collide with the real `@shopify/polaris` in node_modules) and infra ids (db name, `merchant-assets` bucket) — invisible to the KPI, and renaming them breaks every worktree's env.
